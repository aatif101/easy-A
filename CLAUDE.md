# CLAUDE.md

**[`AGENTS.md`](AGENTS.md) is the canonical agent entry point for this repository.** Read it
first — it holds the read-order, the durable hard constraints, and where to find current state.
This file is intentionally only a pointer so the two cannot drift; **do not restate AGENTS.md
content here.**

Read order: `AGENTS.md` → `.planning/STATE.md` (current position + next action) →
`.planning/PROJECT.md` (scope, milestones, decisions) → `.planning/ROADMAP.md` (phase sequence) →
`README.md` (commands and local setup).

Current milestone is **MVP 1** — see `.planning/PROJECT.md` and `.planning/ROADMAP.md`. Live,
volatile facts (section/grade counts, `origin/main` SHA, test baselines) live **only** in
`.planning/STATE.md`.
