---
phase: 06-mvp1-p3-all-tampa-section-ingestion-10-3-782
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - scripts/validate_tampa_ingest.py
  - tests/refresh/test_validate_tampa_ingest.py
autonomous: true
requirements: [REQ-COVERAGE-03]

estimate:
  tokens: 60000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the 33 suffix-variant base courses (derived from the committed config) has stored sections that all resolve to its exact base course_number — no L-variant sections leak into a base course, and each L-variant course holds its own sections."
    - "After the full run: 0 non-Tampa rows (unsupported_campus_section = 0), data quality reports 0 errors for term 202701."
    - "Stored section count, coverage_metadata section-count sum, and the rankings-search total agree for term 202701."
    - "Config vs stored data are reconciled: every stored course for the term is a configured target (the 5-vs-10-vs-1402 discrepancy is closed)."
    - "Every ranking row is honest: course-backed rows (score_source course/instructor_course) have effective_n>0, and any effective_n=0 row carries the explicit global fallback source (D-20); the 10 pilot courses remain course-backed."
  artifacts:
    - scripts/validate_tampa_ingest.py
    - tests/refresh/test_validate_tampa_ingest.py
  key_links:
    - "config/course_targets.toml -> derive 33 base/suffix pairs -> DB section->course join -> exact-course assertion"
    - "section_rankings (score_source, effective_n) -> honest-coverage assertion (D-20)"
    - "coverage_metadata() + Section count + rankings-search total -> reconciliation assertion"
  prohibitions:
    - "Must NOT weaken the campus or suffix guard to make a validation pass — a real violation is a phase failure, not a reason to relax a check."
    - "Must NOT commit raw grade export files (*.xlsx/*.xls) or courses.csv."
    - "Must NOT present a global-prior fallback (effective_n=0) as course history (D-20)."
    - "Must NOT modify the existing scoring model, coverage.py guards, or quality checks to satisfy an assertion."
---

<objective>
Validate the full-scale ingestion against the phase success criteria and close the locked
suffix-scale and config-reconciliation gates.

Three scale assertions run against the post-ingest hosted Supabase DB: (1) the locked
suffix-guard scale validation — all 33 suffix-variant base courses ingested their exact course only
(not the L-variant), across the full set (locked user decision 2); (2) coverage integrity and
reconciliation — 0 non-Tampa rows, quality 0 errors, stored/coverage/API counts agree, and every
stored course is a configured target (closing the 5-vs-10-vs-1402 discrepancy); (3) the honest-
coverage contract at scale (D-20) — no course presents a global fallback as course history, and the
10 pilot courses keep their real Fall-2024 history.

Purpose: prove REQ-COVERAGE-03's success criteria with real, measured assertions rather than
assumption, and guarantee the merged guards generalized correctly at ~29x prior scale.

Output: `scripts/validate_tampa_ingest.py` (reusable scale validator) + its unit test, and a green
run of all three scale assertions against the ingested universe.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-mvp1-p3-all-tampa-section-ingestion-10-3-782/06-RESEARCH.md
@.planning/phases/06-mvp1-p3-all-tampa-section-ingestion-10-3-782/06-PATTERNS.md
@src/easy_a/refresh/coverage.py
@src/easy_a/refresh/targets.py
@src/easy_a/quality/checks.py
@src/easy_a/rankings/cache.py
@src/easy_a/common/campus.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the scale validator (suffix-exact, reconciliation, honest-coverage) + unit test</name>
  <files>scripts/validate_tampa_ingest.py, tests/refresh/test_validate_tampa_ingest.py</files>
  <read_first>
    - src/easy_a/refresh/targets.py (load_targets — enumerate targets and derive base/suffix pairs)
    - src/easy_a/refresh/coverage.py (coverage_metadata — the coverage-count source for reconciliation)
    - src/easy_a/quality/checks.py (run_quality_checks, unsupported_campus_section, no_historical_analytics)
    - src/easy_a/rankings/cache.py (SectionRankingCache: score_source, effective_n columns)
    - src/easy_a/analytics/confidence.py (ScoreSource: course / instructor_course / subject / global_)
    - src/easy_a/common/campus.py (same_campus / SUPPORTED_CAMPUS)
    - src/easy_a/models/core.py and src/easy_a/models/sections.py (Course, Section joins)
    - tests/refresh/test_targets.py (db_session fixture + test style)
  </read_first>
  <behavior>
    - derive_suffix_pairs(targets): given the committed targets, returns the set of (subject, base_number, suffix_number) where both a base number and a base+trailing-letter number exist for the same subject (must yield 33 pairs for the real config).
    - assert_suffix_exact_ingest(session, term, pairs): for each pair, every Section joined to Course (subject, base_number) has Course.number == base_number, and the suffix Course (subject, suffix_number) owns its own sections; raises AssertionError naming the offending course on any leak.
    - assert_coverage_reconciled(session, term, targets): stored Section count for the term == sum of coverage_metadata section_count == rankings-search total; and every distinct stored (subject, number) for the term is present in targets; raises AssertionError with the mismatch on failure.
    - assert_honest_coverage(session, term): for every section_rankings row, score_source in {course, instructor_course} implies effective_n > 0, and effective_n == 0 implies score_source == global_; raises AssertionError naming the offending CRN otherwise.
    - Each assertion function is pure over a SQLAlchemy Session so it can be unit-tested on an in-memory SQLite fixture with a few synthetic courses/sections/rankings (no network).
  </behavior>
  <action>
    Write tests/refresh/test_validate_tampa_ingest.py FIRST (red): using the db_session fixture style
    from tests/refresh/, seed a tiny synthetic dataset — a base course CHM 2045 with two Tampa sections,
    a suffix course CHM 2045L with one section, and section_rankings rows including one course-backed
    (score_source=course, effective_n>0) and one honest fallback (score_source=global_, effective_n=0).
    Assert each validator function passes on the honest dataset and raises AssertionError on injected
    violations (a 2045L section mis-attributed to 2045; a stored course absent from targets; a
    fabricated row with score_source=course but effective_n=0). Then implement
    scripts/validate_tampa_ingest.py following the build_parser()/main(argv)->int shape from
    catalog/cli.py: `from __future__ import annotations`, stdlib imports, then absolute
    `from easy_a.refresh.targets import load_targets`, `from easy_a.refresh.coverage import coverage_metadata`,
    and the model imports. Implement derive_suffix_pairs (regex ^(\d{4})([A-Z])$ on number, base present
    in same subject — reuse the CourseTarget number pattern semantics, do not invent a new one),
    assert_suffix_exact_ingest, assert_coverage_reconciled, and assert_honest_coverage as above. main()
    accepts --term (default 202701) and --targets (default config/course_targets.toml), opens a session
    via get_session_factory(), runs all three assertions, prints a one-line PASS/FAIL summary per check,
    and returns non-zero if any assertion fails. Do NOT modify coverage.py, quality/checks.py, the
    scoring model, or any guard — this is read-only validation.
  </action>
  <verify>
    <automated>uv run pytest tests/refresh/test_validate_tampa_ingest.py -q</automated>
    <fails_when>pytest exit code is non-zero, OR any case reports "failed", OR the collected test count is 0.</fails_when>
    <automated>uv run python -c "from easy_a.refresh.targets import load_targets; from pathlib import Path; import scripts.validate_tampa_ingest as v; print('pairs', len(v.derive_suffix_pairs(load_targets(Path('config/course_targets.toml')).targets)))"</automated>
    <fails_when>The command raises, OR prints a pair count other than "pairs 33".</fails_when>
    <automated>git diff --exit-code -- src/easy_a/refresh/coverage.py src/easy_a/quality/checks.py</automated>
    <fails_when>git diff reports any change to coverage.py or quality/checks.py (exit code non-zero).</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/refresh/test_validate_tampa_ingest.py passes: each validator passes on the honest synthetic dataset and raises on each injected violation.
    - derive_suffix_pairs over the committed config yields exactly 33 base/suffix pairs.
    - The validator is read-only; coverage.py, quality/checks.py, and the scoring model are unchanged.
  </acceptance_criteria>
  <done>A tested, read-only scale validator exists with all three assertion functions and a confirmed 33-pair suffix derivation from the committed config.</done>
</task>

<task type="auto">
  <name>Task 2: Suffix-exact + reconciliation gate against the ingested universe</name>
  <files>scripts/validate_tampa_ingest.py</files>
  <read_first>
    - scripts/validate_tampa_ingest.py (the validator built in Task 1)
    - src/easy_a/quality/checks.py (unsupported_campus_section = the 0-non-Tampa gate)
    - .planning/STATE.md (baseline counts for context)
  </read_first>
  <action>
    Run the validator against the live post-ingest hosted Supabase for term 202701, exercising the
    suffix-exact-ingest and coverage-reconciliation assertions across the FULL set (locked user
    decision 2: all 33 suffix-variant base courses ingest their exact course only). Also run
    check_data_quality.py and confirm 0 errors and specifically 0 unsupported_campus_section findings
    (0 non-Tampa rows). If any assertion fails, treat it as a real ingestion defect to fix by
    re-running the relevant subject via the 06-02 orchestrator — NOT by weakening a guard or a check.
    Record the measured counts (courses, sections, subjects) as dated evidence in the 06-03 SUMMARY.
  </action>
  <verify>
    <automated>uv run python scripts/validate_tampa_ingest.py --term 202701 --targets config/course_targets.toml</automated>
    <fails_when>Exit code is non-zero, OR any per-check line reports FAIL (suffix-exact or reconciliation), OR a stored course is reported absent from the configured targets.</fails_when>
    <automated>uv run python scripts/check_data_quality.py --term 202701 --json | uv run python -c "import sys,json; r=json.load(sys.stdin); f=r.get('findings', r if isinstance(r,list) else []); print('errors', sum(1 for x in f if x.get('severity')=='error')); print('noncampus', sum(1 for x in f if x.get('check_id')=='unsupported_campus_section'))"</automated>
    <fails_when>The printed error count is not "errors 0", OR the non-Tampa count is not "noncampus 0", OR the JSON cannot be parsed.</fails_when>
  </verify>
  <acceptance_criteria>
    - All 33 suffix-variant base courses ingested their exact course only — no L-variant leakage — asserted against the full ingested DB.
    - 0 non-Tampa rows (unsupported_campus_section = 0) and 0 data-quality errors for term 202701.
    - Stored section count, coverage_metadata sum, and rankings-search total agree; every stored course is a configured target.
    - The SUMMARY records the measured, dated counts.
  </acceptance_criteria>
  <precondition>The 06-02 full-scale run completed with 0 failed subjects and hosted Supabase holds the ingested term 202701 rows.</precondition>
  <done>The suffix guard and coverage reconciliation hold at full scale with 0 non-Tampa rows and 0 quality errors.</done>
</task>

<task type="auto">
  <name>Task 3: Honest-coverage (D-20) + tracked-exports gate at scale</name>
  <files>scripts/validate_tampa_ingest.py</files>
  <read_first>
    - scripts/validate_tampa_ingest.py (assert_honest_coverage built in Task 1)
    - src/easy_a/rankings/cache.py (score_source / effective_n on section_rankings)
    - .planning/PROJECT.md (D-19 never commit raw grade exports; D-20 global fallback is not course history)
  </read_first>
  <action>
    Run the honest-coverage assertion against the live post-ingest DB for term 202701: every
    section_rankings row that claims course-backed history (score_source course/instructor_course) has
    effective_n>0, and every effective_n=0 row carries the explicit global fallback source — so the
    ~1,392 newly ingested courses without sourced Fall-2024 history are represented as honest
    effective_n=0 / score_source=global states, never as course history (D-20). Confirm the 10 pilot
    courses (ACG 2021, ACG 2071, AMH 2020, ANT 2000, BSC 1005, ECO 2013, ENC 1101, MAC 1105, MAC 2311,
    PSY 2012) remain course-backed (score_source=course, effective_n>0). Finally run the tracked-exports
    guard verbatim from Phase 05 to prove no raw grade export or the courses.csv snapshot was committed.
    Do NOT fabricate history for the new courses and do NOT modify the scoring model.
  </action>
  <verify>
    <automated>uv run python scripts/validate_tampa_ingest.py --term 202701 --targets config/course_targets.toml</automated>
    <fails_when>Exit code is non-zero, OR the honest-coverage check line reports FAIL (any course-backed row with effective_n=0, or any effective_n=0 row not marked global), OR any of the 10 named pilot courses is reported as no longer course-backed.</fails_when>
    <automated>easy_a_tracked_exports=$(git ls-files -- '*.xlsx' '*.xls'); test -z "$easy_a_tracked_exports"; git status --porcelain courses.csv</automated>
    <fails_when>git ls-files prints any .xlsx/.xls path (the test exits non-zero), OR courses.csv appears in git status --porcelain output (it must remain git-ignored and uncommitted).</fails_when>
  </verify>
  <acceptance_criteria>
    - No ranking row presents a global fallback as course history; every effective_n=0 row is score_source=global (D-20).
    - All 10 named pilot courses remain course-backed (score_source=course, effective_n>0).
    - No *.xlsx/*.xls file is git-tracked and courses.csv is not committed.
    - The scoring model is unchanged.
  </acceptance_criteria>
  <precondition>The 06-02 full-scale run completed and section_rankings is populated for term 202701.</precondition>
  <done>The honest-coverage contract holds across all ~1,402 courses at scale, the pilot history is intact, and no export/snapshot data was committed — REQ-COVERAGE-03 success criteria are proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingested hosted Supabase -> validator (read-only) | Validation reads stored rows; it must not mutate data or relax a guard to pass |
| section_rankings (score_source/effective_n) -> honest-coverage claim surface | The boundary where a fabricated-history claim would be caught (D-20) |
| git working tree -> commit | Raw exports / the courses.csv snapshot must never cross into a commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering | suffix/campus data integrity at scale | high | mitigate | assert_suffix_exact_ingest over all 33 pairs + unsupported_campus_section = 0 against the full DB; a failure is fixed by re-ingest, never by weakening a guard |
| T-06-07 | Repudiation | honest-coverage misrepresentation (D-20) | high | mitigate | assert_honest_coverage proves no global fallback is presented as course history and the 10 pilot courses stay course-backed |
| T-06-03 | Information disclosure | git working tree | high | mitigate | Verbatim Phase-05 tracked-exports guard + courses.csv git-status check |
| T-06-08 | Tampering | reconciliation counts drift | medium | mitigate | Stored count == coverage_metadata sum == rankings-search total, and every stored course is a configured target |

_No package installs in this phase — no supply-chain (T-06-SC) checkpoint applies._
</threat_model>

<verification>
- `uv run pytest tests/refresh/test_validate_tampa_ingest.py -q` green.
- `scripts/validate_tampa_ingest.py --term 202701` passes all three checks against the live DB.
- `check_data_quality.py --term 202701 --json` reports 0 errors and 0 unsupported_campus_section.
- Tracked-exports guard: no *.xlsx/*.xls tracked; courses.csv not committed.
</verification>

<success_criteria>
- All 33 suffix-variant base courses ingest their exact course only, validated across the full set.
- Full Tampa Spring 2027 set ingested; stored/coverage/API counts agree; 0 non-Tampa rows; quality 0 errors (REQ-COVERAGE-03 criterion 1 + 2).
- Config and stored data reconciled: every stored course is a configured target.
- Honest-coverage contract holds at scale (D-20); the 10 pilot courses stay course-backed; no export/snapshot committed.
</success_criteria>

<artifacts_this_phase_produces>
Symbols/files created or changed by this plan (Wave 3):
- `scripts/validate_tampa_ingest.py` (NEW) — read-only scale validator; `build_parser()` / `main(argv: list[str] | None = None) -> int`; CLI flags `--term` (default `202701`), `--targets` (default `config/course_targets.toml`); functions `derive_suffix_pairs(targets)`, `assert_suffix_exact_ingest(session, term, pairs)`, `assert_coverage_reconciled(session, term, targets)`, `assert_honest_coverage(session, term)`.
- `tests/refresh/test_validate_tampa_ingest.py` (NEW) — unit tests for each validator over a synthetic in-memory DB.

No production code (`coverage.py`, `quality/checks.py`, scoring model) is modified — this plan is read-only validation.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-mvp1-p3-all-tampa-section-ingestion-10-3-782/06-03-SUMMARY.md` when done.
</output>
