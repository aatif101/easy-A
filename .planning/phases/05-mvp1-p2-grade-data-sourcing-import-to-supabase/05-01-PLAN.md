---
phase: 05-mvp1-p2-grade-data-sourcing-import-to-supabase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbooks/grade-import.md
  - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md
autonomous: false
requirements: [REQ-GRADES-01]
user_setup:
  - service: usf-infocenter-grade-export
    why: "Approved USF InfoCenter grade-distribution XLSX is a Codex-owned, out-of-band, authenticated data source; it cannot be fetched from inside this codebase (D-08/D-09)."
    env_vars:
      - name: DATABASE_URL
        source: "Supabase project settings -> Database -> Connection string (pooler); already documented in README Environment. Export it (or source .env) before any import/cache/validate command."
    dashboard_config:
      - task: "Source the approved InfoCenter grade-distribution XLSX for the 10 ingested Tampa courses, confirm each workbook's actual historical Banner term, and place the file(s) under the git-ignored data/ path (never inside a tracked path, never renamed to bypass the .gitignore *.xlsx/*.xls globs)."
        location: "USF InfoCenter (authenticated SGDIS report) — Codex-owned sourcing step, external to this repo"

estimate:
  tokens: 45000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One ingested Tampa course covered by the sourced export (the tracer course) reports effective_n > 0 and score_source = course in `scripts/analyze_course.py --term 202701` after the historical import + the separate 202701 cache-only rebuild (D-06/D-20). [edge:boundary — a course with sourced rows crosses from the effective_n=0 fallback to real course history]"
    - "The tracer course's RAW `total_grade_count` in Supabase equals the sum of that course's Total Grades rows in the source workbook — validated against the raw count, never against the Bayesian-smoothed easiness_score (D-06). [edge:precision — rounding/shrinkage lives only in the smoothed score; the raw count is the exact-match contract]"
    - "`docs/runbooks/grade-import.md` documents the two-step recipe (historical `--term` grade import, then a SEPARATE `--term 202701 --skip-grades` cache-only rebuild) and the scope-to-current-10-courses rule, so the imported history is actually visible in `GET /api/v1/rankings/search`."
    - "`05-IMPORT-RECORD.md` records the tracer course's per-course aggregate result (source term, ingest_run_id, raw total_grade_count, effective_n, score_source) — a derived aggregate + provenance record, containing no raw per-row grade data."
    - "No `.xlsx`/`.xls` export file, and no derived file containing raw per-row grade data, is tracked in git after the tracer import (D-19)."
  artifacts:
    - docs/runbooks/grade-import.md
    - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md
  key_links:
    - "`scripts/refresh_data.py --term 202701 --skip-catalog --skip-schedule --skip-grades --skip-syllabi` connects the imported historical GradeDistribution rows to the live `section_rankings` cache the API serves — without this second pass the import stays invisible (refresh/service.py keys its cache stage off the term passed on that invocation)."
    - "`resolve_course_id()` attributes each GradeDistribution row to a course_id so `get_current_section_historical_analytics` aggregates it onto the live 202701 sections (fixed in MVP1-P1)."
  prohibitions:
    - "MUST NOT present a global-prior fallback (effective_n=0, score_source=global) as course history (D-20); `analyze_course.py` Source/Effective N columns must show global for any uncovered course. Enforcement evidence: ScoreSource/effective_n distinction in src/easy_a/analytics/confidence.py + queries.py."
    - "MUST NOT commit any raw grade export — a `.xlsx`/`.xls` file, or a derived file containing raw per-row grade data (D-19). Enforcement evidence: .gitignore excludes *.xlsx/*.xls/data/research/raw; verified by `git ls-files -- '*.xlsx' '*.xls'`."
    - "MUST NOT silently coerce a blank / suppressed canonical grade-count cell to zero (D-06/D-07, OQ-04). Enforcement evidence: fail-closed `_blank_count_cell_message` rejection in src/easy_a/grades/parser.py."
    - "MUST NOT double-count a re-exported / re-imported workbook (D-04). Enforcement evidence: uq_grade_distributions_term_crn_source + source_hash-aware upsert in src/easy_a/grades/ingest.py."
---

<objective>
Prove the real end-to-end grade-import loop on ONE ingested Tampa course, against the actual hosted
Supabase database, and leave behind the operator runbook the repo currently lacks.

The loop: source-or-scope one course's InfoCenter rows -> import under the workbook's own historical
Banner term -> rebuild the live 202701 rankings cache -> confirm the course flips from the honest
`effective_n = 0` / `score_source = global` fallback to `effective_n > 0` / `score_source = course`,
with the RAW `total_grade_count` matching the source workbook's own Total Grades sum.

This phase adds NO new production code (confirmed in 05-PATTERNS.md — 0 new production code paths).
Every mechanism is already built and hardened in MVP1-P1. The work here is operational (source the
right file, scope it, run the right two commands, validate against the right numbers) plus two
tracked documentation artifacts. Do NOT invent a "grade import service", a "validation script", or
a new coverage table — they already exist (`scripts/refresh_data.py`, `scripts/analyze_course.py`,
`scripts/check_data_quality.py`, and the `no_historical_analytics` quality finding).

Purpose: de-risk the production import by proving it on one course under a human-verify gate before
Plan 05-02 scales to the rest.
Output: `docs/runbooks/grade-import.md` (new), `05-IMPORT-RECORD.md` (new), and one course's real
grade history live in Supabase.

Edge resolutions (from the spec-less probe — 3 unresolved edges surfaced for REQ-GRADES-01, none
dropped):
- boundary (0 vs 1 sourced rows; term with data vs none) -> resolved as must_haves truth #1: a
  covered course crosses to effective_n>0/score_source=course; an uncovered course is handled in
  Plan 05-02 (recorded as still-lacking, never omitted).
- precision (where rounding/shrinkage lives) -> resolved as truth #2: validate against the RAW
  total_grade_count, never the smoothed easiness_score.
- concurrency (atomic whole-workbook import; idempotent re-import) -> resolved in Plan 05-02
  (idempotency + atomic-failure scoping); flagged here as the reason the workbook MUST be scoped to
  the current 10 courses before import (a single unresolved (subject, course_number) aborts the
  whole import — grades/ingest.py `_resolve_grade_course_ids`).
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan 05-01)

**0 new production code symbols** — 05-PATTERNS.md confirms 0 new production code paths; every
mechanism (parser, ingest, resolve_course_id, cache rebuild, quality findings) already exists and
was hardened in MVP1-P1. This plan produces only:

- `docs/runbooks/grade-import.md` (NEW, tracked) — operator runbook for the two-step import +
  validation recipe (the recipe RESEARCH.md flags as documented nowhere in the repo).
- `.planning/phases/05-.../05-IMPORT-RECORD.md` (NEW, tracked) — the tracer course's per-course
  derived aggregate + provenance record (source term, ingest_run_id, raw total_grade_count,
  effective_n, score_source). This is the "retained derived aggregate + provenance record (NOT the
  raw XLSX)"; it holds NO raw per-row grade data (D-19).
- (Non-git) One course's real GradeDistribution rows written into hosted Supabase.

No new modules, models, migrations, routes, or CLIs are created or modified.
</artifacts_produced>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-RESEARCH.md
@.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-PATTERNS.md
@.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-VALIDATION.md
@src/easy_a/refresh/cli.py
@src/easy_a/refresh/service.py
@src/easy_a/grades/cli.py
@src/easy_a/analytics/cli.py
@src/easy_a/quality/cli.py
@README.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Source the approved InfoCenter export and prepare the hosted-Supabase target (Codex-owned, external — the phase's true critical path)</name>
  <read_first>
    - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-RESEARCH.md (Environment Availability, Open Questions 2 and 3, Anti-Patterns)
    - README.md (Environment section — DATABASE_URL; Grade Distribution Ingestion section)
    - .gitignore (confirm *.xlsx, *.xls, data/, research/raw/ are ignored)
  </read_first>
  <action>HALT for the operator/Codex: source the approved InfoCenter grade export, confirm each workbook's real historical term, place it under the git-ignored data/ path, export DATABASE_URL for the intended hosted Supabase target, and name the tracer course. See instructions.</action>
  <instructions>
    This step is Codex-owned and external to this repository — Claude cannot fetch an authenticated
    InfoCenter report. Halt and request the operator/Codex to:
    1. Source the approved USF InfoCenter grade-distribution XLSX (authenticated SGDIS report)
       covering as many of the 10 ingested Tampa courses as available: ACG 2021, ACG 2071, AMH 2020,
       ANT 2000, BSC 1005, ECO 2013, ENC 1101, MAC 1105, MAC 2311, PSY 2012. Requests must stay
       narrow and bounded (D-08/D-09) — no scraping, no broad crawl.
    2. For EACH workbook, confirm its actual historical Banner term (e.g. 202408). The workbook does
       not carry a trusted term and its filename is never used as term metadata — the operator must
       state the real term, which is supplied to `--term` at import time.
    3. Place the file(s) under the git-ignored `data/` path (e.g. `data/grades/`), never inside a
       tracked directory and never renamed to an extension outside the .gitignore *.xlsx/*.xls globs.
    4. Export `DATABASE_URL` for the hosted Supabase pooler (or source the repo `.env`), and confirm
       the target is the intended hosted Supabase DB (not a local/other DB) — this is the moment the
       operator authorizes writing externally-sourced data into production.
    5. Name the single "tracer course" to prove first (any one course the export actually covers).
    Record the operator's answers (tracer course, its historical term, the local file path) so
    Task 2 can run non-interactively.
  </instructions>
  <verification>
    - The sourced XLSX exists at the stated path under `data/` (or outside the repo tree) and `git status --porcelain` does NOT list it.
    - `git ls-files -- '*.xlsx' '*.xls'` prints nothing.
    - `DATABASE_URL` is set and the operator confirmed it points at the intended hosted Supabase DB.
    - The operator has stated the tracer course subject+number and the workbook's real historical Banner term.
  </verification>
  <acceptance_criteria>
    - The sourced XLSX exists at a stated path under `data/` (or outside the repo tree); `git status --porcelain` shows it is NOT staged/tracked.
    - The operator has stated, for the tracer course: subject, course number, and the workbook's real historical Banner term.
    - `DATABASE_URL` resolves to the hosted Supabase pooler and the operator has confirmed it is the intended production target.
    - `git ls-files -- '*.xlsx' '*.xls'` prints nothing (no export tracked).
  </acceptance_criteria>
  <resume-signal>Once the file path, tracer course, its historical term, and DATABASE_URL are confirmed, proceed to Task 2.</resume-signal>
</task>

<task type="tracer" tdd="false">
  <name>Task 2: Tracer — author the import runbook, then import + cache-rebuild + validate ONE course against hosted Supabase</name>
  <files>docs/runbooks/grade-import.md, .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md</files>
  <precondition>DATABASE_URL is exported for the hosted Supabase pooler and the scoped InfoCenter workbook is present under data/ (both established in Task 1); the courses table already holds the 10 ingested Tampa course rows (MVP1-P1). If DATABASE_URL is unset or the file is absent, halt — do not fabricate results.</precondition>
  <reversibility rating="costly">Writes GradeDistribution rows into the live hosted Supabase DB that feed live search scores; idempotent and repairable via same-key upsert + term/CRN/source dedup (D-04), so not one-way, but it mutates production and is gated by Task 1 (human authorization) and Task 3 (human verify).</reversibility>
  <read_first>
    - src/easy_a/refresh/cli.py (confirms --grade-file, --term, --skip-catalog/--skip-schedule/--skip-grades/--skip-syllabi live HERE, not on grades/cli.py)
    - src/easy_a/refresh/service.py (lines 88-91 — the cache stage keys off config.term, so the historical-term run does NOT rebuild 202701)
    - src/easy_a/grades/cli.py (the standalone ingest CLI takes --file, NOT --grade-file, and rebuilds no cache)
    - src/easy_a/analytics/cli.py (analyze_course output columns: Effective N, Source)
    - src/easy_a/grades/ingest.py (_resolve_grade_course_ids atomic whole-workbook resolution; hash_file source_hash dedup)
    - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-RESEARCH.md (Pattern 1, Pattern 2, Common Pitfalls 1-4, Code Examples)
    - README.md (Grade Distribution Ingestion / Real-Data Refresh sections)
  </read_first>
  <action>
    First author `docs/runbooks/grade-import.md` (new, tracked). It must document, as reusable
    operator instructions, the two-step recipe that RESEARCH.md flags as documented NOWHERE in the
    repo today: (a) scope the workbook to the current `courses` rows before import (a single
    unresolved (subject, course_number) aborts the entire import via `_resolve_grade_course_ids`);
    (b) import per historical term with `uv run python scripts/refresh_data.py --term <historical>
    --grade-file <data/...path> --skip-catalog --skip-schedule --skip-syllabi`; (c) run a SEPARATE
    `uv run python scripts/refresh_data.py --term 202701 --skip-catalog --skip-schedule
    --skip-grades --skip-syllabi` cache-only pass (the historical run rebuilds only its own term's
    cache); (d) validate with `scripts/analyze_course.py` and `scripts/check_data_quality.py`;
    (e) the honesty checks (raw count not smoothed score; never commit the raw file; blanks fail
    closed, never coerced). State the correct flag explicitly: `--grade-file` belongs to
    `refresh_data.py`; `grades/cli.py` (`scripts/ingest_grades.py`) uses `--file` and rebuilds no
    cache.

    Then execute the tracer for the ONE course named in Task 1:
    1. Scope the workbook so only rows whose (subject, course_number) is in the current 10 courses
       remain (pre-filter or split; operator/data-prep, not new code) — at minimum isolate the
       tracer course's rows so the import resolves cleanly.
    2. Import: `uv run python scripts/refresh_data.py --term <tracer historical term> --grade-file
       <data/...path> --skip-catalog --skip-schedule --skip-syllabi`.
    3. Rebuild the live cache: `uv run python scripts/refresh_data.py --term 202701 --skip-catalog
       --skip-schedule --skip-grades --skip-syllabi`.
    4. Validate: `uv run python scripts/analyze_course.py --term 202701 --subject <SUBJ> --course
       <NUM>` (expect Source=course, Effective N>0) and `uv run python scripts/check_data_quality.py
       --term 202701 --json` (expect 0 errors; no unattributed_grade_row / grade_total_mismatch).
    5. Cross-check RAW counts: sum the source workbook's Total Grades column for the tracer course
       and confirm it equals the DB's raw total for that course_id (compare to the raw
       total_grade_count, NOT the Bayesian-smoothed easiness_score — a smoothed-vs-raw mismatch is
       normal shrinkage, not a bug).
    6. Create `05-IMPORT-RECORD.md` and record ONE row for the tracer course: subject+number,
       source historical term, ingest_run_id, raw total_grade_count (DB) vs source Total Grades sum,
       effective_n, score_source. Record ONLY per-course aggregates + provenance — never raw
       per-row grade data (that would be a D-19 raw-export leak in a derived file).
    If the execution environment cannot reach hosted Supabase, halt at the precondition and have the
    operator run steps 2-5 and paste the command output, which you then transcribe into the record.
    Do NOT weaken the parser, the atomic resolution, or the blank-cell policy to make an import
    "succeed".
  </action>
  <verify>
    <automated>cd /home/aatif101/wsl_projects/easy-A && uv run pytest tests/grades/test_ingest.py tests/test_grade_course_attribution.py -x -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" appears in the pytest summary line — the mechanical import + attribution + cache path is broken.</fails_when>
    <automated>cd /home/aatif101/wsl_projects/easy-A && test -f docs/runbooks/grade-import.md && test -f .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md</automated>
    <fails_when>non-zero exit — the runbook or the import record was not created.</fails_when>
    <automated>cd /home/aatif101/wsl_projects/easy-A && grep -q -- '--term 202701' docs/runbooks/grade-import.md && grep -q -- '--grade-file' docs/runbooks/grade-import.md</automated>
    <fails_when>non-zero exit — the runbook omits the mandatory separate 202701 cache-only rebuild or the correct --grade-file flag.</fails_when>
    <automated>cd /home/aatif101/wsl_projects/easy-A && easy_a_tracked_exports=$(git ls-files -- '*.xlsx' '*.xls') && test -z "$easy_a_tracked_exports"</automated>
    <fails_when>non-zero exit, or a path is printed — a raw grade export is tracked in git (D-19 violation).</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/runbooks/grade-import.md` exists and documents: the scope-to-current-courses rule, the historical `--grade-file` import, the SEPARATE `--term 202701 --skip-grades` cache-only rebuild, the `analyze_course.py` + `check_data_quality.py` validation, and the raw-count-not-smoothed-score rule.
    - `05-IMPORT-RECORD.md` exists with one tracer-course row: source term, ingest_run_id, raw total_grade_count vs source Total Grades sum, effective_n, score_source (aggregates only, no raw per-row data).
    - `tests/grades/test_ingest.py` and `tests/test_grade_course_attribution.py` pass.
    - `git ls-files -- '*.xlsx' '*.xls'` prints nothing.
  </acceptance_criteria>
  <done>The tracer course's real grade history is imported and, after the 202701 cache rebuild, its live sections report effective_n>0/score_source=course; the runbook and import record are committed; no raw export is tracked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — the tracer course's real numbers match the source before scaling</name>
  <read_first>
    - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md (the recorded tracer-course row)
    - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <what-built>
    The tracer course's real historical grade rows are imported into hosted Supabase and, after the
    separate 202701 cache-only rebuild, its live sections resolve course-backed history. The result
    (source term, ingest_run_id, raw total_grade_count vs source Total Grades sum, effective_n,
    score_source) is recorded in 05-IMPORT-RECORD.md, and docs/runbooks/grade-import.md documents the
    reusable two-step recipe.
  </what-built>
  <how-to-verify>
    Present to the operator, from the actual post-import hosted Supabase state:
    - `uv run python scripts/analyze_course.py --term 202701 --subject SUBJ --course NUM` output for
      the tracer course, showing Source=course and Effective N>0 (previously global / 0.0).
    - The recorded raw total_grade_count (DB) alongside the operator's independent sum of the source
      workbook's Total Grades column for that course.
    - `uv run python scripts/check_data_quality.py --term 202701 --json` error count (expect 0).
    Ask the operator to confirm the raw counts match and the course honestly flipped to course-backed
    history. This is the manual data-validation gate (real DB + real file, not machine-checkable in CI).
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms: tracer course Source=course, Effective N>0.
    - Operator confirms: DB raw total_grade_count == source workbook Total Grades sum for that course (raw-to-raw, not against the smoothed score).
    - Operator confirms: check_data_quality.py --term 202701 reports 0 errors and no unattributed_grade_row / grade_total_mismatch for the tracer course.
    - Operator confirms: no raw export file is staged or tracked in git.
  </acceptance_criteria>
  <resume-signal>Operator confirms the tracer course's raw counts match the source and it flipped to score_source=course; proceed to Plan 05-02.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated USF InfoCenter -> operator filesystem | Codex-owned, out-of-band authenticated SGDIS export crosses into local files (bounded, D-08/D-09) |
| operator filesystem -> import CLI (grades/parser.py) | untrusted XLSX content is parsed and validated here |
| import CLI -> hosted Supabase | writes GradeDistribution rows into the live production DB that feeds search scores |
| git working tree -> repository history | the raw export must never cross this boundary (D-19) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | git working tree / repo history (raw InfoCenter export) | high | mitigate | .gitignore excludes *.xlsx/*.xls/data/research/raw; operator keeps the file under data/ or outside the tree; verify `git ls-files -- '*.xlsx' '*.xls'` is empty; the import record holds per-course aggregates only, never raw per-row rows |
| T-05-02 | Tampering | src/easy_a/grades/parser.py (malformed XLSX; blank cell misread as zero) | medium | mitigate | existing fail-closed blank-cell rejection + bucket-sum-vs-Total-Grades cross-check; parser is NOT weakened to force an import |
| T-05-03 | Tampering / Repudiation | src/easy_a/grades/ingest.py (duplicate re-exported workbook) | medium | mitigate | uq_grade_distributions_term_crn_source + source_hash-aware upsert prevents double-counting on re-import |
| T-05-04 | Repudiation | src/easy_a/analytics/confidence.py + validation gate (global fallback shown as course history) | high | mitigate | score_source/effective_n distinction is always present; Task 2 validation + Task 3 human-verify confirm covered courses show course and uncovered courses stay global — never presented as history (D-20) |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed this phase (pandas/openpyxl are pre-pinned in pyproject.toml); package-legitimacy gate N/A |
</threat_model>

<verification>
- `cd /home/aatif101/wsl_projects/easy-A && uv run pytest tests/grades tests/analytics tests/test_grade_course_attribution.py -q` — mechanical grade path green (fails_when: non-zero exit / "failed" in summary).
- `cd /home/aatif101/wsl_projects/easy-A && easy_a_tracked_exports=$(git ls-files -- '*.xlsx' '*.xls') && test -z "$easy_a_tracked_exports"` — no raw export tracked (fails_when: a path prints / non-zero exit).
- Manual (real hosted Supabase, Task 3): `scripts/analyze_course.py --term 202701 --subject <SUBJ> --course <NUM>` shows Source=course / Effective N>0 for the tracer course; `scripts/check_data_quality.py --term 202701 --json` reports 0 errors.
</verification>

<success_criteria>
- The tracer course has real historical grade data in hosted Supabase and flips to effective_n>0/score_source=course after the 202701 cache rebuild.
- The tracer course's DB raw total_grade_count equals the source workbook's Total Grades sum (raw-to-raw).
- `docs/runbooks/grade-import.md` and `05-IMPORT-RECORD.md` exist and are committed; no raw export is tracked in git.
</success_criteria>

<output>
Create `.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-01-SUMMARY.md` when done.
</output>
