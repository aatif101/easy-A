---
phase: 05-mvp1-p2-grade-data-sourcing-import-to-supabase
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md
  - tests/grades/test_parser_real_export_shape.py
autonomous: false
requirements: [REQ-GRADES-01]
user_setup:
  - service: usf-infocenter-grade-export
    why: "Same Codex-owned sourced export(s) and hosted-Supabase target from Plan 05-01; no new external setup, only continued access."
    env_vars:
      - name: DATABASE_URL
        source: "Hosted Supabase pooler (already established in Plan 05-01 Task 1)."

estimate:
  tokens: 40000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the 10 ingested Tampa courses (ACG 2021, ACG 2071, AMH 2020, ANT 2000, BSC 1005, ECO 2013, ENC 1101, MAC 1105, MAC 2311, PSY 2012) appears in 05-IMPORT-RECORD.md with an honest status: effective_n>0/score_source=course for imported courses (raw total_grade_count vs source Total Grades sum), or effective_n=0/score_source=global explicitly recorded as still lacking data — never quietly omitted (D-06/D-07/D-20). [edge:boundary — the 0-sourced-rows course must be recorded as lacking, not dropped]"
    - "scripts/check_data_quality.py --term 202701 --json reports 0 errors and emits the no_historical_analytics info finding ONLY for courses genuinely lacking historical grade data (this existing finding IS the 'recorded as lacking it' mechanism — no new tracking table)."
    - { statement: "A blank canonical grade-count cell present in the real export is encoded as a fail-closed regression fixture tests/grades/test_parser_real_export_shape.py; if the real export contains no blank cells, that absence is recorded in 05-IMPORT-RECORD.md and OQ-04 stays open — the parser is never patched to coerce blanks (D-06/D-07).", verification: backstop }
    - "A re-import of an already-imported workbook is idempotent — GradeDistribution rows are not double-counted (D-04). [edge:concurrency — same term/CRN/source key upserts in place; parallel/interrupted runs cannot inflate counts]"
  artifacts:
    - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md
    - tests/grades/test_parser_real_export_shape.py
  key_links:
    - "Scoping each workbook to the current 10 courses before import prevents atomic whole-workbook failure — _resolve_grade_course_ids (grades/ingest.py) raises GradeCourseResolutionError on ANY unresolved (subject, course_number) and writes nothing."
    - "A single --term 202701 --skip-grades cache-only rebuild after all historical imports makes every imported course's history visible in section_rankings / GET /api/v1/rankings/search."
  prohibitions:
    - "MUST NOT quietly omit a course that still lacks grade data — it must appear in 05-IMPORT-RECORD.md as effective_n=0/score_source=global (D-06/D-07/D-20). Enforcement evidence: no_historical_analytics info finding per section in src/easy_a/quality/checks.py."
    - "MUST NOT weaken or bypass the fail-closed blank-cell rejection to make the real export import (D-06/D-07, OQ-04). Enforcement evidence: _blank_count_cell_message in src/easy_a/grades/parser.py; the OQ-04 fixture asserts the rejection, it does not remove it."
    - "MUST NOT double-count a re-exported / re-imported workbook (D-04). Enforcement evidence: uq_grade_distributions_term_crn_source + source_hash-aware upsert in src/easy_a/grades/ingest.py."
    - "MUST NOT commit any raw grade export or a derived file containing raw per-row grade data (D-19). Enforcement evidence: .gitignore + git ls-files verification."
---

<objective>
Scale the proven loop from Plan 05-01 to the remaining ingested Tampa courses the sourced export
covers, complete the honest per-course coverage record for all 10 courses, and — only if the real
export actually contains blank canonical-count cells — encode that real shape as a permanent
regression fixture (OQ-04).

Still NO new production code: this reuses the same existing CLIs. The one net-new code artifact is
CONDITIONAL (the OQ-04 fixture test), and it only lands if the real export triggers it.

Purpose: finish REQ-GRADES-01's data-import + validation clause for this phase — every ingested
course either has real course-backed history or is honestly recorded as lacking it.
Output: completed 05-IMPORT-RECORD.md (all 10 courses), and conditionally
tests/grades/test_parser_real_export_shape.py.

Edge resolutions (from the spec-less probe, continued from 05-01):
- boundary -> truth #1: the uncovered course (0 sourced rows) is recorded as effective_n=0/global,
  never dropped.
- concurrency -> truth #4: re-import is idempotent (same-key upsert + dedup); scoping to the 10
  courses avoids atomic whole-workbook failure.
- precision (OQ-04 blank-cell contract) -> truth #3 (backstop): blanks fail closed; a real blank is
  captured as a fixture, never coerced.
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan 05-02)

**0 new production code symbols.** The only net-new code artifact is CONDITIONAL:

- `.planning/phases/05-.../05-IMPORT-RECORD.md` (COMPLETED, tracked) — all 10 ingested Tampa courses,
  each with an honest status (course-backed aggregate + provenance, or explicitly still-lacking).
  Per-course aggregates only, no raw per-row data (D-19).
- `tests/grades/test_parser_real_export_shape.py` (NEW, tracked — CONDITIONAL, only if the real
  export contains a blank canonical-count cell) — a regression fixture asserting the existing
  fail-closed rejection against the real blank-cell shape (OQ-04). If the real export has no blank
  cells, this file is NOT created and the absence is recorded in 05-IMPORT-RECORD.md instead.
- (Non-git) The remaining covered courses' real GradeDistribution rows written into hosted Supabase.

No production modules, models, migrations, routes, or CLIs are created or modified.
</artifacts_produced>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-RESEARCH.md
@.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-PATTERNS.md
@.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-01-SUMMARY.md
@docs/runbooks/grade-import.md
@src/easy_a/refresh/cli.py
@src/easy_a/quality/checks.py
@tests/grades/test_parser.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Import the remaining covered courses and complete the honest per-course coverage record</name>
  <files>.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md</files>
  <precondition>DATABASE_URL is exported for the hosted Supabase pooler, the sourced workbook(s) are present under data/, and Plan 05-01 proved the loop on the tracer course. If DATABASE_URL is unset or the files are absent, halt — do not fabricate results.</precondition>
  <reversibility rating="costly">Writes the remaining GradeDistribution rows into the live hosted Supabase DB; idempotent and repairable via same-key upsert + term/CRN/source dedup (D-04), gated by the Plan 05-01 human authorization and by Task 3's human-verify.</reversibility>
  <read_first>
    - docs/runbooks/grade-import.md (the two-step recipe authored in Plan 05-01 — follow it verbatim)
    - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md (the tracer row to extend)
    - src/easy_a/refresh/cli.py (--grade-file / --term / --skip-* flags)
    - src/easy_a/quality/checks.py (no_historical_analytics finding — the "lacking data" signal)
    - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-RESEARCH.md (Pitfalls 3, 4, 5; Open Questions 2, 3)
  </read_first>
  <action>
    For each historical term the sourced export covers, scope the workbook to the current 10 courses
    and import with `uv run python scripts/refresh_data.py --term HIST --grade-file DATA_PATH
    --skip-catalog --skip-schedule --skip-syllabi` (one run per historical term, HIST = that
    workbook's real Banner term, DATA_PATH under the ignored data/). A single unresolved
    (subject, course_number) aborts the whole run (_resolve_grade_course_ids), so the scoping/split
    from the runbook is mandatory. After all historical imports, run ONE cache-only rebuild with
    `uv run python scripts/refresh_data.py --term 202701 --skip-catalog --skip-schedule
    --skip-grades --skip-syllabi` (rebuilding after every term is harmless but redundant).

    Validate every ingested course with `uv run python scripts/analyze_course.py --term 202701
    --subject SUBJ --course NUM` for each of the 10, and `uv run python scripts/check_data_quality.py
    --term 202701 --json`. For each imported course, sum the source workbook's Total Grades column and
    confirm it equals the DB's raw total_grade_count for that course_id (raw-to-raw, never against the
    smoothed easiness_score — a smoothed-vs-raw difference is normal Bayesian shrinkage, not a bug).

    Complete 05-IMPORT-RECORD.md so ALL 10 courses appear with an explicit, honest status: imported
    courses carry source historical term(s), ingest_run_id, raw total_grade_count (DB) vs source Total
    Grades sum, effective_n, score_source=course; courses the export did NOT cover are recorded as
    effective_n=0 / score_source=global and "still lacking historical grade data" — never omitted
    (D-06/D-07/D-20), cross-referenced to the no_historical_analytics finding. Record per-course
    aggregates + provenance ONLY — no raw per-row grade data in any tracked file (D-19). If the
    environment cannot reach hosted Supabase, halt at the precondition and have the operator run the
    commands and paste output for transcription. Do NOT loosen the parser, the atomic resolution, or
    the blank-cell policy to force a course through.
  </action>
  <verify>
    <automated>cd /home/aatif101/wsl_projects/easy-A && uv run pytest tests/grades tests/analytics tests/test_grade_course_attribution.py -x -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" appears in the pytest summary line.</fails_when>
    <automated>cd /home/aatif101/wsl_projects/easy-A && missing=$(for c in "ACG 2021" "ACG 2071" "AMH 2020" "ANT 2000" "BSC 1005" "ECO 2013" "ENC 1101" "MAC 1105" "MAC 2311" "PSY 2012"; do grep -qF "$c" .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md || echo "$c"; done); test -z "$missing"</automated>
    <fails_when>non-zero exit, or a course name prints — an ingested course is missing from the coverage record (a quiet omission, D-06/D-07 violation).</fails_when>
    <automated>cd /home/aatif101/wsl_projects/easy-A && easy_a_tracked_exports=$(git ls-files -- '*.xlsx' '*.xls') && test -z "$easy_a_tracked_exports"</automated>
    <fails_when>non-zero exit, or a path prints — a raw grade export is tracked in git (D-19 violation).</fails_when>
  </verify>
  <acceptance_criteria>
    - All 10 ingested Tampa courses appear in 05-IMPORT-RECORD.md, each with effective_n, score_source, and (for imported courses) raw total_grade_count vs source Total Grades sum.
    - Courses not covered by the export are recorded as effective_n=0/score_source=global and "still lacking data", never omitted.
    - tests/grades, tests/analytics, tests/test_grade_course_attribution.py all pass.
    - git ls-files -- '*.xlsx' '*.xls' prints nothing.
  </acceptance_criteria>
  <done>Every ingested course has real course-backed history in Supabase or is honestly recorded as lacking it; the coverage record is complete and committed; no raw export is tracked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OQ-04 — capture the real export's blank-cell shape as a regression fixture, or record its absence</name>
  <files>tests/grades/test_parser_real_export_shape.py (conditional — only if the real export contains a blank canonical-count cell), .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md</files>
  <precondition>The sourced workbook(s) from Plan 05-01 are available for inspection under data/. If absent, halt.</precondition>
  <behavior>
    - If the real export contains a blank canonical grade-count cell: parse_grade_workbook on a fixture built from the real blank-cell row shape raises GradeWorkbookValidationError whose message names the offending row and column, contains "unverified", and does NOT contain "suppressed the value" (it refuses to assume zero-vs-suppressed-vs-unavailable).
    - If the real export contains no blank cells: no new test is added; the absence is recorded in 05-IMPORT-RECORD.md and OQ-04 stays open.
  </behavior>
  <read_first>
    - tests/grades/test_parser.py (analog — GRADE_HEADER, _write_workbook, _grade_row, _hierarchy_row helpers; test_blank_bucket_count_fails_closed at lines 99-113 is the direct template)
    - src/easy_a/grades/parser.py (_blank_count_cell_message at lines 277-282 — the exact fail-closed message the fixture asserts)
    - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-PATTERNS.md (the conditional-fixture pattern assignment)
  </read_first>
  <action>
    Inspect the sourced export(s) for any blank canonical grade-count cell (blank bucket count or
    blank Total Grades) among the rows for the 10 in-scope courses. Two branches, exactly one applies:
    - PRESENT: create tests/grades/test_parser_real_export_shape.py following the
      tests/grades/test_parser.py idiom — reuse the module-level GRADE_HEADER, _write_workbook(path,
      rows), and _grade_row(...) helpers (do NOT hand-build inline Workbook() calls), synthesize a row
      matching the REAL export's blank-cell shape (column position, and any surrounding
      hierarchy/total rows if the real layout differs from the synthetic one), and assert
      pytest.raises(GradeWorkbookValidationError) with the message asserting "row N", the offending
      column, "unverified" present, and "suppressed the value" absent. Encode NO raw grade values —
      use synthetic counts around a single blank cell (the fixture proves the parser's REACTION to
      the real shape, it is not a copy of real data, so it is not a D-19 leak). Record in
      05-IMPORT-RECORD.md that OQ-04 was triggered and captured.
    - ABSENT: create no test. Record in 05-IMPORT-RECORD.md that the real export contained no blank
      canonical-count cells, so OQ-04 is not triggered by this data and remains open (escalate to the
      ODS/registrar-access owner if it ever appears). Do NOT weaken the parser either way.
  </action>
  <verify>
    <automated>cd /home/aatif101/wsl_projects/easy-A && uv run pytest tests/grades -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" appears in the pytest summary line — an added fixture is red or the existing parser suite broke.</fails_when>
    <automated>cd /home/aatif101/wsl_projects/easy-A && grep -qi 'OQ-04' .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md</automated>
    <fails_when>non-zero exit — the OQ-04 disposition (triggered+captured, or absent) was not recorded in the import record.</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one branch is taken and recorded in 05-IMPORT-RECORD.md under an OQ-04 heading.
    - If a fixture was added, tests/grades passes and the fixture asserts the fail-closed message shape (row, column, "unverified" present, "suppressed the value" absent) — it does not weaken or remove the rejection.
    - If no fixture was added, the record states the real export had no blank canonical-count cells and OQ-04 remains open.
    - No raw per-row grade data appears in the fixture or the record.
  </acceptance_criteria>
  <done>OQ-04 is dispositioned against the real export — captured as a regression fixture or recorded as not-triggered — with the fail-closed policy intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — phase data gate against the real hosted Supabase state</name>
  <read_first>
    - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md (completed, all 10 courses)
    - .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-VALIDATION.md (Manual-Only Verifications; Sampling Rate phase gate)
  </read_first>
  <what-built>
    The remaining covered courses are imported into hosted Supabase, 05-IMPORT-RECORD.md now records
    all 10 ingested Tampa courses with an honest status (course-backed history or explicitly
    still-lacking), and OQ-04 is dispositioned against the real export (regression fixture captured or
    absence recorded).
  </what-built>
  <how-to-verify>
    Present to the operator, from the actual post-import hosted Supabase state:
    - `uv run python scripts/check_data_quality.py --term 202701 --json` — confirm 0 errors, and that
      no_historical_analytics appears ONLY for courses genuinely lacking data.
    - The completed 05-IMPORT-RECORD.md — confirm all 10 courses are present with an honest status and
      that imported courses' raw total_grade_count matches the operator's independent source-workbook
      Total Grades sum.
    - `uv run pytest -q` — full suite green.
    Ask the operator to confirm the data is honest (no global fallback presented as course history),
    complete (no course omitted), and that no raw export is staged or tracked in git. This is the
    phase's real-data gate before /gsd-verify-work.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms check_data_quality.py --term 202701 reports 0 errors; no_historical_analytics only for genuinely uncovered courses.
    - Operator confirms all 10 courses are recorded with honest statuses; imported-course raw counts match the source workbook sums.
    - Operator confirms `uv run pytest -q` is green.
    - Operator confirms no raw grade export is staged or tracked (git ls-files -- '*.xlsx' '*.xls' empty).
  </acceptance_criteria>
  <resume-signal>Operator confirms 0 quality errors, all 10 courses honestly recorded, full suite green, and no raw export tracked; the phase is ready for /gsd-verify-work.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator filesystem -> import CLI (grades/parser.py) | untrusted XLSX content is parsed and validated here |
| import CLI -> hosted Supabase | writes the remaining GradeDistribution rows into the live production DB |
| git working tree -> repository history | neither the raw export nor a derived file with raw per-row data may cross this boundary (D-19) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Repudiation | 05-IMPORT-RECORD.md + analytics (global fallback shown / recorded as course history; a course quietly omitted) | high | mitigate | every ingested course recorded with an explicit honest status; no_historical_analytics cross-checked; Task 3 human-verify confirms no omission and no global-as-history (D-06/D-07/D-20) |
| T-05-06 | Tampering / Repudiation | src/easy_a/grades/ingest.py (re-import double-counting across the multi-term scale-out) | medium | mitigate | uq_grade_distributions_term_crn_source + source_hash-aware upsert make re-import idempotent (D-04) |
| T-05-07 | Tampering | src/easy_a/grades/parser.py (blank/suppressed cell in the real export coerced to force import) | high | mitigate | fail-closed rejection kept intact; OQ-04 captured as a fixture asserting the rejection, never a parser weakening (D-06/D-07) |
| T-05-08 | Information Disclosure | git working tree (raw export / derived raw-row file, incl. the OQ-04 fixture) | high | mitigate | fixture encodes only synthetic counts around a blank cell; import record holds per-course aggregates only; verify `git ls-files -- '*.xlsx' '*.xls'` empty (D-19) |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages installed this phase; package-legitimacy gate N/A |
</threat_model>

<verification>
- `cd /home/aatif101/wsl_projects/easy-A && uv run pytest -q` — full suite green (fails_when: non-zero exit / "failed" in summary).
- `cd /home/aatif101/wsl_projects/easy-A && uv run ruff check .` and `uv run mypy src migrations scripts tests` — clean if a fixture was added (fails_when: non-zero exit).
- `cd /home/aatif101/wsl_projects/easy-A && easy_a_tracked_exports=$(git ls-files -- '*.xlsx' '*.xls') && test -z "$easy_a_tracked_exports"` — no raw export tracked (fails_when: a path prints / non-zero exit).
- Manual (real hosted Supabase, Task 3): `scripts/check_data_quality.py --term 202701 --json` reports 0 errors with no_historical_analytics only for genuinely uncovered courses.
</verification>

<success_criteria>
- All 10 ingested Tampa courses are recorded in 05-IMPORT-RECORD.md with an honest status — real course-backed history or explicitly still-lacking, never omitted.
- Imported courses' DB raw total_grade_count matches their source-workbook Total Grades sum.
- OQ-04 is dispositioned against the real export (fixture captured or absence recorded) with the fail-closed policy intact.
- Full suite green; 0 quality errors on term 202701; no raw export tracked in git.
</success_criteria>

<output>
Create `.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-02-SUMMARY.md` when done.
</output>
