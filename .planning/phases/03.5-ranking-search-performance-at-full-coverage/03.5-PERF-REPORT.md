# Phase 03.5 — Rankings Search Performance Report

**Measured:** 2026-09-20
**Requirement:** REQ-PERF-01 (rankings search performance at widened coverage)
**Harness:** `scripts/benchmark_rankings_search.py` (synthetic fixture, throwaway schema, rolled back)

## AFTER — rewritten SQL search (Plans 01–03)

Full projected-coverage scale, run via `DATABASE_URL` (no `--url`):

| Metric | Value |
|--------|-------|
| Dataset | 3,782 sections (synthetic test fixture) |
| Iterations | 50 |
| p50 | 2191.34 ms |
| **p95** | **2397.90 ms (~2.40 s)** |
| max | 2493.44 ms |

**Environment label caveat (honest reporting):** the harness printed
`dialect=postgresql, pooler=False, synthetic test fixture over local Postgres`. That label is
**derived only from the `--url` argument** (`_environment_label` / the `pooler` computation use
the passed URL, which was `None` because the run relied on `DATABASE_URL` via `easy_a.db`). It is
therefore **not evidence of the connection target**. The ~15-minute seed of 3,782 rows over the
network strongly indicates the remote hosted Supabase instance was used (a local Postgres would
seed in seconds), but **this run is not certified as "Supabase"** because the harness cannot label
it as such without `--url`. A certified Supabase measurement should re-run with `--url <supabase>`.
This is a harness limitation, tracked below.

## BEFORE — pre-rewrite N+1 baseline (not re-measured)

- Documented baseline: **~600 s at the 132-section Tampa pilot** (plan-stated); REQUIREMENTS.md
  also records an initial local measurement of **~10.1 s for 77 sections**.
- The old N+1 path was **not** re-measured at 3,782-section scale: per Plan 05 Task 3, running the
  pre-rewrite N+1 at that scale is infeasible (hours against remote Supabase). The before/after
  comparison is therefore pilot-scale BEFORE vs. full-scale AFTER, stated explicitly.

## Acceptance criteria status

| # | Criterion | Status |
|---|-----------|--------|
| 2 | Measured p95 < ~1.5 s at widened coverage, Supabase | **NOT MET — ~2.40 s at 3,782 sections. Deferred (accepted deviation).** |
| 3 | `check_data_quality.py --term 202701` = 0 errors | ✅ Met — 0 errors, 132 sections (2026-09-20) |
| 5 | Full test suite green on the PostgreSQL path | ✅ Met — 256 passed, 0 failed with `EASY_A_TEST_POSTGRES_URL` exported |
| D-05 | No secrets committed / printed | ✅ Met — harness prints dialect + pooler boolean only, never the connection string |

## Deferred — accepted deviation (tracked in `.planning/WINDOWS.md`)

**p95 at full 3,782-section scale (~2.40 s) exceeds the ~1.5 s target.** Deferred to a post-pilot
tuning pass by explicit decision (2026-09-20): the miss is at a *synthetic projected* scale, not
the current **~132-section Tampa pilot**, where the rewritten SQL path replaces the ~600 s N+1 and
is expected to be well under target. The remaining work is **additive index/query tuning**
(candidate: an index on `section_rankings` sort/filter columns; `EXPLAIN ANALYZE` at scale), not a
structural rewrite, so it does not block or entangle downstream work. Recommended before broad
(non-pilot) coverage is released to users.

Two follow-ups recorded with this deferral:
1. Tune rankings search so p95 < ~1.5 s at full scale, then re-measure.
2. Fix `benchmark_rankings_search.py` to derive the environment label / `pooler` from the resolved
   engine URL (not only `--url`), so a `DATABASE_URL`-driven run reports its real target.
