---
phase: 03.5-ranking-search-performance-at-full-coverage
plan: 03
type: execute
wave: 2
depends_on: [03.5-01]
files_modified:
  - src/easy_a/refresh/coverage.py
  - src/easy_a/refresh/service.py
  - tests/refresh/test_rankings_cache_refresh.py
  - tests/refresh/test_coverage_suffix_guard.py
autonomous: true
requirements: [REQ-PERF-01]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Running refresh_targets()/refresh_data() for a term populates or updates section_rankings for that term's sections, atomically with the ingest (D-09)."
    - "A just-ingested grade/section change is reflected in a subsequent search with no separate manual step; the cache cannot silently go stale (D-09)."
    - "A refresh of a base course whose USF query also returns suffix variants (CHM 2045 -> CHM 2045 + CHM 2045L) ingests only the exact (subject, course_number) matches and silently excludes the variants, without raising and without weakening the Tampa/CRN guard (D-10)."
  artifacts:
    - src/easy_a/refresh/coverage.py
    - src/easy_a/refresh/service.py
    - tests/refresh/test_rankings_cache_refresh.py
    - tests/refresh/test_coverage_suffix_guard.py
  key_links:
    - "refresh_section_rankings() called inside refresh_targets' own transaction, right after ingest_schedule_html (atomic — rolls back together on failure)"
    - "new _refresh_rankings_cache stage in refresh_data, after syllabi, wrapped in RefreshStageError"
    - "D-10 exact-course pre-filter inserted BEFORE the existing any(...) Tampa/CRN guard, guard strictness unchanged"
---

<objective>
Wire cache population into both ingestion entry points so `section_rankings` is populated atomically
with an ingest and can never silently go stale (D-09), and fix the CHM 2045 vs CHM 2045L
suffix-variant matching with an exact-course pre-filter that does NOT weaken the Tampa/CRN guard
(D-10).

Purpose: D-09 (cache freshness documented + tested) and D-10 (suffix guard) are the two remaining
locked decisions that make a full-scale run correct and self-maintaining.
Output: population calls in refresh_targets and a new refresh_data stage, the D-10 pre-filter, and
two tests (cache-freshness/D-09 and suffix-guard/D-10).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-CONTEXT.md
@.planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-RESEARCH.md
@.planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-PATTERNS.md

@src/easy_a/refresh/coverage.py
@src/easy_a/refresh/service.py
@src/easy_a/rankings/cache.py
@src/easy_a/schedule/client.py
@tests/refresh/test_postgres_coverage.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-10 exact-course pre-filter + atomic cache population in refresh_targets</name>
  <files>src/easy_a/refresh/coverage.py</files>
  <reversibility rating="reversible">Both changes are internal to refresh_targets (a pre-filter list-comprehension and one added call inside the caller's existing transaction); git-revertible; no external contract or schema change.</reversibility>
  <behavior>
    - When the parsed schedule rows for target CHM 2045 contain both CHM 2045 and CHM 2045L rows, refresh_targets ingests only the CHM 2045 rows and does NOT raise; the CHM 2045L rows are silently excluded.
    - The Tampa-campus and CRN-scope checks remain exactly as strict for the exact-course-number rows: a non-Tampa or wrong-CRN row AMONG the exact matches still raises ValueError and rolls back.
    - After a successful per-target ingest, section_rankings rows for that target's course exist within the same transaction (a failure before commit rolls back both the ingest and the cache write).
  </behavior>
  <action>
    In `refresh_targets` (src/easy_a/refresh/coverage.py:72-164), insert the D-10 pre-filter immediately AFTER `rows = parse_schedule_html(html)` (line 142) and BEFORE the existing guard (lines 143-149): `rows = [r for r in rows if (r.subject, r.course_number) == (target.subject, target.number)]`. Then narrow the existing `any(...)` guard so it no longer re-checks the course-number exactness (now guaranteed by the pre-filter) but keeps the Tampa and CRN checks exactly as strict — it must still `raise ValueError` and roll back if a non-Tampa or wrong-CRN row appears among the exact-course-number rows. Keep the existing duplicate-CRN check (line 151) intact. Do NOT loosen the Tampa/CRN strictness — this is a pure pre-filter (RESEARCH Key Finding #5, Pattern 3). Note in a code comment that suffix variants like CHM 2045L are separately configurable targets if wanted.
    After the successful ingest (`result = ingest_schedule_html(...)`, ~line 152), call `refresh_section_rankings(session, term=term)` scoped to the just-ingested target's course (pass the target's subject/course_number so only that course's cache rows are recomputed) INSIDE the same session/transaction, honoring the "Caller owns transaction; failures must roll it back" contract (coverage.py:85). This makes population atomic with the ingest (D-09; RESEARCH Key Finding #2). Import refresh_section_rankings from easy_a.rankings.cache.
  </action>
  <verify>
    <automated>cd /home/aatif101/wsl_projects/easy-A && uv run pytest tests/refresh -q</automated>
    <fails_when>Any existing refresh test regresses, the coverage module errors on import (bad import of refresh_section_rankings), or nonzero exit.</fails_when>
  </verify>
  <done>refresh_targets pre-filters to exact (subject, course_number) before the unchanged Tampa/CRN guard, and calls refresh_section_rankings for the ingested course inside the same transaction; existing refresh tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New _refresh_rankings_cache stage in refresh_data</name>
  <files>src/easy_a/refresh/service.py</files>
  <behavior>
    - After a full `refresh_data(...)` run, section_rankings is populated/updated for the refreshed term.
    - A failure inside cache population surfaces as a RefreshStageError("rankings cache", ...), exactly like any other stage failure — it does not silently succeed.
    - The stage runs after the syllabi stage and before the summary block.
  </behavior>
  <action>
    In `refresh_data` (src/easy_a/refresh/service.py:40-128), add a private `_refresh_rankings_cache(factory, term)` that opens a session via the factory and calls `refresh_section_rankings(session, term=term)` for the whole term, committing within its own `factory.begin()`/session scope like the other stage helpers (_refresh_catalog/_refresh_schedule/_refresh_grades/_refresh_syllabi). Invoke it via the existing `_run_stage("rankings cache", lambda: _refresh_rankings_cache(factory, config.term))` positioned AFTER the syllabi stage (after line 85) and BEFORE the `with factory() as session:` summary block (line 87), so a population failure is wrapped in RefreshStageError like every other stage (D-09; PATTERNS "Refresh-stage error wrapping"). Import refresh_section_rankings from easy_a.rankings.cache.
  </action>
  <verify>
    <automated>cd /home/aatif101/wsl_projects/easy-A && uv run pytest tests/refresh -q</automated>
    <fails_when>Any refresh test regresses, the new stage is not wrapped in RefreshStageError, the service module errors on import, or nonzero exit.</fails_when>
  </verify>
  <done>refresh_data runs a "rankings cache" stage after syllabi that populates section_rankings for the term and surfaces failures as RefreshStageError; existing refresh tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cache-freshness (D-09) and suffix-guard (D-10) tests</name>
  <files>tests/refresh/test_rankings_cache_refresh.py, tests/refresh/test_coverage_suffix_guard.py</files>
  <behavior>
    - test_rankings_cache_refresh: after a refresh that ingests a grade/section change, section_rankings for the affected course is populated/updated and a subsequent search reflects the change without a manual cache step; a fresh term with no prior cache is populated on refresh.
    - test_coverage_suffix_guard: a schedule response mixing CHM 2045 and CHM 2045L rows results in only the CHM 2045 rows ingested, no raise; a non-Tampa row among the exact-course matches still raises ValueError.
  </behavior>
  <action>
    Create tests/refresh/test_rankings_cache_refresh.py following the seed/refresh/reassert pattern in tests/refresh/test_postgres_coverage.py (SQLite in-memory `Base.metadata.create_all` for the default path). Assert D-09: (1) calling the refresh path (refresh_targets with file/seeded input, or refresh_section_rankings via the stage) populates section_rankings rows for the term's sections; (2) after changing underlying grade/section data and re-running the refresh, the cache row and a subsequent `hydrate_ranking`/search reflect the change — no separate manual cache step; (3) a term with no prior cache rows gets populated.
    Create tests/refresh/test_coverage_suffix_guard.py exercising refresh_targets' D-10 pre-filter: feed a parsed/mocked schedule response (use the file/seam or a monkeypatched parse_schedule_html) containing both CHM 2045 and CHM 2045L rows and assert only the 5 exact CHM 2045 rows are ingested and no ValueError is raised; then assert that a non-Tampa row among the exact-course-number matches still raises ValueError (guard strictness unchanged). Reference the guard at coverage.py:143-149.
  </action>
  <verify>
    <automated>cd /home/aatif101/wsl_projects/easy-A && uv run pytest tests/refresh/test_rankings_cache_refresh.py tests/refresh/test_coverage_suffix_guard.py -x -q</automated>
    <fails_when>The cache is not populated/updated by a refresh, a suffix variant is ingested for the base target, the guard fails to raise on a non-Tampa exact-course row, 0 tests collect, or nonzero exit.</fails_when>
  </verify>
  <done>Both tests pass: D-09 cache freshness (refresh populates/updates, no silent staleness) and D-10 suffix pre-filter (exact-course only, guard strictness preserved) are proven on the SQLite default path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| USF schedule source → ingest | Untrusted schedule rows (incl. suffix/other-campus variants) cross into the ingest guard |
| refresh pipeline → DB (cache writes) | Cache population must be atomic with ingest so served scores are never stale |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3.5-02 | Tampering / Information Disclosure | Cache silently going stale after an ingestion (search serves outdated scores as authoritative) | high | mitigate | Population is atomic with the ingest inside refresh_targets' transaction and a wrapped refresh_data stage; the D-09 cache-freshness test proves a change is reflected with no manual step |
| T-3.5-06 | Spoofing / Tampering | Suffix-variant or non-Tampa rows contaminating ingested data (the class of bug PR #16/#18 fixed) | medium | mitigate | D-10 pre-filter drops non-exact-course rows; the Tampa/CRN guard strictness is unchanged and still rolls back on any non-Tampa/wrong-CRN exact-course row; suffix-guard test asserts both directions |
| T-3.5-SC | Tampering | package installs | low | accept | No new dependencies this phase (RESEARCH: N/A) |
</threat_model>

<verification>
- `uv run pytest tests/refresh -q` green (existing + two new tests).
- `uv run pytest -q` (full SQLite suite) green.
- `uv run mypy src migrations scripts tests` and `uv run ruff check .` clean.
- Cross-check quality: `uv run python scripts/check_data_quality.py --term 202701` reports 0 errors (Tampa-only guard clean).
</verification>

<success_criteria>
- Cache population is wired into both refresh_targets (per-target, atomic) and refresh_data (new stage), and cannot silently go stale (D-09), proven by an automated test.
- D-10 suffix pre-filter ingests only exact-course rows without weakening the Tampa/CRN guard, proven by an automated test.
- No regression to existing refresh tests; quality gate stays 0 errors for 202701.
</success_criteria>

<output>
Create `.planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-03-SUMMARY.md` when done.
</output>
