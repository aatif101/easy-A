---
phase: 03.5-ranking-search-performance-at-full-coverage
plan: 05
type: execute
wave: 3
depends_on: [03.5-02, 03.5-03]
files_modified:
  - scripts/benchmark_rankings_search.py
  - tests/refresh/test_postgres_coverage.py
autonomous: false
requirements: [REQ-PERF-01]
user_setup:
  - service: supabase
    why: "Acceptance criterion 2 requires a real before/after p95 measurement against the hosted Supabase database, and criterion 5 requires the PostgreSQL integration test path."
    env_vars:
      - name: EASY_A_TEST_POSTGRES_URL
        source: "A reachable PostgreSQL/Supabase connection string for the integration + benchmark path (session pooler / direct, port 5432). Never commit it; keep it out of git (.env is gitignored)."
      - name: DATABASE_URL
        source: "Hosted Supabase transaction pooler URL (port 6543) for the real-endpoint p95 measurement, per README 'Hosted Supabase Postgres'. Never commit it."
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A benchmark harness seeds a larger synthetic dataset (toward ~3,782 sections) and reports rankings-search p95 with dataset size and environment printed on every number."
    - "Rankings-search p95 against the real hosted Supabase at the seeded larger scale is well under ~1.5s, recorded with dataset size + environment, alongside the before number."
    - "The full test suite is green including the PostgreSQL integration path (EASY_A_TEST_POSTGRES_URL set)."
    - "scripts/check_data_quality.py reports 0 errors for term 202701; Tampa-only guard clean."
  artifacts:
    - scripts/benchmark_rankings_search.py
    - .planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-PERF-REPORT.md
  key_links:
    - "benchmark measures the rewritten /api/v1/rankings/search endpoint (Plan 02) against real Supabase"
    - "synthetic seed labeled a test fixture, never inserted into the real coverage DB (D-04)"
---

<objective>
Prove REQ-PERF-01's performance target with real numbers: build a repeatable benchmark harness that
seeds a larger synthetic dataset and measures rankings-search p95, then run the before/after
measurement against the real hosted Supabase and confirm the full test suite is green on the
PostgreSQL integration path.

Purpose: acceptance criteria 2 (measured p95 < ~1.5s against Supabase, with dataset size +
environment) and 5 (full suite green incl. Postgres) can only be closed with an actual measurement
against real infrastructure — the research sandbox could not reach it.
Output: `scripts/benchmark_rankings_search.py`, an extended Postgres integration test for the live
seat join, and a recorded performance report with before/after numbers.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-CONTEXT.md
@.planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-RESEARCH.md
@.planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-PATTERNS.md

@src/easy_a/api/routes/rankings.py
@src/easy_a/rankings/cache.py
@src/easy_a/db.py
@scripts/check_data_quality.py
@tests/refresh/test_postgres_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Benchmark harness + larger synthetic seed + live-seat Postgres test extension</name>
  <files>scripts/benchmark_rankings_search.py, tests/refresh/test_postgres_coverage.py</files>
  <precondition>The rewritten SQL search path (Plan 02) and cache population (Plans 01/03) are merged; without them the harness measures the old N+1, not the fix.</precondition>
  <behavior>
    - The harness seeds a parameterized number of synthetic sections (toward ~3,782) spanning realistic subject/course/instructor/grade-history diversity, populates section_rankings, and times N iterations of the rankings-search path, printing p50/p95/max WITH the dataset size and environment on every reported number.
    - The harness runs against a provided PostgreSQL/Supabase URL (env-driven) and also runs in a small-scale smoke mode that works on SQLite in-memory for CI-safe self-verification.
    - The synthetic data is clearly labeled a test fixture and is never written to the real coverage database (D-04).
    - The extended Postgres integration test asserts the search response's live seat fields update after a newer SeatSnapshot while cached score fields do not (D-08) on the real Postgres dialect.
  </behavior>
  <action>
    Create scripts/benchmark_rankings_search.py following the CLI scaffold of scripts/check_data_quality.py (argparse + session/engine setup + printed report). Args: `--term`, `--iterations` (default 50), `--sections` (seed size, default toward 3782), `--url` (optional; else read the DB URL from env via the existing config/db helpers — use easy_a.db's engine/pooler-aware setup and is_transaction_pooler rather than re-parsing URLs). Seed synthetic sections/courses/grade history/seat snapshots via `session.add_all(...)` at the requested scale (RESEARCH Key Finding #6), populate section_rankings via refresh_section_rankings, then time the rewritten search path over `--iterations` representative queries (vary sort/filters/pagination). Compute and print p50/p95/max latency, and print the dataset size (section count) and environment (dialect + "synthetic test fixture over <Supabase|local Postgres|SQLite>") with EVERY number. NEVER print or log the connection string / credentials (only the dialect + pooler boolean). Add a `--smoke` mode that seeds a small set on SQLite in-memory so the script self-verifies without network. Do NOT insert synthetic rows into any real coverage database — operate on an ephemeral schema/DB or the seeded fixture only, labeled as such.
    Extend tests/refresh/test_postgres_coverage.py (or add a sibling gated test) reusing its "insert a newer SeatSnapshot mid-test" pattern (lines 53-68) to cover the NEW cache table + rewritten search: populate section_rankings, insert a newer SeatSnapshot, then assert the SEARCH response reflects the new seat data while historical_analytics/score fields (from the cache) are unchanged — on the real Postgres dialect (DISTINCT ON path).
  </action>
  <verify>
    <automated>cd /home/aatif101/wsl_projects/easy-A && uv run python scripts/benchmark_rankings_search.py --smoke --iterations 5 --sections 50</automated>
    <fails_when>The script errors, prints a latency number without an accompanying dataset size + environment label, prints any part of a connection string, or exits nonzero.</fails_when>
  </verify>
  <verify>
    <automated>cd /home/aatif101/wsl_projects/easy-A && uv run pytest tests/refresh/test_postgres_coverage.py -q</automated>
    <fails_when>The extended live-seat test fails or errors (note: the Postgres-gated case skips without EASY_A_TEST_POSTGRES_URL — the SQLite-runnable portions must still pass); nonzero exit.</fails_when>
  </verify>
  <done>scripts/benchmark_rankings_search.py runs in smoke mode and reports p50/p95/max with dataset size + environment and no credential leakage; the Postgres integration test covers the cache + live-seat-join freshness on the real dialect.</done>
</task>

<task type="auto">
  <name>Task 2: Run the real Supabase before/after measurement + full Postgres suite + quality gate</name>
  <files>.planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-PERF-REPORT.md</files>
  <precondition>EASY_A_TEST_POSTGRES_URL and DATABASE_URL are exported for a reachable hosted Supabase instance (user_setup). If unset/unreachable, this task cannot measure real Supabase and must halt as a precondition-unmet checkpoint rather than fabricate numbers (D-04/D-06).</precondition>
  <action>
    Using the user-provided env vars (never printed/committed), run the benchmark against real Supabase: `uv run python scripts/benchmark_rankings_search.py --term <term> --iterations 50 --sections <largest practical, toward 3782>` and capture p50/p95/max + dataset size + environment. Also capture a BEFORE number (the ~600s pilot / current on-demand baseline, e.g. by running the harness against the pre-rewrite path or citing the recorded STATE.md pilot figure) so the report is a genuine before/after. Run the full suite on the Postgres path: `EASY_A_TEST_POSTGRES_URL=<url> uv run pytest -q`. Run `uv run python scripts/check_data_quality.py --term 202701`. Write all results into 03.5-PERF-REPORT.md: before vs after p95 (with dataset size + environment on every number, labeled "synthetic test fixture over Supabase"), full-suite pass/fail counts on the Postgres path, and the quality-gate error count. Never write any credential/connection string into the report or logs. If p95 is not well under ~1.5s, record the result and return to Plan 02 for query/index tuning rather than passing.
  </action>
  <verify>
    <automated>cd /home/aatif101/wsl_projects/easy-A && test -f .planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-PERF-REPORT.md && grep -Eiq 'p95' .planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-PERF-REPORT.md && ! grep -Eiq 'postgres(ql)?://|supabase\.com|password=' .planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-PERF-REPORT.md && echo REPORT_OK</automated>
    <fails_when>The report is missing, contains no p95 figure, or contains a connection string / credential (any `postgres://`/`postgresql://`/`supabase.com`/`password=` substring); prints REPORT_OK only when all hold. Nonzero exit or absence of REPORT_OK is failure.</fails_when>
  </verify>
  <done>03.5-PERF-REPORT.md records the real Supabase before/after p95 (with dataset size + environment), the full Postgres-path suite result, and the 202701 quality error count, with no credentials leaked.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human sign-off on real Supabase p95, Postgres-path suite, quality, and no leaked secrets</name>
  <what-built>The rewritten rankings search measured against real hosted Supabase, with before/after p95 recorded in 03.5-PERF-REPORT.md, the full test suite run on the PostgreSQL integration path, and the data-quality gate run for term 202701. This gate exists because criteria 2 and 5 are the phase's load-bearing acceptance numbers that a human must sign off on, and the measurement depends on hosted credentials/network CI does not have.</what-built>
  <how-to-verify>
    Open .planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-PERF-REPORT.md and confirm:
    1. An AFTER p95 for rankings search against real Supabase that is well under ~1.5s, with dataset size and environment stated on the number, alongside a BEFORE number (e.g. the ~600s pilot baseline).
    2. The full test suite is green on the Postgres path (`EASY_A_TEST_POSTGRES_URL` set) — pass count recorded, no failures.
    3. `check_data_quality.py --term 202701` reported 0 errors.
    4. `git status` shows no staged .env* / .env.save / credential files, and the report contains no connection string.
    If AFTER p95 is not under ~1.5s, do NOT approve — return to Plan 02 for query/index tuning.
  </how-to-verify>
  <resume-signal>Type "approved" once the recorded p95 is under ~1.5s, the Postgres-path suite is green, quality is 0 errors, and no secrets are staged — or describe what is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app/benchmark → Supabase (DB credentials) | DATABASE_URL / EASY_A_TEST_POSTGRES_URL secrets must stay out of git and out of logs |
| synthetic seed → data honesty | Fixture data must never be presented as real coverage (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3.5-04 | Information Disclosure | Supabase DB credentials leaking into git or benchmark logs (untracked .env.save present in the working tree) | high | mitigate | Credentials read from env only (never a hardcoded/committed string); benchmark prints dialect + pooler boolean, never the URL; .env* gitignored; the human-verify gate confirms `git status` shows no staged secrets |
| T-3.5-07 | Information Disclosure / Integrity | Synthetic benchmark data presented as real coverage, or written into the real DB | medium | mitigate | Seed labeled a test fixture in every report line; harness operates on an ephemeral/seeded DB, never the real coverage database; performance report states "synthetic test fixture over Supabase" distinct from the real pilot numbers (D-04) |
| T-3.5-SC | Tampering | package installs | low | accept | No new dependencies this phase (RESEARCH: N/A) |
</threat_model>

<verification>
- `uv run python scripts/benchmark_rankings_search.py --smoke ...` runs and reports p50/p95/max with dataset size + environment, no credential leakage.
- Human-verify gate: real Supabase before/after p95 (< ~1.5s after) recorded in 03.5-PERF-REPORT.md; `EASY_A_TEST_POSTGRES_URL=<url> uv run pytest -q` green; `uv run python scripts/check_data_quality.py --term 202701` = 0 errors; `git status` shows no staged secrets.
</verification>

<success_criteria>
- Repeatable benchmark harness exists, self-verifies in smoke mode, and reports p95 with dataset size + environment (acceptance criterion 2 methodology).
- Real Supabase before/after p95 measured and recorded, after < ~1.5s (acceptance criterion 2).
- Full test suite green including the PostgreSQL integration path (acceptance criterion 5).
- Quality gate 0 errors for 202701 (acceptance criterion 3); no secrets committed (D-05).
</success_criteria>

<output>
Create `.planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-05-SUMMARY.md` when done, and `.planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-PERF-REPORT.md` with the before/after measurement.
</output>
