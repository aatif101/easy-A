---
phase: "3.5"
slug: "ranking-search-performance-at-full-coverage"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-20"
---

# Phase 3.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Detailed per-task rows are filled by the planner; see 03.5-RESEARCH.md §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (backend); existing frontend suite (vitest/jest) unaffected |
| **Config file** | project root (pyproject/pytest config); Postgres path gated on `EASY_A_TEST_POSTGRES_URL` |
| **Quick run command** | `pytest tests/rankings tests/refresh -q` (planner to confirm exact scope) |
| **Full suite command** | `pytest` (SQLite default), and `EASY_A_TEST_POSTGRES_URL=... pytest` for the integration path |
| **Estimated runtime** | ~baseline: 192 passed / 1 skipped (SQLite); 193 passed (Postgres) |

---

## Sampling Rate

- **After every task commit:** Run the quick run command for touched areas.
- **After every plan wave:** Run the full suite (SQLite).
- **Before `/gsd-verify-work`:** Full suite green on BOTH SQLite and the Postgres integration path.
- **Max feedback latency:** target < 120s for the quick path.

---

## Per-Task Verification Map

> One row per task authored in 03.5-0N-PLAN.md. Every task has an `<automated>` verify or a Wave 0
> dependency; the leading tracer (3.5-01-01) carries the D-02 parity check (the phase's central risk).

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 3.5-01-01 | 01 | 1 | REQ-PERF-01 (D-01/D-02/D-04/D-07/D-08) | T-3.5-02 | Cached rows byte-for-byte identical to on-demand `rank_section()`; no seat fields cached | parity/unit + Postgres integration | `uv run pytest tests/rankings/test_cache_parity.py -x -q` | ❌ W0 | ⬜ pending |
| 3.5-01-02 | 01 | 1 | REQ-PERF-01 (D-02/D-04/D-08) | T-3.5-02 | Parity holds across all score/provenance/confidence branches + seat-fresher-than-cache | parity/unit | `uv run pytest tests/rankings/test_cache_parity.py -q` | ❌ W0 | ⬜ pending |
| 3.5-02-01 | 02 | 2 | REQ-PERF-01 (D-03/D-06/D-08) | T-3.5-01, T-3.5-03 | SQL-side filter/sort/paginate + live seat join; parameterized queries; contract unchanged | unit/contract | `uv run pytest tests/api/test_rankings_api.py -q` | ✅ (contract guard exists) | ⬜ pending |
| 3.5-02-02 | 02 | 2 | REQ-PERF-01 (D-03/D-06/D-08) | T-3.5-01, T-3.5-03 | Ordering incl. NULL seats; total==paged items; filters; seat 3-tier fallback; injection-literal safe | unit | `uv run pytest tests/api/test_rankings_search_sql.py -x -q` | ❌ W0 | ⬜ pending |
| 3.5-03-01 | 03 | 2 | REQ-PERF-01 (D-09/D-10) | T-3.5-06, T-3.5-02 | Exact-course pre-filter before unchanged Tampa/CRN guard; atomic cache population | unit/integration | `uv run pytest tests/refresh -q` | ✅ (refresh suite exists) | ⬜ pending |
| 3.5-03-02 | 03 | 2 | REQ-PERF-01 (D-09) | T-3.5-02 | New refresh_data stage wrapped in RefreshStageError; cache populated per term | unit/integration | `uv run pytest tests/refresh -q` | ✅ (refresh suite exists) | ⬜ pending |
| 3.5-03-03 | 03 | 2 | REQ-PERF-01 (D-09/D-10) | T-3.5-06, T-3.5-02 | Cache freshness (no silent staleness) + suffix pre-filter, guard strictness preserved | unit/integration | `uv run pytest tests/refresh/test_rankings_cache_refresh.py tests/refresh/test_coverage_suffix_guard.py -x -q` | ❌ W0 | ⬜ pending |
| 3.5-04-01 | 04 | 2 | REQ-PERF-01 (D-01) | T-3.5-05 | Explicit delete + identity verification of cache rows on section removal | unit/integration | `uv run pytest tests/refresh -q` | ✅ (refresh suite exists) | ⬜ pending |
| 3.5-04-02 | 04 | 2 | REQ-PERF-01 (D-01) | T-3.5-05 | No orphaned cache rows, no FK violation, identity check covers cache table | unit/integration | `uv run pytest tests/refresh/test_cleanup_cascade.py -x -q` | ❌ W0 | ⬜ pending |
| 3.5-05-01 | 05 | 3 | REQ-PERF-01 (D-04/D-06) | T-3.5-04, T-3.5-07 | Benchmark reports p95 with dataset size + environment, no credential leak; live-seat Postgres test | integration/benchmark | `uv run python scripts/benchmark_rankings_search.py --smoke --iterations 5 --sections 50` | ❌ W0 | ⬜ pending |
| 3.5-05-02 | 05 | 3 | REQ-PERF-01 (D-04/D-06) | T-3.5-04, T-3.5-07 | Real Supabase before/after p95 recorded (< ~1.5s), no credentials in report | integration/benchmark | `test -f .../03.5-PERF-REPORT.md && grep -Eiq 'p95' ... && ! grep -Eiq 'postgres(ql)?://\|supabase.com\|password='` | ❌ W0 | ⬜ pending |
| 3.5-05-03 | 05 | 3 | REQ-PERF-01 (criteria 2/3/5) | T-3.5-04 | Human sign-off: p95 < ~1.5s, Postgres-path suite green, 0 quality errors, no staged secrets | manual (checkpoint:human-verify, blocking-human) | `<human-check>` on 03.5-PERF-REPORT.md + `EASY_A_TEST_POSTGRES_URL=… uv run pytest -q` + `uv run python scripts/check_data_quality.py --term 202701` | manual | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Sampling continuity:** No run of 3 consecutive tasks lacks an automated verify — every task above
carries an `<automated>` command except the final human-verify sign-off (3.5-05-03), which is
immediately preceded by two automated tasks (3.5-05-01, 3.5-05-02).

---

## Wave 0 Requirements

Per 03.5-RESEARCH.md "Wave 0 Gaps", finalized net-new test/bench scaffolding (exact paths):

- [ ] `tests/rankings/test_cache_parity.py` (Plan 01) — cached `section_rankings` rows equal
      on-demand `rank_section()` across ALL non-seat fields (scores, provenance, confidence,
      `effective_n`), all branches; seats compared separately with controlled `as_of`.
- [ ] `tests/api/test_rankings_search_sql.py` (Plan 02) — SQL ordering incl. NULL seats,
      total/pagination consistency, filters, seat 3-tier fallback, parameterized-query safety.
- [ ] `tests/refresh/test_rankings_cache_refresh.py` (Plan 03) — cache is (re)populated by the
      refresh pipeline and cannot silently go stale after an ingestion (D-09).
- [ ] `tests/refresh/test_coverage_suffix_guard.py` (Plan 03) — exact `(subject, course_number)`
      pre-filter before the Tampa/CRN guard; guard strictness unchanged (D-10).
- [ ] `tests/refresh/test_cleanup_cascade.py` (Plan 04) — deleting sections purges/verifies
      `section_rankings` rows (no FK violation, no orphans), matching `refresh/cleanup.py`.
- [ ] `scripts/benchmark_rankings_search.py` (Plan 05) — seeds a larger dataset (toward ~3,782
      sections) and measures rankings-search p95 against Supabase before/after (records dataset
      size + environment on every number); `--smoke` self-verifies on SQLite.
- [ ] Extend `tests/refresh/test_postgres_coverage.py` (Plan 05) — the existing mid-test newer-
      `SeatSnapshot` pattern (lines 53-68) covers the new cache table's live-seat-join freshness.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real Supabase p95 latency at full coverage | REQ-PERF-01 | Requires the hosted Supabase DB and a large seeded dataset unavailable in unit CI | Run the benchmark harness against Supabase; record p95, dataset size, environment |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
