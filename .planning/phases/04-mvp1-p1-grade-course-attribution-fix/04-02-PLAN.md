---
phase: 04-mvp1-p1-grade-course-attribution-fix
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/easy_a/grades/parser.py
  - tests/grades/test_parser.py
  - src/easy_a/quality/checks.py
  - tests/quality/test_checks.py
autonomous: true
requirements: [REQ-GRADES-01]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A blank canonical A/B/C/D/F/I/S/U/W/O or Total Grades cell rejects the workbook with row, column, and unverified-semantics context; an explicit numeric zero remains valid (D-06, D-07)."
    - "The parser never labels a blank as suppressed and never manufactures a zero where the source supplied no canonical count (D-06, D-07)."
    - "Data quality emits one deterministic error finding for each stored GradeDistribution whose course_id is null, while attributed rows do not receive that finding (D-04, D-07)."
    - "Parser and quality hardening do not alter scoring, term/CRN/source identity, provenance, the analytics fallback, or the rankings API (D-02, D-04)."
  artifacts:
    - path: src/easy_a/grades/parser.py
      provides: "Fail-closed canonical grade-count cell validation"
    - path: tests/grades/test_parser.py
      provides: "Blank bucket/total rejection and explicit-zero acceptance coverage"
    - path: src/easy_a/quality/checks.py
      provides: "`unattributed_grade_row` quality invariant"
    - path: tests/quality/test_checks.py
      provides: "Attributed/null-attributed quality finding controls"
  key_links:
    - from: src/easy_a/grades/parser.py
      to: src/easy_a/grades/ingest.py
      via: "GradeWorkbookValidationError aborts the workbook before the attribution preflight/upsert"
      pattern: "GradeWorkbookValidationError"
    - from: src/easy_a/quality/checks.py
      to: src/easy_a/models/core.py
      via: "_check_grades inspects persisted GradeDistribution.course_id without inferring from CRN"
      pattern: "course_id is None"
---

<objective>
Fail closed where upstream blank grade-count semantics are unknown and expose any persisted null course attribution as an explicit data-quality error.

Purpose: Prevent unsupported blank-to-zero/suppression claims (D-06/D-07), make legacy/null attribution visible, and complete the safety envelope around Plan 04-01 without changing scoring or grade identity (D-02/D-04).
Output: Strict blank-cell validation with explicit-zero support, an `unattributed_grade_row` quality finding, and focused regression coverage.
</objective>

<execution_context>
@/home/aatif101/.codex/gsd-core/workflows/execute-plan.md
@/home/aatif101/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-mvp1-p1-grade-course-attribution-fix/04-RESEARCH.md
@.planning/phases/04-mvp1-p1-grade-course-attribution-fix/04-PATTERNS.md
@.planning/phases/04-mvp1-p1-grade-course-attribution-fix/04-VALIDATION.md

@src/easy_a/grades/parser.py
@src/easy_a/quality/checks.py
@tests/grades/test_parser.py
@tests/quality/test_checks.py
</context>

## Artifacts this phase produces

- `tests/grades/test_parser.py`: new `test_blank_bucket_count_fails_closed()`, `test_blank_total_fails_closed()`, and `test_explicit_zero_count_succeeds()` tests; the old blank-as-zero expectation is removed.
- `src/easy_a/quality/checks.py`: new stable `QualityFinding.check_id` value `unattributed_grade_row` emitted from existing `_check_grades()`; no new public function.
- `tests/quality/test_checks.py`: new `test_unattributed_grade_row_is_an_error()` and `test_attributed_grade_row_has_no_unattributed_finding()` tests; existing `_grade()` gains a `course_id: int | None` parameter.
- No new parser model field, database field, migration, CLI option, package, score state, or API field is created.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject ambiguous blank canonical counts while accepting explicit zero</name>
  <reversibility rating="reversible">This is an internal input-eligibility policy in one parser helper; authoritative upstream evidence can later replace it without a schema or stored-data migration.</reversibility>
  <files>src/easy_a/grades/parser.py, tests/grades/test_parser.py</files>
  <read_first>
    - `.planning/PROJECT.md` decisions D-06 and D-07 plus open question OQ-04
    - `.planning/phases/04-mvp1-p1-grade-course-attribution-fix/04-RESEARCH.md` Pattern 4 and blank-cell confidence/open-question sections
    - `.planning/phases/04-mvp1-p1-grade-course-attribution-fix/04-PATTERNS.md` parser/test assignments and canonical bucket list
    - `src/easy_a/grades/parser.py` `_cell_to_int()`, `_is_empty_cell()`, aggregate row-error handling, and `GRADE_BUCKETS`
    - `tests/grades/test_parser.py` generated workbook helpers and current blank-as-zero test
  </read_first>
  <behavior>
    - A blank canonical grade bucket cell raises GradeWorkbookValidationError containing its row, canonical column name, and the reason that blank semantics are unverified.
    - A blank Total Grades cell fails through the same aggregate workbook validation path.
    - Explicit numeric integer zero in every canonical count field is accepted; ignored percentage columns may remain blank.
    - Boolean, nonnumeric, noninteger, negative/count-sum, and multi-row error aggregation behavior remains under existing validation contracts.
  </behavior>
  <action>
    Replace `test_empty_count_cells_are_zero` with failing tests for a blank bucket and blank `Total Grades`, and add an explicit-zero positive control using the existing `_write_workbook()` and `_grade_row()` helpers under `tmp_path`. Assert the workbook-level exception includes the row, canonical column, and language that the blank's semantics are unverified; do not assert or introduce a source claim that the cell is suppressed.

    In `src/easy_a/grades/parser.py`, change only `_cell_to_int()`'s empty-cell branches so `_is_empty_cell(value)` and stripped empty text raise a row/column-specific `ValueError` instead of returning zero. Keep explicit integer/numeric-string zero valid, keep percentage columns ignored, keep all ten grade buckets and Total Grades required, and retain the existing aggregate `GradeWorkbookValidationError` behavior. Do not introduce nullable counts, a suppression state, or any scoring change (D-02, D-06, D-07).
  </action>
  <acceptance_criteria>
    - Blank canonical bucket and blank Total Grades inputs both produce GradeWorkbookValidationError with row and column details.
    - The failure reason says blank semantics are unverified and does not assert that the source suppressed the value.
    - Explicit numeric zero is accepted and the resulting ParsedGradeDistribution retains integer zero counts.
    - Percentage-column blanks remain ignored, and existing parser validation tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aatif101/wsl_projects/easy-A && uv run pytest tests/grades/test_parser.py -x -q</automated>
    <fails_when>Blank canonical counts are accepted, explicit zero is rejected, the error lacks row/column/unverified-semantics context, existing parser tests regress, no tests collect, or the command exits nonzero.</fails_when>
  </verify>
  <done>Canonical blank count cells are explicit rejected inputs with honest rationale, while numeric zeros remain valid and no unsupported suppression meaning is stored or claimed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface persisted null course attribution as a data-quality error</name>
  <reversibility rating="reversible">The new invariant is one additional deterministic finding inside the existing grade-quality pass and can be removed without changing stored data or public contracts.</reversibility>
  <files>src/easy_a/quality/checks.py, tests/quality/test_checks.py</files>
  <read_first>
    - `.planning/PROJECT.md` decisions D-04, D-06, D-07, and D-20
    - `.planning/phases/04-mvp1-p1-grade-course-attribution-fix/04-RESEARCH.md` validation map and quality recommendation
    - `.planning/phases/04-mvp1-p1-grade-course-attribution-fix/04-PATTERNS.md` `_check_grades()` and finding-test analogs
    - `src/easy_a/quality/checks.py` `run_quality_checks()`, `_check_grades()`, and `QualityFinding` construction
    - `tests/quality/test_checks.py` `_grade()`, `_finding()`, and existing grade finding tests
    - `src/easy_a/models/core.py` nullable `GradeDistribution.course_id` field
  </read_first>
  <behavior>
    - Each GradeDistribution with course_id null produces one error finding with check_id `unattributed_grade_row`, its term/CRN, and `grade_distribution:{id}` source_record.
    - The message explains that the row cannot supply course historical analytics; it does not infer identity from CRN and does not call the row suppressed.
    - An otherwise equivalent attributed row produces no `unattributed_grade_row` finding.
    - Existing grade-total and historical term+CRN orphan checks remain present and independent.
  </behavior>
  <action>
    In `tests/quality/test_checks.py`, extend `_grade()` with an explicit `course_id: int | None = 10` parameter. Add a null-attributed-row test that asserts `unattributed_grade_row`, error severity, term, CRN, and source record, plus a positive control proving an attributed row does not receive that check ID. Write these tests before the production finding.

    In the existing `src/easy_a/quality/checks.py::_check_grades()` distribution loop, append one deterministic `QualityFinding` for every row whose `course_id is None`. Use check ID `unattributed_grade_row`, error severity, the current term and row CRN, and `grade_distribution:{distribution.id}` source record. State that the row cannot provide course historical analytics until attributed. Do not infer a course from CRN, remove the legacy term+CRN orphan check, alter grade totals, or change analytics/scoring behavior (D-02, D-04, D-07).
  </action>
  <acceptance_criteria>
    - A null-attributed stored row produces exactly one `unattributed_grade_row` error with deterministic identity fields.
    - An attributed stored row produces no finding with that check ID.
    - Existing `grade_total_mismatch` and `orphan_grade_row` tests remain green and retain their distinct meanings.
    - No course is inferred from CRN and no data is mutated by the quality pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/aatif101/wsl_projects/easy-A && uv run pytest tests/quality/test_checks.py -x -q</automated>
    <fails_when>The null row is not flagged, an attributed row is falsely flagged, existing grade-quality checks regress, no tests collect, or the command exits nonzero.</fails_when>
  </verify>
  <done>Stored null-attributed grade rows are explicit quality errors, while correctly attributed rows remain clean and existing grade-quality checks remain independent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator XLSX -> parser | Blank, formula-like, boolean, and numeric cells cross into canonical grade counts and frozen scoring inputs. |
| Parsed records -> canonical database | Plan 04-01's canonical lookup and transaction boundary must remain the only attribution path for new rows. |
| Canonical database -> quality report | Stored null attribution must be reported without mutating rows or inventing a course identity. |
| Workspace -> Git | Raw and generated workbooks must never become tracked project artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Tampering | XLSX canonical count parsing | high | mitigate | Reject blank canonical counts and Total Grades with aggregate row-specific validation; accept only explicit validated integer zero; retain count-sum checks. |
| T-04-08 | Spoofing / Tampering | Canonical course attribution | high | mitigate | Depend on Plan 04-01's all-key shared Course lookup preflight; quality never guesses identity from CRN or creates stand-in courses. |
| T-04-09 | Tampering | Atomicity and term/CRN/source dedup | high | mitigate | Parser validation completes before attribution/upsert; retain existing unique identity and Plan 04-01 atomicity/backfill tests unchanged. |
| T-04-10 | Repudiation | Grade source provenance | medium | mitigate | Parser/quality changes do not modify `source` or SHA-256 `source_hash`; the Plan 04-01 provenance tests remain phase gates. |
| T-04-11 | Information Disclosure | Raw/generated XLSX files | high | mitigate | Use `tmp_path` generation only and gate tracked `.xlsx`/`.xls` files at phase verification (D-19). |
| T-04-12 | Denial of Service | Compression-heavy or unexpectedly large workbook | low | accept | Files are operator-controlled and no evidence-backed limit exists; do not invent a threshold, and reassess with representative Phase 05 exports. |
| T-04-13 | Tampering | Quality SQL predicates | medium | mitigate | Use existing SQLAlchemy model expressions and loaded ORM rows; add no interpolated SQL. |
| T-04-SC | Tampering | Package installation/supply chain | low | accept | This plan installs no package; it uses already locked pandas/openpyxl/SQLAlchemy/pytest dependencies. |
</threat_model>

<source_coverage_audit>

| Source | ID | Feature / constraint | Plan / task | Status |
|--------|----|----------------------|-------------|--------|
| ROADMAP goal | — | Decide safe blank-cell handling without changing score behavior | 04-02 Task 1 | COVERED |
| REQUIREMENTS | REQ-GRADES-01 | Honest imported evidence and explicit lack of usable attribution | 04-01 Tasks 1-2; 04-02 Tasks 1-2 | COVERED |
| RESEARCH | — | Blank canonical counts fail closed; explicit numeric zero succeeds | 04-02 Task 1 | COVERED |
| RESEARCH | — | Null-attributed stored rows surface through quality checks | 04-02 Task 2 | COVERED |
| RESEARCH | — | Canonical preflight, same-key repair, 202701 cache rebuild | 04-01 Tasks 1-2 | COVERED |
| PATTERNS | — | Existing parser aggregate-error and `_check_grades()` finding conventions | 04-02 Tasks 1-2 | COVERED |
| VALIDATION | — | Replace blank-as-zero test, preserve zero control, add quality test when guard included | 04-02 Tasks 1-2 | COVERED |
| VALIDATION | — | Generated-XLSX direct-attribution vertical proof and ingest invariants | 04-01 Tasks 1-2 | COVERED |
| PROJECT decisions | D-02/D-04/D-06/D-07/D-19/D-20 | Frozen score, identity/provenance, honest states, no raw export, no false history | 04-01 Tasks 1-2; 04-02 Tasks 1-2 | COVERED |
| CONTEXT | — | No CONTEXT.md exists; user explicitly authorized planning from roadmap, requirements, research, patterns, and validation | — | EXCLUDED (documented) |

</source_coverage_audit>

<verification>
  <automated>cd /home/aatif101/wsl_projects/easy-A && uv run pytest -q tests/grades tests/analytics tests/test_grade_course_attribution.py tests/rankings/test_cache_parity.py tests/refresh/test_rankings_cache_refresh.py tests/quality/test_checks.py</automated>
  <fails_when>Any phase-focused parser, ingest, analytics, cache, search, or quality regression fails; no tests collect; or the command exits nonzero.</fails_when>
  <automated>cd /home/aatif101/wsl_projects/easy-A && uv run pytest -q</automated>
  <fails_when>The full default suite fails or exits nonzero; expected environment-dependent skips must remain reported and must not be described as passes.</fails_when>
  <automated>cd /home/aatif101/wsl_projects/easy-A && uv run ruff check .</automated>
  <fails_when>Ruff reports any violation or exits nonzero.</fails_when>
  <automated>cd /home/aatif101/wsl_projects/easy-A && uv run mypy src migrations scripts tests</automated>
  <fails_when>Strict mypy reports any error or exits nonzero.</fails_when>
  <automated>cd /home/aatif101/wsl_projects/easy-A && easy_a_tracked_exports=$(git ls-files -- '*.xlsx' '*.xls') && test -z "$easy_a_tracked_exports"</automated>
  <fails_when>Git tracks one or more `.xlsx` or `.xls` files, the listing command fails, or the emptiness test exits nonzero (D-19).</fails_when>
  <environment_gate>If `EASY_A_TEST_POSTGRES_URL` is available in an authorized environment, run `cd /home/aatif101/wsl_projects/easy-A && uv run pytest tests/refresh/test_postgres_coverage.py -q` and require zero exit with no unexpected skip. If the variable is unavailable, record the configured PostgreSQL path as not run and do not claim it passed (D-12).</environment_gate>
</verification>

<success_criteria>
- Blank canonical grade counts and blank Total Grades fail closed with explicit unverified-semantics reasons; explicit numeric zero remains accepted (D-06, D-07).
- Null-attributed GradeDistribution rows are visible as `unattributed_grade_row` quality errors; attributed rows are not falsely flagged.
- The full phase-focused suite, full default suite, ruff, and strict mypy pass; PostgreSQL status is reported honestly according to environment availability (D-12).
- Git tracks no `.xlsx` or `.xls` export, and scoring/analytics/API behavior remains unchanged except for the newly supplied evidence cohort (D-02, D-19, D-20).
</success_criteria>

<output>
Create `.planning/phases/04-mvp1-p1-grade-course-attribution-fix/04-02-SUMMARY.md` when done.
</output>
