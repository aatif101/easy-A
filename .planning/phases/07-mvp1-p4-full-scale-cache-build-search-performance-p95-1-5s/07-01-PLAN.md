---
phase: 07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/benchmark_rankings_search.py
  - tests/api/test_benchmark_rankings_search.py
  - .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md
autonomous: true
requirements: [REQ-PERF-01]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - A read-only benchmark measures both direct search_rankings calls for diagnosis and actual HTTP GET responses from a loopback-served API using the stored 202701 term; it reports row count, deterministic query mix, p50/p95/max and resolved database environment separately for each mode.
    - The benchmark cannot disclose DATABASE_URL credentials or present a synthetic fixture as live Supabase evidence.
    - Baseline evidence labels loopback HTTP round-trip latency separately from in-process route, count, page and hydration timings and includes representative PostgreSQL count/page plans.
  artifacts:
    - scripts/benchmark_rankings_search.py
    - tests/api/test_benchmark_rankings_search.py
    - .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md
  key_links:
    - Direct live mode uses the resolved SQLAlchemy engine and existing search_rankings route with no fixture seed or write; HTTP mode issues GET /api/v1/rankings/search to a local server that resolves the same hosted database.
    - The report records sanitized engine classification and compiled, bound count/page plan summaries.
---

<objective>
Measure the production search path and expose the real bottlenecks before tuning it (REQ-PERF-01).
Purpose: The prior 2.40s figure came from a synthetic fixture with a misleading environment label; a full-scale live baseline must be unambiguous.
Output: Read-only live benchmark, label regression tests, and dated baseline evidence.
</objective>

<execution_context>
@C:/Users/smati/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/smati/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-RESEARCH.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PATTERNS.md
@.planning/phases/03.5-ranking-search-performance-at-full-coverage/03.5-PERF-REPORT.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Run one stored-term search through the real benchmark path</name>
  <files>scripts/benchmark_rankings_search.py, tests/api/test_benchmark_rankings_search.py</files>
  <read_first>scripts/benchmark_rankings_search.py; src/easy_a/api/routes/rankings.py; tests/api/test_rankings_search_sql.py</read_first>
  <behavior>Direct --live mode counts stored 202701 ranking rows, calls search_rankings for a representative page and reports in-process elapsed wall time without writing. --live --http-base-url sends the same deterministic queries as actual HTTP GET /api/v1/rankings/search requests to a loopback server, consumes each complete response body, requires 200 and a valid search response, uses warmup plus at least 50 measured calls, and reports HTTP round-trip p50/p95/max separately. Synthetic mode still seeds then rolls back. Both live modes label dialect, host class and pooler from resolved engine.url even when --url is omitted. Secret URL, password, username and host text never enter output.</behavior>
  <action>Add a separate --live read-only mode with bounded term, iterations and query mix; reuse _representative_queries, _percentile and existing route call for an explicitly labeled in-process diagnostic. Add --http-base-url for a loopback-only local API target using the existing httpx dependency; send the same deterministic query mix as GET /api/v1/rankings/search, time request through complete response-body read and JSON parse, check HTTP 200/search response, warm up before at least 50 measured calls, and label this as locally served HTTP over hosted Supabase rather than deployed/browser latency. Reject a non-loopback base URL. Build the database environment label from the actual resolved engine.url rather than optional CLI URL; require the local API process to inherit the same DATABASE_URL for the hosted comparison. Label live, HTTP and synthetic datasets distinctly, include actual count and p50/p95/max for each, dispose the engine, and keep synthetic rollback behavior. Write focused failing tests first for the DATABASE_URL label bug, HTTP URL validation/query-mix/response handling, live no-write behavior and redaction, then implement. D-02 and D-20: this is observation only; do not alter scoring or make global-prior rows sound course-backed.</action>
  <verify><automated>uv run pytest tests/api/test_benchmark_rankings_search.py tests/api/test_rankings_search_sql.py -q</automated></verify>
  <done>One real stored-term page is timed by both direct-call and loopback HTTP paths with clearly different labels; tests prove resolved-engine label, query mix, response validation, no credential leakage and no fixture writes.</done>
</task>

<task type="auto">
  <name>Capture the full-scale search and cache baseline</name>
  <precondition>DATABASE_URL resolves to a reachable hosted PostgreSQL database containing term 202701 and its section_rankings; a loopback uvicorn easy_a.api.app:app server is running with the same DATABASE_URL; assert only presence and connectivity without displaying the URL.</precondition>
  <files>.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md</files>
  <read_first>scripts/benchmark_rankings_search.py; src/easy_a/api/routes/rankings.py; src/easy_a/rankings/cache.py; .planning/phases/06-mvp1-p3-all-tampa-section-ingestion-10-3-782/06-03-SUMMARY.md</read_first>
  <action>Start a loopback-only local uvicorn server configured with the same hosted DATABASE_URL, verify it serves the stored 202701 term, then run both --live direct-call and --live --http-base-url loopback modes with warmup and at least 50 measured calls using the same representative broad/filtered, seat/no-seat and supported sort mix; stop the server afterward. Record UTC date, sanitized target, committed dataset counts, exact query parameters, iterations, and per-query/aggregate p50/p95/max for both modes, labeling HTTP request+body+parse separately from in-process timing. Instrument the existing search path locally to distinguish count-query, page-query, network-inclusive SQL wall time and hydration; capture EXPLAIN (ANALYZE, BUFFERS) for representative bound count and page statements and summarize plan nodes, rows, loops and buffers without credentials or grade records. Measure a whole-term cache rebuild and whole-term analytics quality pass only through existing operator-approved transaction paths, record elapsed time, statement counts, grade aggregate reads and resulting row count; preserve caller commit/rollback boundaries and do not repeat the rebuild solely for benchmarking. If credentials are absent, stop this task at its external precondition and record it as unmeasured, never extrapolate the synthetic 2.40s value to live Supabase. D-06: every figure needs date, environment and actual source.</action>
  <verify><automated>uv run python scripts/benchmark_rankings_search.py --live --http-base-url http://127.0.0.1:8000 --term 202701 --iterations 50</automated></verify>
  <done>07-PERF-REPORT.md holds a dated, sanitized live baseline for loopback HTTP serving, separately labeled in-process diagnostics and cache construction with real counts and independent count/page diagnoses; no p95 success claim is made from direct-call or synthetic data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Environment to benchmark output | DATABASE_URL contains credentials and must be classified without being echoed. |
| CLI parameters to SQL | User-provided term, iteration and filters must stay bounded and SQLAlchemy-parameterized. |
| Benchmark to hosted DB | Live measurement must issue reads only; the existing synthetic transaction must roll back. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Information disclosure | benchmark label/report | high | mitigate | Derive only sanitized dialect/host class/pooler from engine.url; test password and host redaction. |
| T-07-02 | Tampering | live benchmark | high | mitigate | Separate --live branch performs no seed, refresh, DDL or commit; test no-write behavior. |
| T-07-03 | Denial of service | benchmark CLI | medium | mitigate | Bound iterations and page size; retain FastAPI search limit and parameter binding. |
</threat_model>

<verification>
Run focused benchmark/search tests. Run the live read-only benchmark when its external precondition is met; compare report's stored count with Phase 6 reconciliation and record the exact query mix.
</verification>

<success_criteria>
The project has truthful, reproducible full-scale search and build baselines; REQ-PERF-01 remains open until a later plan demonstrates live p95 below 1.5s.
</success_criteria>

## Artifacts this phase produces
- `benchmark_rankings_search.py --live`: read-only stored-term in-process route diagnostic and sanitized resolved-engine classification.
- `benchmark_rankings_search.py --live --http-base-url http://127.0.0.1:8000`: loopback HTTP GET benchmark using the same deterministic query mix, warmup and complete-response timing; reports the separate HTTP p50/p95/max gate metric.
- `07-PERF-REPORT.md`: baseline evidence and count/page EXPLAIN summaries, extended in Plan 03.

<output>
Create .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-01-SUMMARY.md when done.
</output>
