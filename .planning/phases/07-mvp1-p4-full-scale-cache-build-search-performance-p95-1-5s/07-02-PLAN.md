---
phase: 07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/easy_a/analytics/queries.py
  - src/easy_a/rankings/cache.py
  - src/easy_a/quality/checks.py
  - tests/analytics/test_queries.py
  - tests/rankings/test_cache_parity.py
  - tests/refresh/test_rankings_cache_refresh.py
autonomous: true
requirements: [REQ-PERF-01]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Whole-term cache and quality builds reuse historical grade evidence across courses and finish materially faster than the measured baseline.
    - Cached and on-demand ranking payloads are byte-for-byte equal across course, subject, global and instructor-course evidence branches.
    - A global fallback still has effective_n=0 and no claim of course history; the 10 sourced pilot courses remain course-backed.
  artifacts:
    - src/easy_a/analytics/queries.py
    - src/easy_a/rankings/cache.py
    - src/easy_a/quality/checks.py
    - tests/analytics/test_queries.py
    - tests/rankings/test_cache_parity.py
  key_links:
    - Batch inputs feed the existing fallback and scoring functions, preserving grade weighting and source selection.
    - refresh_section_rankings and _check_analytics consume the same whole-term batched evidence while public per-course APIs remain available to on-demand callers.
---

<objective>
Make full-term cache construction and quality analytics practical at 3,783 sections without changing historical scoring (REQ-PERF-01, D-02, D-20).
Purpose: Phase 6 observed repeated global/subject aggregation per course and a roughly 35-minute quality pass; shared evidence can remove those repeated reads.
Output: Batch analytics path, cache/quality consumers, and exact parity/query-count tests.
</objective>

<execution_context>
@C:/Users/smati/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/smati/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-RESEARCH.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PATTERNS.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Batch historical evidence across courses with exact fallback parity</name>
  <files>src/easy_a/analytics/queries.py, tests/analytics/test_queries.py</files>
  <read_first>src/easy_a/analytics/queries.py; src/easy_a/analytics/grades.py; src/easy_a/analytics/scoring.py; tests/analytics/test_queries.py</read_first>
  <behavior>For a supplied target term and course set, batch results equal existing per-course HistoricalOutcomeStats for sourced course, subject prior excluding that course, global prior, and sufficient/insufficient instructor-course evidence. Rows joined by term+CRN or attributed course_id remain included once under the existing source-selection policy; pre-term filtering, recency weighting, ten buckets, confidence, effective_n, score_source, prior level and provenance are unchanged. SQL grade-read count stays bounded as course count grows.</behavior>
  <action>Write failing batch-vs-public parity and query-count tests first. Add a private whole-term evidence context that reads the same pre-term GradeDistribution observations and instructor mappings once, groups observations by attributed or joined course identity, and derives per-course and course-excluding subject inputs. Pass these inputs to existing aggregate_grade_observations, _stats_with_course_subject_global_fallback and compute_historical_outcome_stats; retain public per-course functions and instructor sufficiency branch. Do not approximate a weighted aggregate by subtracting totals. D-02 freezes the grade/withdrawal formula, Bayesian shrinkage and confidence labels; D-04 preserves source deduplication; D-20 keeps global-prior effective_n=0 explicitly unevidenced.</action>
  <verify><automated>uv run pytest tests/analytics/test_queries.py -q</automated></verify>
  <done>Batch stats exactly equal existing public stats for all evidence branches and query count no longer grows by repeated global/subject grade scans per course.</done>
</task>

<task type="auto" tdd="true">
  <name>Use batch evidence in the full-term cache and quality paths</name>
  <files>src/easy_a/rankings/cache.py, src/easy_a/quality/checks.py, tests/rankings/test_cache_parity.py, tests/refresh/test_rankings_cache_refresh.py</files>
  <read_first>src/easy_a/rankings/cache.py; src/easy_a/quality/checks.py; tests/rankings/test_cache_parity.py; tests/refresh/test_rankings_cache_refresh.py</read_first>
  <behavior>Refreshing all sections in term 202701 writes one ranking row per section, caller-owned transaction remains intact, and the full non-seat cache payload matches on-demand rank_section byte-for-byte with fixed as_of. The quality pass retains no_historical_analytics and low_confidence_ranking findings for global fallback rows; grade imports still cause a subsequent cache rebuild to change sourced stats.</behavior>
  <action>Write failing whole-term parity and query-budget tests, then replace the per-course analytics loops in refresh_section_rankings and _check_analytics with the new batch API. Preserve cache.py's local import to avoid circular imports, update-or-insert by section ID, flush without commit, and existing findings/severity/provenance. Keep live seats outside the ranking cache per D-03. Measure an actual full-term rebuild and whole-term quality pass against the Plan 01 baseline, record wall time, statement count and resulting cache count in the Plan 03 final evidence; if an unbatched instructor/signal/GenEd lookup becomes dominant, optimize only that measured path while preserving exact output.</action>
  <verify><automated>uv run pytest tests/rankings/test_cache_parity.py tests/refresh/test_rankings_cache_refresh.py tests/analytics/test_queries.py -q</automated></verify>
  <done>Full-term cache and quality operations use bounded grade reads, preserve transaction and findings semantics, and pass byte-for-byte parity across the specified evidence branches; measured build latency improves from baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Historical grade storage to derived ranking cache | Source selection, term/CRN/course attribution and evidence provenance must remain intact. |
| Caller transaction to cache writes | Refresh functions may flush but must not unexpectedly commit or erase prior cache on failure. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-04 | Tampering | batch analytics | high | mitigate | Exact batch-vs-public stats and byte-for-byte cache parity tests across source/fallback branches. |
| T-07-05 | Information disclosure | perf diagnostics | medium | mitigate | Report counts and timings only; keep raw grades and database URL out of logs and artifacts. |
| T-07-06 | Denial of service | whole-term rebuild | medium | mitigate | Bound grade reads to a whole-term batch and verify query count plus caller-owned transaction. |
</threat_model>

<verification>
Run targeted analytics/cache/refresh tests, then the full pytest suite and PostgreSQL integration path when EASY_A_TEST_POSTGRES_URL is configured. On the live term, reconcile SectionRankingCache count with stored sections and Phase 6 validator; compare elapsed build time and quality pass with Plan 01 baseline.
</verification>

<success_criteria>
The full-term cache build is materially faster and produces the same rankings and honest evidence labels; no score composition, API contract or grade source-selection rule changes.
</success_criteria>

## Artifacts this phase produces
- Private whole-term analytics batch interface in `queries.py` reused by cache and quality.
- Exact parity and bounded-query regression tests.

<output>
Create .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-02-SUMMARY.md when done.
</output>
