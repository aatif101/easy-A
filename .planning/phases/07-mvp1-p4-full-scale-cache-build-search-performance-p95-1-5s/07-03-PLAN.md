---
phase: 07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/easy_a/api/routes/rankings.py
  - tests/api/test_rankings_search_sql.py
  - migrations/versions/0004_rankings_search_indexes.py
  - .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md
  - .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-VALIDATION.md
autonomous: true
requirements: [REQ-PERF-01]
estimate:
  tokens: 19000
  raw_tokens: 19000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Locally served HTTP GET /api/v1/rankings/search over the full stored Tampa term measures p95 below 1.5 seconds while its API process uses hosted Supabase; direct route-call time is reported only as a diagnostic.
    - Every filter, sort, tie break, total, page boundary, latest-seat value and unavailable state has the same API contract as before.
    - Cached results remain byte-for-byte identical to on-demand rankings for course, subject, global and instructor evidence; D-20 global fallbacks remain unevidenced.
    - The final report includes dated dataset size, sanitized environment, deterministic HTTP query mix, warmup and at least 50 measured calls, HTTP and direct-call p50/p95/max separately, EXPLAIN before/after and cache-build latency before/after.
  artifacts:
    - src/easy_a/api/routes/rankings.py
    - tests/api/test_rankings_search_sql.py
    - .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md
  key_links:
    - Count and page queries share exact predicates; latest seats affect counts only when seats_open requires it and always affect seat sort and page hydration.
    - Any index migration is added only after plan and latency evidence and has a downgrade before hosted application.
---

<objective>
Reach and prove REQ-PERF-01 on the live full-scale Supabase term with the API and scoring contracts unchanged.
Purpose: Optimize the measured count/page/seat bottleneck, then verify loopback HTTP request plus response serialization/body-read latency against the hosted database rather than treating EXPLAIN or direct route-call time as the target.
Output: Evidence-driven query changes, regression tests, and a dated final performance report.
</objective>

<execution_context>
@C:/Users/smati/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/smati/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-RESEARCH.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PATTERNS.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tune the measured count and page bottleneck while preserving search behavior</name>
  <files>src/easy_a/api/routes/rankings.py, tests/api/test_rankings_search_sql.py</files>
  <read_first>src/easy_a/api/routes/rankings.py; tests/api/test_rankings_search_sql.py; .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md</read_first>
  <behavior>All supported broad and filtered queries retain exact totals, order and ties, page boundaries, literal filter escaping, GenEd EXISTS behavior, latest seat (observed_at DESC then id DESC), snapshot-to-section seat fallback and unavailable states. Count/page SQL stays parameterized and bounded; no score or response-field value changes.</behavior>
  <action>Use Plan 01 count/page EXPLAIN and route-stage timings to select the dominant serve path. Write failing contract cases for that path before changing SQL. If count dominates, build a narrow count selectable that shares predicates with page and omits wide cache payload; omit latest-seat join from count only when seats_open is false. If page or seat lookup dominates, page section IDs under the exact filter/order then hydrate only that page, or compare indexed per-section latest lookup against the existing window. Retain the latest-seat tie break, GenEd, supported sorts and fallback semantics. Keep SQLAlchemy bound expressions and FastAPI bounds. Adopt only a variant whose hosted plan and locally served HTTP request latency improve; record rejected variants. Direct-call latency remains a stage diagnostic. D-02 and D-03 prohibit score or seat-to-score changes.</action>
  <verify><automated>uv run pytest tests/api/test_rankings_search_sql.py tests/rankings/test_cache_parity.py -q</automated></verify>
  <done>Measured serve-path SQL is faster on the same live query mix and all API/parity regression cases pass.</done>
</task>

<task type="auto">
  <name>Evaluate only workload-proven indexes and close the live performance gate</name>
  <precondition>DATABASE_URL resolves to reachable hosted Supabase with full term 202701; a loopback uvicorn easy_a.api.app:app process uses that same DATABASE_URL; the executor must assert these privately and stop without a live-success claim if unavailable.</precondition>
  <files>migrations/versions/0004_rankings_search_indexes.py (conditional), .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md, .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-VALIDATION.md</files>
  <read_first>migrations/versions/0003_create_section_rankings.py; scripts/benchmark_rankings_search.py; scripts/validate_tampa_ingest.py; .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md; .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-VALIDATION.md</read_first>
  <action>Rerun identical representative count/page EXPLAIN and the direct --live diagnostic at full stored coverage. Start a loopback-only uvicorn API process connected to the same hosted Supabase database and rerun --live --http-base-url with the same deterministic query mix, warmup and at least 50 measured HTTP GETs spanning broad/filtered, seat/no-seat and sort variants. Time each GET through complete response-body consumption and JSON parse; require successful response and stored-term row-count reconciliation. If plan evidence shows a missing index, compare with a candidate index in an isolated PostgreSQL environment; create migrations/versions/0004_rankings_search_indexes.py with a reversible downgrade only when it improves hosted-equivalent HTTP p95 after its rebuild write cost is measured. Inspect Alembic head and do not duplicate 0003 indexes; if the head differs, choose the next valid revision filename and update files_modified accordingly. An additive, downgradable index is reversible; any migration that rewrites or drops stored data is one-way and requires a checkpoint:decision before execution. Run the HTTP benchmark again after final schema/code, all tests, quality and Phase 6 scale validator; stop the local server afterward. Report actual section/cache/grade counts, sanitized target, date, exact query mix, warmup/measured counts, HTTP and direct-call p50/p95/max separately by class and aggregate, EXPLAIN before/after, build-time before/after and residual risks. The REQ-PERF-01 pass/fail gate is the loopback HTTP p95, not the direct-call p95. If HTTP p95 remains at or above 1.5s, continue measured query or index tuning within this task and leave REQ-PERF-01 open; never claim success from direct-call, synthetic, local-database, deployment or browser results. Preserve D-06 evidence provenance and D-20 honest coverage.</action>
  <verify><automated>uv run pytest -q; uv run python scripts/benchmark_rankings_search.py --live --http-base-url http://127.0.0.1:8000 --term 202701 --iterations 50; uv run python scripts/validate_tampa_ingest.py --term 202701 --targets config/course_targets.toml</automated></verify>
  <done>07-PERF-REPORT.md shows locally served HTTP p95 below 1.5s over the full hosted term with at least 50 measured GETs, unchanged ranking/API parity, cache row reconciliation and acceptable measured rebuild latency; otherwise the plan remains incomplete with the exact measured gap recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| HTTP query to SQL | Filters and pagination are untrusted input; stay typed, bounded and bound as SQL parameters. |
| Live seat rows to search response | Latest snapshot order and section fallback must not silently change. |
| Schema change to hosted database | Any new index must be justified, reversible and checked in as an Alembic migration. |
| Environment to report | Database URL and raw grade data must not leave the runtime. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-07 | Tampering | count/page refactor | high | mitigate | Shared predicate builder and exhaustive sort/filter/pagination/seat tests; full cache parity gate. |
| T-07-08 | Denial of service | search and index migration | medium | mitigate | Preserve Query bounds; require measured plan benefit and account for rebuild/index-write cost. |
| T-07-09 | Information disclosure | EXPLAIN/report | high | mitigate | Publish sanitized target class and aggregate timings only, no connection URL or grade rows. |
| T-07-10 | Tampering | database migration | medium | mitigate | Additive index only with Alembic downgrade; gate any data-rewriting migration as one-way before execution. |
</threat_model>

<verification>
Run focused route and cache parity checks after each SQL variant, full pytest and quality gates at final candidate, PostgreSQL integration when configured, then the read-only hosted benchmark and Phase 6 scale validator. Update 07-VALIDATION.md with the final per-task evidence and measured status.
</verification>

<success_criteria>
Locally served HTTP search p95 is below 1.5 seconds with the API backed by hosted Supabase at stored full Tampa scale, documented with date, environment, warmup and query mix; score methodology and API response contract remain unchanged and cache-build latency is measured and improved.
</success_criteria>

## Artifacts this phase produces
- Search SQL regression cases for the chosen count/page/seat optimization.
- `benchmark_rankings_search.py --live --http-base-url`: final HTTP GET p95 gate; `--live` direct-call timings remain diagnostics.
- Dated `07-PERF-REPORT.md` and updated `07-VALIDATION.md` with live latency, build-time and parity evidence.
- Conditional reversible Alembic index migration only if live plan evidence proves it useful.

<output>
Create .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-03-SUMMARY.md when done.
</output>
