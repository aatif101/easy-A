---
phase: 01-baseline-scope-and-contracts
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - .planning/phases/01-baseline-scope-and-contracts/01-LAUNCH-COVERAGE-MANIFEST.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [REQ-LAUNCH-01, REQ-RMP-01, REQ-POLICY-01]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A published manifest names the institution, the campus, the supported registration term, the default registration term, the enumerated subjects and courses, the historical grade term coverage, the refresh cadence, and the exclusions (REQ-LAUNCH-01)."
    - "Spring 2027 is the only supported and default registration term, and historical terms are stated as non-selectable registration targets (D-02)."
    - "Every coverage figure in the manifest cites the inventory artifact, its observation timestamp and its denominator; the manifest quotes no number it cannot trace."
    - "Historical grade term coverage is stated as not yet possessed, naming the dependency that would settle it, rather than named with invented semesters."
    - "All offered Tampa sections remain in declared scope, including sections with no grades, no named instructor, no verified profile and no current syllabus (D-01, D-04)."
    - "Refresh cadence values are stated as configurable product targets, not as verified upstream rate limits."
    - "The Phase 3 exit criterion is restated to something the inventory evidence can actually prove, and the restatement is a recorded human decision, not a silent edit."
    - "OQ-02, OQ-03 and OQ-04 are recorded as resolved with their evidence, and OQ-05 is recorded as identified with owners."
  artifacts:
    - .planning/phases/01-baseline-scope-and-contracts/01-LAUNCH-COVERAGE-MANIFEST.md
  key_links:
    - "The manifest's section and course counts are the same numbers the schedule inventory JSON holds, cited rather than retyped from memory."
    - "The manifest's syllabus availability statement is the same finding line the syllabus inventory holds."
    - "The manifest's baseline reference links to 01-BASELINE.md rather than restating test counts."
    - "The restated Phase 3 exit criterion links to the named-instructor counts that make it provable."
    - "The supported registration term links to the existing Banner term validation module as the single validation path, not to a frontend constant."
  prohibitions:
    - "No semester name, coverage percentage or section count is written that does not trace to an inspected artifact."
    - "No percentage appears without its denominator."
    - "No historical term becomes a selectable registration target."
    - "No section is excluded from declared scope for lacking grades, an instructor name, a verified profile or a syllabus."
    - "ROADMAP.md is never rewritten whole; only scoped replacements are applied."
    - "No requirement is reassigned and no phase is added, removed or reordered."
    - "No cadence figure is described as a verified source rate limit."
    - "No source file, schema, migration, manifest or lockfile is modified."
---

<objective>
Publish the launch coverage manifest from the evidence the phase gathered, restate the Phase 3 exit
criterion against what that evidence can actually prove, and close the phase's open questions in the
project record.

Purpose: ROADMAP Phase 1 success criteria 1 and 4, and the phase exit gate. This is the plan where
observations become a declared scope, so it is also the plan where a fabricated coverage claim would
do the most damage.
Output: `01-LAUNCH-COVERAGE-MANIFEST.md`, a scoped ROADMAP edit, and a STATE update.

Every number here is cited from an artifact produced earlier in this phase. Nothing is recalled.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-baseline-scope-and-contracts/01-CONTEXT.md
@.planning/phases/01-baseline-scope-and-contracts/01-01-SUMMARY.md
@.planning/phases/01-baseline-scope-and-contracts/01-02-SUMMARY.md
@.planning/phases/01-baseline-scope-and-contracts/01-03-SUMMARY.md
@.planning/phases/01-baseline-scope-and-contracts/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the launch coverage manifest from cited evidence</name>
  <files>.planning/phases/01-baseline-scope-and-contracts/01-LAUNCH-COVERAGE-MANIFEST.md</files>
  <read_first>
    - .planning/phases/01-baseline-scope-and-contracts/01-INVENTORY-SCHEDULE-202701-TAMPA.md — the subject states, section counts, course counts and instructor counts, with their timestamps
    - .planning/phases/01-baseline-scope-and-contracts/01-INVENTORY-SYLLABUS-202701-TAMPA.md — the syllabus availability finding and its stated denominator
    - .planning/phases/01-baseline-scope-and-contracts/01-BASELINE.md — the baseline commit and test counts to reference rather than restate
    - .planning/phases/01-baseline-scope-and-contracts/01-DEPENDENCY-REGISTER.md — the dependency identifiers to cite where coverage is not yet possessed
    - .planning/REQUIREMENTS.md — REQ-LAUNCH-01's acceptance text, which enumerates what the manifest must name
    - .planning/phases/01-baseline-scope-and-contracts/01-CONTEXT.md — D-01 through D-04
    - src/easy_a/common/terms.py — the single validated Banner term path the supported-term contract points at
  </read_first>
  <action>
    Write 01-LAUNCH-COVERAGE-MANIFEST.md. Every figure is copied from an artifact and cited with the
    artifact name and its observation timestamp. If a figure is not in an artifact, it does not go
    in the manifest.

    Open with labelled declaration lines, each on its own line so they are individually checkable:
    `Institution and campus:`, `Supported registration terms:`, `Default registration term:`,
    `Declared scope subject count:`, `Declared scope course count:`, `Declared scope section count:`,
    `Historical grade term coverage:`, `Refresh cadence (configurable target):`, and `Exclusions:`.

    For the registration term, state the single supported Banner term and that it is also the
    default, per D-02. State that historical grade and source terms retain their own identities and
    are never selectable registration targets, and point at the existing Banner term validation
    module as the single place that validation belongs — not a frontend constant.

    For subjects, courses and sections, cite the schedule inventory: the subject list with each
    subject's state, the course count and the section count drawn from complete subjects only, and
    the count of subjects in each non-complete state. Where the inventory is not complete, say so in
    the same sentence as the number and describe the count as a floor over observed subjects rather
    than a campus total.

    For historical grade term coverage, state plainly that no real approved export has been supplied
    to the project and therefore no actual imported semester can be named. Cite the dependency
    identifier from the register that would settle it. Do not name a semester, a term count or a
    coverage percentage here. This is the single place in the phase most likely to attract an
    invented figure, and the product's core value is that it does not invent one.

    For evidence availability, report coverage separately for searchable sections, named-instructor
    assignment, current-term syllabus availability, grade evidence and verified profile links, each
    with its own denominator or an explicit not-yet-measured statement. Do not blend them into one
    coverage number. Grade evidence and verified profiles are not measurable in this phase; say so
    and name their owning phases.

    For cadence, state the proposed watched-section and remaining-section intervals and the
    freshness expiry as configurable product targets, and state explicitly that they are not
    verified upstream rate limits.

    For exclusions, state what is out of declared scope — the other campuses, terms other than the
    supported one — and restate D-01 and D-04: a section is not excluded for lacking grades, a named
    instructor, a verified profile or a syllabus; such a section is shown with an explicit
    unavailable state instead.

    Close with a sampling discipline section: name every figure that came from a sample rather than
    a full pass, with its sample size against its frame, and state that a sample is never reported
    as a census.
  </action>
  <verify>
    <automated>for k in 'Institution and campus:' 'Supported registration terms:' 'Default registration term:' 'Declared scope subject count:' 'Declared scope course count:' 'Declared scope section count:' 'Historical grade term coverage:' 'Refresh cadence (configurable target):' 'Exclusions:'; do grep -q "$k" .planning/phases/01-baseline-scope-and-contracts/01-LAUNCH-COVERAGE-MANIFEST.md || { echo "missing: $k"; exit 1; }; done && uv run python -c "import json;d=json.load(open('.planning/phases/01-baseline-scope-and-contracts/01-INVENTORY-SCHEDULE-202701-TAMPA.json'));t=open('.planning/phases/01-baseline-scope-and-contracts/01-LAUNCH-COVERAGE-MANIFEST.md',encoding='utf-8').read();n=d['totals']['section_count_from_complete_subjects'];c=d['totals']['course_count_from_complete_subjects'];assert str(n) in t,f'manifest must cite section count {n}';assert str(c) in t,f'manifest must cite course count {c}';print('manifest cites inventory totals')"</automated>
  </verify>
  <acceptance_criteria>
    - All nine labelled declaration lines are present, each on its own line.
    - The supported registration terms line names exactly one Banner term, and the default term line names the same one.
    - The section count and course count in the manifest are byte-identical to the totals in the schedule inventory JSON.
    - Every coverage figure cites the artifact it came from and that artifact's observation timestamp.
    - The historical grade term coverage line states that no export has been supplied and cites a dependency identifier; it names no semester and no percentage.
    - Evidence availability is reported separately for at least five categories, each with its own denominator or an explicit not-yet-measured statement.
    - The cadence line states that the intervals are configurable targets and not verified upstream rate limits.
    - The exclusions section names the out-of-scope campuses and terms, and restates that missing evidence does not exclude a section.
    - The sampling discipline section names every sampled figure with its sample size against its frame.
  </acceptance_criteria>
  <done>The declared launch scope is published, and every number in it can be traced back to an artifact and a timestamp rather than to a recollection.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Approve the restated Phase 3 exit criterion</name>
  <files>none</files>
  <read_first>
    - .planning/ROADMAP.md — the Phase 3 section, its current exit gate wording, and the carried-from-Phase-1 note that instructs this restatement
    - .planning/phases/01-baseline-scope-and-contracts/01-INVENTORY-SCHEDULE-202701-TAMPA.md — the named, staff and absent instructor counts that decide what is provable
    - .planning/phases/01-baseline-scope-and-contracts/01-INVENTORY-SYLLABUS-202701-TAMPA.md — the source ordering line
    - .planning/REQUIREMENTS.md — REQ-RMP-01 and REQ-POLICY-01, including the coverage-risk notes
  </read_first>
  <reversibility rating="costly">The restated criterion becomes the acceptance bar Phase 3 is planned and executed against. Loosening it wrongly ships an unverifiable claim; tightening it wrongly blocks a phase on evidence that cannot exist. Reversing it after Phase 3 is planned means replanning that phase.</reversibility>
  <decision>The exact replacement wording for the Phase 3 exit criterion, given the measured instructor and syllabus coverage</decision>
  <context>
    The roadmap's Phase 3 section carries an explicit instruction from Phase 1 to restate its exit
    criterion against actual coverage before Phase 3 is planned. The current wording requires
    demonstrating that a verified profile opens the correct professor and university page. Whether
    that is demonstrable depends on how many sections have a named instructor at all, which this
    phase has now counted.

    Present alongside this decision: the named-instructor section count against the observed section
    denominator, the staff-assigned and absent counts, the syllabus source-ordering line from the
    syllabus inventory, and the current exit gate wording verbatim.

    Two things are not on the table, and the developer should be told so explicitly: narrowing
    declared launch scope to the sections that happen to have verified profiles, and importing any
    profile content. Both are forbidden by the locked decisions, and neither is a way to make the
    criterion easier to pass.
  </context>
  <options>
    <option id="approve">
      <name>Approve the proposed replacement text as written</name>
      <pros>The criterion becomes provable against the coverage that actually exists while the identity-verification bar stays intact; Phase 3 can be planned immediately</pros>
      <cons>Commits Phase 3's acceptance bar to this wording; changing it later means replanning that phase</cons>
    </option>
    <option id="amend">
      <name>Approve with stated edits</name>
      <pros>Keeps the structure but corrects wording the developer reads differently; still unblocks Phase 3 planning</pros>
      <cons>The amended text must be recorded verbatim before it is applied, or the roadmap and the decision drift apart</cons>
    </option>
    <option id="replace">
      <name>Reject and state a different criterion</name>
      <pros>Appropriate if the proposed text weakens something the developer considers load-bearing, or if it is provable but not worth proving</pros>
      <cons>Needs the replacement written out in full at decision time; a criterion stated loosely here becomes a loose acceptance bar in Phase 8</cons>
    </option>
  </options>
  <action>
    Write the proposed replacement text out in full before asking — do not ask an open question. The
    proposal must keep the identity-verification bar intact and change only what is demonstrable: if
    named instructors are scarce, the criterion becomes provable on the named instructors that exist
    plus an explicit demonstration of the unavailable state across the remainder, rather than being
    weakened to whatever happens to match. It must also reorder the policy acceptance cases to match
    the syllabus source-ordering finding, making the historical-source path and the link-without-chip
    case primary if the evidence says so. Record the developer's answer and the final wording
    verbatim in the summary.
  </action>
  <resume-signal>Select: approve, amend or replace. For amend or replace, give the final wording in full — it is applied verbatim in task 3.</resume-signal>
  <acceptance_criteria>
    - The summary records the named, staff and absent instructor counts that were presented.
    - The summary records the proposed replacement text in full, as it was presented.
    - The summary records the developer's answer as approve, amend or replace, with the final wording verbatim.
    - The final wording preserves the identity-verification bar and does not narrow declared launch scope.
  </acceptance_criteria>
  <done>The Phase 3 exit criterion is restated by a recorded human decision against presented evidence, not adjusted quietly to fit whatever the inventory happened to find.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the scoped roadmap and state updates and close the phase gates</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md — the Phase 1 and Phase 3 sections, to locate the exact blocks being replaced
    - .planning/STATE.md — the Accumulated Context, Blockers and Session Continuity sections
    - .planning/PROJECT.md — the Open Questions table, for the exact OQ wording being resolved
    - .planning/phases/01-baseline-scope-and-contracts/01-DEPENDENCY-REGISTER.md — the identifiers OQ-05 now points at
  </read_first>
  <action>
    Apply the approved wording, then prove the phase did not drift.

    Edit ROADMAP.md with scoped replacements only. Replace the Phase 3 exit gate wording with the
    text approved in task 2, and replace the Phase 3 carried-from-Phase-1 note with a short statement
    that the restatement has happened, citing the inventory artifact. In the Phase 1 section, replace
    the open-questions line to record OQ-02, OQ-03 and OQ-04 as resolved with their evidence
    artifacts and OQ-05 as identified against the dependency register. Never pass the whole file to a
    write; use targeted replacements, and if a replacement is ambiguous, make several smaller ones.
    Do not add, remove or reorder any phase, and do not change any phase's requirement list.

    Update STATE.md: move OQ-02, OQ-03 and OQ-04 out of blockers into accumulated context with their
    resolving artifacts; restate OQ-05 as identified with the dependency register as its home; and
    update the current position and last-activity lines to reflect that Phase 1's contracts and
    inventory are complete. Keep the untracked-local-main warning exactly as it stands — it is still
    true and is not a Phase 1 outcome.

    Then run the phase closing gates and record their results in the summary: both test suites green
    at or above the recorded baseline counts, lint and type checks clean, no change to any model,
    migration, dependency manifest or lockfile since the baseline commit, and the roadmap still
    holding all eight phase headings after the edit.

    If any gate fails, stop and report it rather than adjusting the gate.
  </action>
  <verify>
    <automated>test "$(grep -c '^### Phase ' .planning/ROADMAP.md)" = "8" && test -z "$(git diff --name-only 06634490de5c765bdc7b55e4f439476b0e4fa0f7..HEAD -- src/easy_a/analytics src/easy_a/grades src/easy_a/models migrations pyproject.toml uv.lock web/package.json web/package-lock.json)" && uv run pytest -q && uv run ruff check . && uv run mypy src migrations scripts tests</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md still contains exactly eight `### Phase ` headings, and its Phases list still contains eight entries.
    - The Phase 3 exit gate wording matches the text approved in task 2, verbatim.
    - The Phase 1 open-questions line records OQ-02, OQ-03 and OQ-04 as resolved, each naming the artifact that resolved it, and OQ-05 as identified against the dependency register.
    - No phase was added, removed or reordered, and no phase's requirement list changed.
    - STATE.md no longer lists OQ-02, OQ-03 or OQ-04 as blockers, and still carries the untracked-local-main warning unchanged.
    - The git range check over analytics, grades, models, migrations and all four dependency manifests and lockfiles returns nothing.
    - `uv run pytest -q` reports at least the baseline Python count recorded in 01-BASELINE.md, `cd web && npm test` reports the recorded frontend count, and lint and type checks pass.
  </acceptance_criteria>
  <done>The roadmap and project state carry the phase's outcomes, the phase's closing gates are recorded as passing, and nothing outside the phase's declared scope changed.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces

**New document:** `.planning/phases/01-baseline-scope-and-contracts/01-LAUNCH-COVERAGE-MANIFEST.md`

**Labelled declaration lines the manifest publishes:**
`Institution and campus:`, `Supported registration terms:`, `Default registration term:`,
`Declared scope subject count:`, `Declared scope course count:`, `Declared scope section count:`,
`Historical grade term coverage:`, `Refresh cadence (configurable target):`, `Exclusions:`

**Scoped edits to existing files:**
- `.planning/ROADMAP.md` — the Phase 3 exit gate wording, the Phase 3 carried-from-Phase-1 note, and
  the Phase 1 open-questions line
- `.planning/STATE.md` — accumulated context, blockers and session continuity

**No code symbols, no CLI flags, no schema:** this plan creates no classes, functions, dataclass
fields, CLI flags, or migrations, and modifies no source file.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory artifact → published manifest | an observation becomes a declared coverage claim; a number that drifts crossing this boundary becomes a fabricated claim |
| manifest → registration behavior | the supported term declared here decides what a student can select; a historical term leaking in becomes a selectable target for a term nobody can register for |
| approved wording → roadmap file | a scoped edit becomes the acceptance bar for a later phase; a whole-file write here destroys phases outside the edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05-01 | Spoofing | manifest coverage figures | high | mitigate | Every figure cites its artifact and observation timestamp, and an automated check reads the inventory JSON and asserts the manifest contains the same section and course totals, so a drifted or recalled number fails the gate |
| T-01-05-02 | Spoofing | invented historical grade semesters or a coverage percentage | high | mitigate | The historical grade coverage line is required to state that no export has been supplied and to cite a dependency identifier; acceptance forbids naming a semester or a percentage on that line |
| T-01-05-03 | Elevation of Privilege | a historical term becoming a selectable registration target | high | mitigate | The manifest names exactly one supported and default Banner term, states that historical terms are never selectable, and points validation at the existing Banner term module rather than a frontend constant |
| T-01-05-04 | Tampering | declared scope narrowed to sections that happen to have evidence | high | mitigate | The exclusions section restates that missing grades, instructor, profile or syllabus never exclude a section; the decision checkpoint explicitly puts narrowing off the table; evidence availability is reported per category with separate denominators rather than as one blended number |
| T-01-05-05 | Tampering | a whole-file write destroying roadmap phases outside the edit | high | mitigate | The action requires scoped replacements only and forbids passing the whole file to a write; an automated gate asserts exactly eight phase headings survive the edit |
| T-01-05-06 | Repudiation | the exit criterion quietly loosened | high | mitigate | The restatement is a blocking human decision presented as concrete proposed text against shown counts, and the approved wording is recorded verbatim in the summary before it is applied |
| T-01-05-07 | Information Disclosure | committed manifest | low | accept | Contents are public schedule facts, counts and declared scope; no credential, no personal data, no source permission claim |
| T-01-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan adds no dependency, and its closing gate asserts that `pyproject.toml`, `uv.lock`, `web/package.json` and `web/package-lock.json` are all unchanged against the baseline commit for the whole phase |
</threat_model>

<verification>
- The manifest carries all nine labelled declaration lines.
- The manifest's section and course counts equal the schedule inventory JSON totals, asserted programmatically.
- ROADMAP.md holds exactly eight phase headings after the edit.
- `git diff --name-only 06634490de5c765bdc7b55e4f439476b0e4fa0f7..HEAD -- src/easy_a/analytics src/easy_a/grades src/easy_a/models migrations pyproject.toml uv.lock web/package.json web/package-lock.json` returns nothing.
- `uv run pytest -q` at or above the recorded baseline count; `cd web && npm test` at the recorded frontend count.
- `uv run ruff check .` and `uv run mypy src migrations scripts tests` pass.
</verification>

<success_criteria>
ROADMAP Phase 1 success criterion 1 is satisfied: the published manifest names the campus, the
supported and default registration terms, the enumerated subjects and courses, the historical grade
coverage position, the cadence and the exclusions, with no invented semester and no coverage
percentage lacking a denominator, and with historical terms excluded from registration selection.

The second half of criterion 4 is satisfied: the Phase 3 exit criterion is restated to match what
the inventory evidence supports, by a recorded decision.

The phase exit gate is met: baseline checks recorded, every requirement mapped, source gaps and
owners listed, formulas reproducible, UI screens and failure states specified.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-scope-and-contracts/01-05-SUMMARY.md` when done.
</output>
