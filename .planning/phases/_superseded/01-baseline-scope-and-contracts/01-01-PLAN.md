---
phase: 01-baseline-scope-and-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-baseline-scope-and-contracts/01-BASELINE.md
  - .planning/phases/01-baseline-scope-and-contracts/01-METHOD-V2-CONTRACT.md
autonomous: true
requirements: [REQ-LAUNCH-01, REQ-EVID-01, REQ-EVID-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reader can run two named commands and get exactly the test counts 01-BASELINE.md records."
    - "01-BASELINE.md names the baseline commit, the branch/worktree strategy, and the disposition of the untracked web/ directory on local main (D-05)."
    - "The recorded baseline is stated as a SQLite-only baseline, so a later PostgreSQL failure is not mistaken for a regression against it."
    - "A reader can reproduce method v2's worked example from 01-METHOD-V2-CONTRACT.md alone, without opening any source file."
    - "RULE-20, RULE-60A, RULE-60B, RULE-60C and RULE-K60 are each stated separately, each with its own distinct test intent."
    - "The five metric states (available, insufficient, unavailable, suppressed, invalid) are each defined with their own reason, not as one boolean."
    - "The blank-cell/suppression gap and the effective_n equivalence gap are each recorded with the external input required, a named owner role, and a blocked phase."
  artifacts:
    - .planning/phases/01-baseline-scope-and-contracts/01-BASELINE.md
    - .planning/phases/01-baseline-scope-and-contracts/01-METHOD-V2-CONTRACT.md
  key_links:
    - "Each rule statement in the method contract links to the file:line of the code that implements it, partially implements it, or does not implement it."
    - "The recorded test counts link to the exact commands that reproduce them, so the number is re-derivable rather than quoted."
    - "The blank-cell gap links src/easy_a/grades/parser.py to REQ-DATA-01 and to the phase that owns the fix."
    - "The effective_n gap links src/easy_a/analytics/confidence.py to src/easy_a/analytics/grades.py and to the RecencyConfig flag the equivalence depends on."
  prohibitions:
    - "No source file under src/easy_a/analytics, src/easy_a/grades, src/easy_a/models or migrations is modified by this plan."
    - "No new Alembic revision is created."
    - "No measured figure is adjusted to match a document; a mismatch is reported as a finding."
    - "No claim that the recorded baseline demonstrates PostgreSQL compatibility."
    - "No hard-coded reference mean is presented as an acceptable value for mu."
    - "Local main is not checked out, merged, or fast-forwarded."
---

<objective>
Record the confirmed implementation baseline, and write the method v2 contract so that method v2
is reproducible from text alone.

Purpose: ROADMAP Phase 1 success criteria 2 and 3. Every later phase measures regression against
this baseline (D-ADR-16) and implements grades against this contract (D-ADR-02, D-ADR-03,
D-ADR-12). Both are documents; neither changes behavior.
Output: `01-BASELINE.md` and `01-METHOD-V2-CONTRACT.md` in the phase directory.

This plan is documentation only. The phase's end-to-end tracer lives in plan 01-02 task 1, which
is the only place Phase 1 touches running code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-baseline-scope-and-contracts/01-RESEARCH.md
@.planning/phases/01-baseline-scope-and-contracts/01-CONTEXT.md
@.planning/phases/01-baseline-scope-and-contracts/01-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the confirmed implementation baseline</name>
  <files>.planning/phases/01-baseline-scope-and-contracts/01-BASELINE.md</files>
  <read_first>
    - .planning/phases/01-baseline-scope-and-contracts/01-RESEARCH.md — the "Confirmed Baseline" section carries the measured figures and the exact commands that produced them
    - .planning/phases/01-baseline-scope-and-contracts/01-VALIDATION.md — the measured test-infrastructure table and the SQLite-only caveat
    - .planning/PROJECT.md — Constraints block and the OQ-01 / OQ-04 rows
    - AGENTS.md — the hard constraint on local main
    - tests/conftest.py — the in-memory SQLite engine construction that scopes the baseline
  </read_first>
  <precondition>web/node_modules is present; it is not committed, so run npm install inside web/ once before the frontend suite if the directory is absent.</precondition>
  <action>
    Re-measure, then record. Run the Python suite and the frontend suite and capture their exact
    final summary lines. Re-verify the git baseline: fetch origin main, read its current commit,
    and confirm the current worktree is a descendant of it. Do not check out, merge, or
    fast-forward local main (D-05, AGENTS.md hard constraint).

    Write 01-BASELINE.md containing, each as its own labelled section: the baseline commit
    06634490de5c765bdc7b55e4f439476b0e4fa0f7 and the evidence that the current worktree descends
    from it; the branch and worktree strategy per D-05, including that local main stays at its
    current commit with its untracked web directory intact until developer 1 merges; the measured
    Python test count and the measured frontend test count, each printed next to the exact command
    that reproduces it; and the dialect limitation — every Python test builds an in-memory SQLite
    engine in tests/conftest.py while the deployment target is PostgreSQL 16, so this is a
    SQLite-only baseline and real PostgreSQL coverage is net-new Phase 7 work under D-ADR-16.

    Close with a fenced block carrying machine-readable keys python_tests_passed,
    frontend_tests_passed, baseline_commit, worktree_head and measured_at, so a later phase can
    diff a number instead of re-reading prose.

    State that this resolves OQ-04 and records the already-resolved OQ-01, citing the measurement
    rather than choosing between the two previously conflicting document figures (D-06).

    If either measured count differs from the figure recorded in 01-RESEARCH.md, stop and report
    the discrepancy as a finding in the summary. D-ADR-16 locks the passing baseline; do not edit
    the expectation to match a new observation without surfacing it.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | grep -q "166 passed" && grep -q "python_tests_passed: 166" .planning/phases/01-baseline-scope-and-contracts/01-BASELINE.md && grep -q "frontend_tests_passed: 19" .planning/phases/01-baseline-scope-and-contracts/01-BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - 01-BASELINE.md contains the string 06634490de5c765bdc7b55e4f439476b0e4fa0f7.
    - The document prints both reproducing commands verbatim, and each measured count appears adjacent to the command that produced it.
    - The document states the SQLite-only limitation, names tests/conftest.py as its cause, and names Phase 7 as its owner.
    - The document states that local main must not be checked out, merged, or fast-forwarded.
    - The fenced machine-readable block contains all five keys: python_tests_passed, frontend_tests_passed, baseline_commit, worktree_head, measured_at.
    - `cd web && npm test` reports 19 passing tests, and that number matches frontend_tests_passed.
  </acceptance_criteria>
  <done>The baseline is a re-derivable record, not a quoted figure: a reader runs two commands and gets the two numbers the document states.</done>
</task>

<task type="auto">
  <name>Task 2: Write the method v2 reproducibility contract</name>
  <files>.planning/phases/01-baseline-scope-and-contracts/01-METHOD-V2-CONTRACT.md</files>
  <read_first>
    - .planning/phases/01-baseline-scope-and-contracts/01-RESEARCH.md — the "Method v2 Reproducibility Contract" section already assembles the formula, every rule, and each rule's current-code status; restate it as a locked contract rather than re-deriving it (D-06)
    - .planning/REQUIREMENTS.md — the "Threshold rules" table, the reference cohort chain, and REQ-GRADE-01 through REQ-GRADE-04, REQ-EVID-01, REQ-EVID-02
    - src/easy_a/analytics/scoring.py — the existing prior-strength and global-prior constants and the existing instructor-course evidence gate
    - src/easy_a/analytics/confidence.py — the existing label boundary function
    - src/easy_a/analytics/grades.py — how the effective sample size is derived and where term weights come from
  </read_first>
  <action>
    Write 01-METHOD-V2-CONTRACT.md so a reader with no access to this codebase can reproduce every
    number it describes.

    State the three formula lines (grade favorability, the shrinkage adjustment, and the 0-10
    presentation scale) with every symbol defined, including the exact composition of N and the
    exact composition of T. State the prior strength and the reference mean as two separate
    entries: the prior strength is a formula coefficient with a locked default; the reference mean
    is a measured value derived from eligible imported history, and the contract must state plainly
    that no fixed population constant may stand in for it (D-ADR-02, D-ADR-03).

    Give each of RULE-20, RULE-60A, RULE-60B, RULE-60C and RULE-K60 its own third-level section
    beginning `### RULE-`. Each section carries four labelled parts: the normative statement; its
    kind (score-eligibility gate, cohort-selection gate, reference-eligibility gate, display label,
    or formula coefficient); its owning requirement; and a line beginning `Test intent:` stating
    what a test of this rule must exercise and, explicitly, what it does not cover. Add a sentence
    to each recording whether current code implements it, partially implements it, or does not
    implement it, citing file and line range. Five rules share the numbers 20 and 60; the contract
    exists so nobody collapses them.

    State the reference cohort chain and mark plainly that it supplies the reference mean only —
    no subject-level or corpus-level value may become a displayed score.

    Define the five metric states as five separate entries — available, insufficient, unavailable,
    suppressed, invalid — each with its own reason string and its own trigger condition, and record
    that source suppression takes precedence over the display-score floor. This vocabulary is
    canonical for the phase; plan 01-04's state matrix cites it rather than restating it.

    Include the worked example under a heading that marks it synthetic. Show A, B, C, D, F and W
    inputs, then N, then the grade favorability value, then the adjusted value, then the displayed
    0-10 value, as five separately printed intermediate results — a reader must be able to check
    their arithmetic at each step, not just at the end. Add the isolation rule: changing only the
    withdrawal count must leave the displayed grade ease unchanged (D-ADR-05).
  </action>
  <verify>
    <automated>test "$(grep -c '^### RULE-' .planning/phases/01-baseline-scope-and-contracts/01-METHOD-V2-CONTRACT.md)" = "5" && test "$(grep -c 'Test intent:' .planning/phases/01-baseline-scope-and-contracts/01-METHOD-V2-CONTRACT.md)" -ge 5 && grep -q '7.234375' .planning/phases/01-baseline-scope-and-contracts/01-METHOD-V2-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly five sections begin with `### RULE-`, one each for RULE-20, RULE-60A, RULE-60B, RULE-60C, RULE-K60.
    - At least five lines begin with `Test intent:`, and no rule's test intent is written as covering another rule.
    - The worked example prints N, the favorability value, the adjusted value and the displayed value as four distinct stated numbers, and 7.234375 appears among them.
    - The worked example is labelled synthetic in its own heading.
    - The document names all five metric states, each with its own reason string.
    - Each of the five rule sections cites a file and line range for its current-code status.
    - The reference cohort chain section states that it supplies the reference mean only.
  </acceptance_criteria>
  <done>Method v2 is reproducible from this document alone; the five threshold rules are five documents-worth of separate statements, not one number mentioned five times.</done>
</task>

<task type="auto">
  <name>Task 3: Record the two confirmed code gaps and gate that no source changed</name>
  <files>.planning/phases/01-baseline-scope-and-contracts/01-METHOD-V2-CONTRACT.md</files>
  <read_first>
    - src/easy_a/grades/parser.py — the cell-to-integer conversion, the empty-cell predicate, the structured row/workbook validation error types, and the check that the summed buckets equal the source's own total
    - src/easy_a/analytics/confidence.py — the label boundary constants and function
    - src/easy_a/analytics/grades.py — the effective sample size derivation and the uniform-weight branch taken when recency weighting is disabled
    - .planning/phases/01-baseline-scope-and-contracts/01-RESEARCH.md — the "Source Suppression and Blank-Cell Semantics" section and pitfalls 2 and 3
    - .planning/phases/01-baseline-scope-and-contracts/01-PATTERNS.md — the two self-analog documentation targets and their exact cited line ranges
  </read_first>
  <action>
    Append two gap sections to 01-METHOD-V2-CONTRACT.md. Phase 1 documents both; Phase 2 owns both
    fixes. Change neither source file.

    First section: the blank-cell and suppression semantics of the grade parser. Record exactly what
    is confirmed by reading the code — an empty cell converts to the integer zero, and a non-empty
    cell that cannot be read as a number rejects the whole row through the structured validation
    error type — citing the file and line ranges. Then record exactly what is not confirmed: whether
    real InfoCenter exports ever use a blank cell to mean a suppressed small count rather than a
    genuine zero. State that this is a data question, not a code question. Name the external input
    required (one real or representative export file), name the owner as the holder of registrar and
    ODS download access, and name the blocked phase as 2, with real-data confirmation in 7. Record
    the conflict against REQ-DATA-01 in one sentence: an unconditional blank-to-zero conversion
    contradicts the rule that a blank source cell must not silently become zero.

    Second section: the effective sample size equivalence. Record that the label boundaries operate
    on an effective sample size which reduces exactly to the A-F outcome count only while term
    recency weighting is disabled, and that this holds today because the uniform-weight branch is
    taken under the locked default. Cite the deriving lines in both files. State the consequence: if
    that flag is ever enabled the equivalence breaks silently and every label can change without any
    label code changing. State the Phase 2 obligation as two acceptable resolutions — an explicit
    regression test locking the equivalence under the default configuration, or a distinctly named
    A-F outcome count field that the rules read instead — and note that the phase must pick one, not
    inherit the coincidence.

    Then run the protected-path gate below and record its result in the document. If any protected
    path shows a change, stop and report it: this phase must not modify grade, analytics, model or
    migration code.
  </action>
  <verify>
    <automated>test -z "$(git diff --name-only 06634490de5c765bdc7b55e4f439476b0e4fa0f7..HEAD -- src/easy_a/analytics src/easy_a/grades src/easy_a/models migrations)" && grep -q 'parser.py' .planning/phases/01-baseline-scope-and-contracts/01-METHOD-V2-CONTRACT.md && grep -q 'confidence.py' .planning/phases/01-baseline-scope-and-contracts/01-METHOD-V2-CONTRACT.md && uv run pytest -q 2>&1 | grep -q "166 passed"</automated>
  </verify>
  <acceptance_criteria>
    - Both gap sections exist, each with its own heading.
    - Each gap section names the required external input, an owner role, and a blocked phase number.
    - Each gap section cites at least one file and line range from the source it describes.
    - The blank-cell section names the requirement it conflicts with.
    - The effective sample size section names the configuration flag the equivalence depends on and states both acceptable Phase 2 resolutions.
    - The git range check over src/easy_a/analytics, src/easy_a/grades, src/easy_a/models and migrations returns no filenames.
    - The Python suite still reports the same passing count as task 1 recorded.
  </acceptance_criteria>
  <done>Both gaps are written down with an owner and a blocked phase, and the protected-path gate proves Phase 1 documented them rather than quietly fixing them.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this plan produces

**New files:**
- `.planning/phases/01-baseline-scope-and-contracts/01-BASELINE.md`
- `.planning/phases/01-baseline-scope-and-contracts/01-METHOD-V2-CONTRACT.md`

**Named sections other plans and phases depend on:**
- `01-BASELINE.md` fenced keys: `python_tests_passed`, `frontend_tests_passed`, `baseline_commit`, `worktree_head`, `measured_at`
- `01-METHOD-V2-CONTRACT.md` headings: `### RULE-20`, `### RULE-60A`, `### RULE-60B`, `### RULE-60C`, `### RULE-K60`
- `01-METHOD-V2-CONTRACT.md` canonical metric-state vocabulary: `available`, `insufficient`, `unavailable`, `suppressed`, `invalid` — cited by plan 01-04's state matrix
- `01-METHOD-V2-CONTRACT.md` gap sections for the grade parser blank-cell semantics and the effective sample size equivalence

**No code symbols, no CLI flags, no schema:** this plan creates no classes, functions, dataclass
fields, CLI flags, or migrations.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning document → later-phase implementer | A figure written here is consumed as fact by phases 2 through 8; an unverified number crossing this boundary becomes a fabricated baseline |
| source code → contract prose | Every code-status claim in the contract is a restatement of code that can drift from it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-01 | Repudiation | 01-BASELINE.md test counts | medium | mitigate | Every count is printed next to the exact command that reproduces it, plus a commit SHA and a measurement timestamp, so any reader can re-derive it; task 1's automated verify re-runs the suite and compares against the recorded number rather than trusting it |
| T-01-01-02 | Spoofing | SQLite-only baseline presented as deployment-dialect coverage | high | mitigate | The document states the dialect limitation as its own section, names tests/conftest.py as the cause, and assigns real PostgreSQL coverage to Phase 7 under D-ADR-16; acceptance requires that section to exist |
| T-01-01-03 | Tampering | source edited to match a contract claim | high | mitigate | Task 3's gate runs a git range diff over src/easy_a/analytics, src/easy_a/grades, src/easy_a/models and migrations and requires an empty result, so a documented gap cannot be silently closed by editing code |
| T-01-01-04 | Information Disclosure | committed documents | low | accept | Content is repository metadata, formulas and public test counts; no credential, personal data, or source permission claim is recorded |
| T-01-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan adds no dependency. The only package-manager invocation is restoring web/node_modules from the committed package-lock.json, which introduces no new package, so no entry is added to the package legitimacy audit; the acceptance gate for the phase (plan 01-05) requires pyproject.toml, uv.lock, web/package.json and web/package-lock.json to be unchanged across Phase 1 |
</threat_model>

<verification>
- `uv run pytest -q` reports the same passing count 01-BASELINE.md records.
- `cd web && npm test` reports the same passing count 01-BASELINE.md records.
- `uv run ruff check .` and `uv run mypy src migrations scripts tests` pass unchanged — this plan touches no Python.
- `git diff --name-only 06634490de5c765bdc7b55e4f439476b0e4fa0f7..HEAD -- src/easy_a/analytics src/easy_a/grades src/easy_a/models migrations` returns nothing.
- Both documents exist in the phase directory and are committed.
</verification>

<success_criteria>
ROADMAP Phase 1 success criterion 2 is satisfied: the baseline commit, branch strategy, untracked
web disposition and exact test counts are recorded and re-derivable, replacing both previously
conflicting figures.

ROADMAP Phase 1 success criterion 3 is satisfied: a reader reproduces method v2 from the contract
alone — formula, prior strength, reference mean, five separately stated threshold rules each with
its own test intent, and the confirmed suppression and blank-cell behavior.
</success_criteria>

<output>
Create `.planning/phases/01-baseline-scope-and-contracts/01-01-SUMMARY.md` when done.
</output>
