---
phase: 08-mvp1-p5-end-to-end-mvp-1-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_rankings_pages.py
  - tests/api/test_verify_rankings_pages.py
autonomous: true
requirements: [REQ-COVERAGE-03]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every stored Tampa 202701 term-plus-CRN identity appears exactly once across the complete GET /api/v1/rankings/search page walk, and the API identity set equals the stored Section identity set (no missing, extra or duplicated identity)."
    - "A matching total alone cannot PASS: a missing, substituted or duplicated CRN, a changed total between pages, or a malformed page makes scripts/verify_rankings_pages.py exit 1 with a named reason."
    - "Adjacency: at an exact page-size multiple (200 rows) the walk ends after the full page with no empty-page overlap, and at page-size+1 (201 rows) the second page holds exactly one identity; adjacent pages neither overlap nor leave a gap."
    - "Empty: a term with zero stored sections yields total=0, no items, stored_count=0 and an identity verdict of PASS for that term, while the 202701 coverage gate in 08-04 still requires stored_count greater than zero."
    - "Ordering: sort=course orders by (subject, course_number, crn); CRN is unique within a term (uq_sections_term_crn, uq_section_rankings_section_id), so the order is total and the scanner fails with reason order_violation on any non-increasing key."
    - "Searchability is counted for every stored section regardless of evidence state; the scanner's api_score_source_split reports course/instructor_course/subject/global and effective_n=0 counts but never treats any of them as grade coverage (D-20, D-21)."
    - "No ranking calculation, cache row or API response field changes in this plan (D-02); the scanner is read-only and loopback-only."
  artifacts:
    - scripts/verify_rankings_pages.py
    - tests/api/test_verify_rankings_pages.py
  key_links:
    - "Read-only hosted Section/SectionRankingCache identity query (term 202701) is compared with validated RankingsSearchResponse pages fetched from the loopback API that uses the same DATABASE_URL."
    - "The scanner's JSON verdict (gate api_identity) feeds the REQ-COVERAGE-03 row of 08-VERIFICATION-REPORT.md in 08-04; its api_score_source_split is cross-checked there against the 08-02 inventory's cache split."
  prohibitions:
    - requirement_id: REQ-COVERAGE-03
      category: transparency
      statement: "MUST NOT report full-term searchability from a count match alone; PASS requires exact identity-set equality over every page."
      status: resolved
      verification: test
    - requirement_id: REQ-COVERAGE-03
      category: values
      statement: "MUST NOT change the scoring model, the cached rankings or the RankingsSearchResponse contract to make the identity scan pass (D-02)."
      status: resolved
      verification: test
---

<objective>
Prove that every stored Tampa Spring 2027 section can be found through the complete rankings search API, not only through a matching aggregate count.

Purpose: REQ-COVERAGE-03 requires all ~3,782 Tampa sections to be searchable with target, stored and API counts reconciled. The existing benchmark checks only a first-page total; this plan adds the set-based identity proof that 08-04 records.
Output: a read-only, loopback-only full-page identity scanner and focused regression tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-RESEARCH.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-PATTERNS.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md
@scripts/benchmark_rankings_search.py
@src/easy_a/api/routes/rankings.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace stored sections through real HTTP search pages to an identity verdict</name>
  <precondition>DATABASE_URL resolves privately to reachable hosted Supabase holding term 202701, and a loopback server started with `uv run uvicorn easy_a.api.app:app --host 127.0.0.1 --port 8000 --no-access-log` uses that same DATABASE_URL; if either is unavailable the live command must exit 3 (NOT MEASURED) and the plan relies on the unit tests only.</precondition>
  <files>scripts/verify_rankings_pages.py, tests/api/test_verify_rankings_pages.py</files>
  <read_first>scripts/benchmark_rankings_search.py (loopback validation `_http_base_url`, `_http_search`, `_run_live` read-only session with SET TRANSACTION READ ONLY, `_environment_label` redaction); scripts/validate_tampa_ingest.py (build_parser/main shape, term normalization); src/easy_a/api/routes/rankings.py (search_rankings limit 1..200, `_search_order` course tiebreak); src/easy_a/models/sections.py (uq_sections_term_crn); src/easy_a/common/campus.py (same_campus); tests/api/test_benchmark_rankings_search.py (importlib loading of a script, httpx.MockTransport); tests/api/test_rankings_api.py (api_session_factory, TestClient with get_db_session override, refresh_section_rankings seeding)</read_first>
  <behavior>
    - A seeded SQLite DB with 3 Tampa sections and their cache rows, served through FastAPI TestClient with get_db_session overridden, scanned with page size 2, yields verdict PASS, stored_count=3, api_total=3, pages_fetched=2, missing_count=0, extra_count=0, duplicate_count=0.
    - A MockTransport page set that omits one stored CRN yields verdict FAIL with reason identity_mismatch and that CRN in sample_missing.
    - A connection error (httpx.ConnectError from the transport) yields exit code 3 and verdict NOT MEASURED.
  </behavior>
  <action>Create scripts/verify_rankings_pages.py as a read-only CLI with `build_parser()` and `main(argv) -> int`, flags `--term` (default 202701, normalized with normalize_banner_term_code), `--http-base-url` (required; reuse the benchmark's plain-loopback-origin validation semantics so non-loopback hosts, credentials, paths and queries are rejected) and `--page-size` (integer 1..200, default 200, matching the route's `limit` bound). Implement dataclasses `StoredSnapshot` (identity set of (term, crn), cache_count, non_tampa_count), `PageScan` (ordered identities, per-page metadata, api_total, score-source tallies) and `IdentityVerdict`, plus functions `stored_identities(session, term)`, `scan_pages(client, base_url, term, page_size)` and `reconcile(stored, scan)`. Read stored identities in one session: on PostgreSQL execute SET TRANSACTION READ ONLY first, as `_run_live` does; count non-Tampa stored sections with same_campus and report them rather than filtering them out. Page through `/api/v1/rankings/search` with `sort=course`, `limit=page_size`, increasing `offset`, validating each body with RankingsSearchResponse.model_validate. Print exactly one JSON object to stdout with keys gate ("api_identity"), verdict (PASS, FAIL or NOT MEASURED), reason, term, observed_at_utc, environment (sanitized like `_environment_label`: dialect and host class only, never the connection string, credentials or host name), stored_count, cache_count, api_total, pages_fetched, page_size, missing_count, extra_count, duplicate_count, non_tampa_count, order_violations, sample_missing, sample_extra, sample_duplicates (each capped at 20 CRNs), and api_score_source_split (counts keyed by score_source with an effective_n_zero sub-count). PASS requires stored_count == cache_count == api_total, non_tampa_count == 0, and exact identity-set equality. Exit 0 on PASS, 1 on FAIL, 3 on NOT MEASURED (DB or HTTP unreachable). The split is reporting only: searchability counts every section, and no score_source or effective_n value is treated as grade coverage (D-20, D-21). Do not modify any ranking calculation, cache row or response field (D-02). Close the httpx client and dispose any engine the CLI creates. Write the three behavior tests first, loading the script via importlib as tests/api/test_benchmark_rankings_search.py does; then run the live command against the loopback server and stop the server afterwards.</action>
  <verify>
    <automated>uv run pytest tests/api/test_verify_rankings_pages.py -q</automated>
    <fails_when>non-zero exit, "no tests ran" in the summary, or any of the PASS/omitted-CRN/connection-error behavior tests reports failed</fails_when>
    <automated>uv run python scripts/verify_rankings_pages.py --term 202701 --http-base-url http://127.0.0.1:8000</automated>
    <fails_when>exit code 1 with "verdict": "FAIL" in stdout (an omission, extra, duplicate, count disagreement or non-Tampa row); exit code 3 with "verdict": "NOT MEASURED" means the live path was not proven and must be recorded as such, not as PASS</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/api/test_verify_rankings_pages.py -q` exits 0 with at least 3 passed.
    - `uv run python scripts/verify_rankings_pages.py --help` prints the flags `--term`, `--http-base-url` and `--page-size`.
    - The live run prints one JSON object containing the keys "gate", "verdict", "stored_count", "api_total", "missing_count", "duplicate_count", "extra_count" and "api_score_source_split"; the SUMMARY records its verdict, counts and UTC time verbatim.
    - `uv run python scripts/verify_rankings_pages.py --term 202701 --http-base-url http://example.com:8000` exits non-zero before any network request (non-loopback target rejected).
    - The JSON output contains no substring "postgresql://", "password" or "pooler.supabase.com".
  </acceptance_criteria>
  <done>A real 202701 page walk from hosted Supabase through the loopback API ends in a set-based PASS, FAIL or NOT MEASURED identity verdict with exact counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close page-boundary, empty-term, ordering and unstable-snapshot cases</name>
  <files>scripts/verify_rankings_pages.py, tests/api/test_verify_rankings_pages.py</files>
  <read_first>scripts/verify_rankings_pages.py; tests/api/test_verify_rankings_pages.py; src/easy_a/api/routes/rankings.py (`_search_order` course tiebreak); tests/api/test_rankings_search_sql.py (existing sort assertions)</read_first>
  <behavior>
    - Exactly 200 stored rows with page size 200: one full page then termination; PASS with pages_fetched=1.
    - 201 stored rows with page size 200: page 2 holds exactly 1 identity; PASS with pages_fetched=2.
    - A short non-final page, a CRN repeated across adjacent pages, a substituted CRN (same count, different identity), and a total that changes between pages each give FAIL with reasons short_page, duplicate_identity, identity_mismatch and snapshot_changed respectively.
    - A page whose items violate (subject, course_number, crn) ascending order gives FAIL with reason order_violation.
    - HTTP 500 and a malformed JSON body each give FAIL with reasons http_status and invalid_response; a stored non-Tampa section gives FAIL with reason non_tampa_section.
    - An empty term (0 stored, API total 0, no items) gives PASS with stored_count=0 and pages_fetched=1.
  </behavior>
  <action>Add the behavior cases above, using httpx.MockTransport page fixtures for HTTP-shape cases and the seeded SQLite DB for stored-side cases. Bound the walk: stop after ceil(first_total / page_size) pages, or 1 page when the total is 0; fail with reason page_overrun if a further non-empty page is observed. Check every page's total against the first page's total and fail snapshot_changed on any difference, printing a retry instruction in reason. Never report a partial scan as PASS. Check ordering on the concatenated identities using the route's course tiebreak (subject, course_number, crn); the order is already total because uq_sections_term_crn makes CRN unique within a term, so do not modify src/easy_a/api/routes/rankings.py. If the ordering test disproves that, record it as a FAIL finding in the SUMMARY instead of changing the route (D-02, API contract unchanged). Document the page-size bound, sort=course ordering assumption and exit codes (0 PASS, 1 FAIL, 3 NOT MEASURED) in the argparse description.</action>
  <verify>
    <automated>uv run pytest tests/api/test_verify_rankings_pages.py tests/api/test_rankings_search_sql.py -q</automated>
    <fails_when>non-zero exit, or any boundary, empty-term, ordering, snapshot or existing search-sort test reports failed</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/api/test_verify_rankings_pages.py contains test cases whose asserted reasons include each of: short_page, duplicate_identity, identity_mismatch, snapshot_changed, order_violation, http_status, invalid_response, non_tampa_section, page_overrun.
    - Tests exist for exactly 200 rows (pages_fetched=1) and 201 rows (pages_fetched=2) at page size 200, and for an empty term (stored_count=0, PASS).
    - `git diff --quiet origin/main -- src/easy_a/api src/easy_a/rankings src/easy_a/analytics` exits 0 after this task.
    - `uv run python scripts/verify_rankings_pages.py --help` output mentions "sort=course" and the exit codes 0, 1 and 3.
    - `uv run ruff check scripts/verify_rankings_pages.py tests/api/test_verify_rankings_pages.py` and `uv run mypy scripts/verify_rankings_pages.py` each exit 0 (repo-wide ruff/mypy already fail on untouched files, so the gate is scoped to this plan's files).
  </acceptance_criteria>
  <done>Full-term searchability is measured by exact identities with every boundary, empty and unstable case failing loudly rather than passing on a matching total.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI arguments to DB and HTTP | term, page size and base URL are bounded inputs; SQL uses bound parameters; HTTP is loopback-only. |
| Hosted DB and HTTP response to verdict | Complete identity sets are compared; unstable or partial snapshots never PASS. |
| Runtime environment to stdout | DB URLs and credentials must not leave the process. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01 | Tampering | identity verdict in reconcile() | high | mitigate | Set equality, uniqueness, stable total, page-boundary and order checks are all mandatory before PASS; each failure has a named reason covered by a test. |
| T-08-02 | Denial of service | scan_pages() | medium | mitigate | page_size capped at the route's 200 limit, page count bounded by ceil(total/page_size), one normalized term per run. |
| T-08-03 | Information disclosure | JSON stdout | high | mitigate | Sanitized environment label only; acceptance criterion checks stdout for URL/password/host substrings; no grade rows are read. |
| T-08-13 | Spoofing | --http-base-url | medium | mitigate | Reuse the benchmark's plain-loopback-origin validation; non-loopback targets are rejected before any request (acceptance criterion). |
</threat_model>

<verification>
Run the focused scanner tests and the existing search SQL tests. During 08-04 the scanner runs against a loopback API that uses the same hosted database as the read-only identity query, and its JSON verdict is recorded verbatim.
</verification>

<success_criteria>
The scanner proves, or explicitly disproves, exact full-term API searchability for 202701 without changing scoring, cached rankings or response fields.
</success_criteria>

## Artifacts this phase produces

- `scripts/verify_rankings_pages.py` (new): `build_parser()`, `main(argv) -> int`, `StoredSnapshot`, `PageScan`, `IdentityVerdict`, `stored_identities(session, term)`, `scan_pages(client, base_url, term, page_size)`, `reconcile(stored, scan)`.
- CLI flags: `--term` (default 202701), `--http-base-url` (required, loopback only), `--page-size` (1..200, default 200). Exit codes: 0 PASS, 1 FAIL, 3 NOT MEASURED.
- JSON output keys: `gate` ("api_identity"), `verdict`, `reason`, `term`, `observed_at_utc`, `environment`, `stored_count`, `cache_count`, `api_total`, `pages_fetched`, `page_size`, `missing_count`, `extra_count`, `duplicate_count`, `non_tampa_count`, `order_violations`, `sample_missing`, `sample_extra`, `sample_duplicates`, `api_score_source_split`.
- Failure reasons: `identity_mismatch`, `count_mismatch`, `short_page`, `duplicate_identity`, `snapshot_changed`, `order_violation`, `http_status`, `invalid_response`, `non_tampa_section`, `page_overrun`.
- `tests/api/test_verify_rankings_pages.py` (new).

## Edge resolutions (spec-less probe fallback)

| Requirement | Edge | Resolution |
|---|---|---|
| REQ-COVERAGE-03 | adjacency | explicit: 200-row and 201-row boundary tests (Task 2) |
| REQ-COVERAGE-03 | empty | explicit: empty-term PASS test; 08-04 separately requires stored_count > 0 for 202701 |
| REQ-COVERAGE-03 | ordering | explicit: sort=course total order (subject, course_number, crn) with order_violation test |

<output>Create .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-01-SUMMARY.md when done.</output>
