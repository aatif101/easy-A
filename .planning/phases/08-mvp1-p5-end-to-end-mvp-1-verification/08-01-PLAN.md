---
phase: 08-mvp1-p5-end-to-end-mvp-1-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_rankings_pages.py
  - tests/api/test_verify_rankings_pages.py
  - src/easy_a/api/routes/rankings.py
  - tests/api/test_rankings_search_sql.py
autonomous: true
requirements: [REQ-COVERAGE-03]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Every stored Tampa 202701 term-plus-CRN identity appears exactly once across the complete rankings API page walk, with stable totals and deterministic ordering.
    - A count match alone cannot pass if a section is missing, duplicated, or substituted; empty and adjacent page boundaries are defined and tested.
    - Existing score fields and search response contract remain unchanged per D-02, while global effective_n=0 never counts as evidence per D-20.
  artifacts:
    - scripts/verify_rankings_pages.py
    - tests/api/test_verify_rankings_pages.py
  key_links:
    - Read-only hosted Section identity query is compared to validated GET /api/v1/rankings/search pages from the same term and database.
    - The page scanner supplies a dated identity verdict for the final 08-VERIFICATION-REPORT.md.
---

<objective>
Prove that every stored Tampa section can be found through the complete rankings search API, not only through a matching aggregate count.
Purpose: Establish an end-to-end tracer from hosted Section rows through the locally served HTTP API to a reproducible identity verdict for REQ-COVERAGE-03.
Output: Read-only full-page identity scanner and focused regression tests.
</objective>

<execution_context>
@/home/aatif101/.codex/gsd-core/workflows/execute-plan.md
@/home/aatif101/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-RESEARCH.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-PATTERNS.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Trace one stored section through a real HTTP search page and then scan the term</name>
  <precondition>DATABASE_URL resolves privately to reachable hosted Supabase for term 202701, and a loopback easy_a.api.app:app server can use the same URL; stop with NOT MEASURED if this cannot be established.</precondition>
  <files>scripts/verify_rankings_pages.py, tests/api/test_verify_rankings_pages.py, src/easy_a/api/routes/rankings.py, tests/api/test_rankings_search_sql.py (last two only if ordering repair is needed)</files>
  <read_first>scripts/benchmark_rankings_search.py; scripts/validate_tampa_ingest.py; src/easy_a/api/routes/rankings.py; tests/api/test_benchmark_rankings_search.py; tests/api/test_rankings_api.py</read_first>
  <behavior>A successful locally served HTTP page validates the existing RankingsSearchResponse fields; the scanner pages the complete term with sort=course, collects term-plus-CRN identities, and compares the set to a read-only hosted Section query. The first page proves the live DB-to-API path before the complete scan continues.</behavior>
  <action>Implement a bounded term, page-size, and loopback-base-url CLI following the existing benchmark's HTTP validation and read-only DB session pattern. Query stored Tampa Section identities for normalized term 202701; reject non-Tampa rows rather than filtering them out of the verdict. Require stored/cache count equality, page total/limit/offset consistency, no duplicate or extra identities, and final equality of the exact stored and API identity sets. Use the existing response model, SQLAlchemy bindings and source labels; do not change ranking calculations or the API contract (D-02). Emit counts, UTC timestamp, sanitized environment and PASS/FAIL in machine-readable output without connection strings (D-06). Start a loopback uvicorn process using the same hosted URL, prove one real stored identity on its first HTTP page and complete the scan, then stop the process safely. D-20 means a global prior may appear in search but cannot establish grade coverage. The scanner is read-only and must close clients/engine.</action>
  <verify>
    <automated>uv run pytest tests/api/test_verify_rankings_pages.py -q</automated>
    <fails_when>Any seeded stored identity is absent from pages, page metadata is invalid, a duplicate appears, or the command exits nonzero.</fails_when>
    <automated>uv run python scripts/verify_rankings_pages.py --term 202701 --http-base-url http://127.0.0.1:8000</automated>
    <fails_when>The live hosted Section-to-loopback-HTTP identity path cannot be proven or the complete page scan finds an omission, duplicate or inconsistency.</fails_when>
  </verify>
  <acceptance_criteria>A 202701 HTTP search page is validated against a stored identity and the same executable can walk all pages and issue a set-based identity verdict.</acceptance_criteria>
  <done>The read-only scanner and focused tests establish the full DB-to-HTTP identity path.</done>
</task>

<task type="auto" tdd="true">
  <name>Close page-boundary, empty-result and ordering failure cases</name>
  <files>scripts/verify_rankings_pages.py, tests/api/test_verify_rankings_pages.py</files>
  <read_first>scripts/verify_rankings_pages.py; tests/api/test_verify_rankings_pages.py; src/easy_a/api/routes/rankings.py</read_first>
  <behavior>Adjacent pages have no overlap or gaps, the last page may be short, an empty term has zero total and no items, and a changed total or unstable ordering fails rather than yielding a false pass.</behavior>
  <action>Add synthetic MockTransport/seeded-DB cases for exact 200-row boundary, 201-row next page, short last page, duplicate across pages, omitted and substituted CRNs, changing total, HTTP failure, malformed response, non-Tampa stored row, and empty term. Assert the existing course sort and stable tie behavior used by the scanner; if the API's explicit order cannot guarantee unique stable pages, add the narrow deterministic tie break in src/easy_a/api/routes/rankings.py, preserving every existing supported sort and D-02 parity. Document the page-size and ordering assumptions in CLI help. A changing live snapshot fails with a retry instruction; it does not pass as a partial scan.</action>
  <verify>
    <automated>uv run pytest tests/api/test_verify_rankings_pages.py tests/api/test_rankings_search_sql.py -q</automated>
    <fails_when>An edge case passes despite missing, repeated, re-ordered, or extra identities; an existing search sort or API contract regression appears.</fails_when>
  </verify>
  <acceptance_criteria>All page identity edge cases have assertions, and the scanner exits nonzero with a specific reason on every incomplete or unstable scan.</acceptance_criteria>
  <done>Full-term searchability can be measured without equating a matching total to complete identity coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| CLI term/base URL to DB and HTTP | Treat term, page size and host as bounded inputs; use bound SQL and loopback-only HTTP. |
| Hosted DB and HTTP response to evidence | Compare complete identities and reject unstable snapshots before declaring PASS. |
| Runtime environment to output | Redact DB URLs and credentials from logs and report. |

## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01 | Tampering | page identity verdict | high | mitigate | Set equality, uniqueness, stable total, page boundaries and deterministic order are all mandatory; a high-severity mismatch blocks PASS. |
| T-08-02 | Denial of service | page scanner | medium | mitigate | Bound page size to the API limit and scope scans to one normalized term. |
| T-08-03 | Information disclosure | CLI output | high | mitigate | Emit derived identity counts and sanitized errors only; never serialize credentials or grade rows. |
</threat_model>

<verification>Run focused scanner and existing SQL search tests. During Phase 08-04, run this scanner against a loopback API using the same hosted database as the read-only identity query; record its exact result.</verification>

<success_criteria>The scanner can prove, or explicitly disprove, exact full-term API searchability at 202701 without changing scoring or response fields.</success_criteria>

## Artifacts this phase produces

- `scripts/verify_rankings_pages.py` and focused tests.
- A machine-readable full-page identity result consumed by the Phase 8 report.

## Assumptions to test

- `sort=course` has a stable, unique tie break across adjacent pages. If inspection or a seeded test disproves this, repair the narrow API ordering path before relying on the scanner.
- An empty stored term returns `total=0` and no items; this is a valid scanner result for a general term, but term 202701 still fails the full Tampa coverage gate.

<output>Create .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-01-SUMMARY.md when done.</output>
