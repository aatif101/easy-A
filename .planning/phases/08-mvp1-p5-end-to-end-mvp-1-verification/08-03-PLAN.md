---
phase: 08-mvp1-p5-end-to-end-mvp-1-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/RankingEvidence.test.tsx
  - web/src/components/RankingTable.tsx
  - web/src/components/RankingDetails.tsx
  - web/src/utils/rankings.ts
autonomous: true
requirements: [REQ-GRADES-01]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Desktop rows, mobile cards and expanded details tell students when a course has no historical outcomes, even if the API returns a numeric global-prior score.
    - Subject evidence is named as subject-level history and cannot be read as that course's own grade distribution.
    - Course/instructor-course sourced evidence retains the actual denominator, source, timestamp and confidence already supplied by the API; D-02 scoring and the API contract are unchanged.
    - D-07 unavailable states and D-20 global effective_n=0 are explicit in both layouts.
  artifacts:
    - web/src/components/RankingEvidence.test.tsx
    - web/src/components/RankingTable.tsx
    - web/src/components/RankingDetails.tsx
  key_links:
    - API score_source/effective_n values drive shared UI copy through web/src/utils/rankings.ts.
    - The same evidence classification appears in desktop and mobile render paths and expanded details.
---

<objective>
Verify and repair student-facing evidence language wherever the existing fallback display implies course history that does not exist.
Purpose: Large subject/global coverage gaps make D-07 and D-20 visible product contracts during MVP-1 verification.
Output: Focused component assertions and, where those assertions expose misleading copy, narrow UI presentation changes.
</objective>

<execution_context>
@/home/aatif101/.codex/gsd-core/workflows/execute-plan.md
@/home/aatif101/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-RESEARCH.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-PATTERNS.md
@web/src/fixtures/rankings.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Make global-prior evidence absence explicit in rows, cards and details</name>
  <files>web/src/components/RankingEvidence.test.tsx, web/src/components/RankingTable.tsx, web/src/components/RankingDetails.tsx, web/src/utils/rankings.ts</files>
  <read_first>web/src/components/RankingTable.tsx; web/src/components/RankingDetails.tsx; web/src/utils/rankings.ts; web/src/fixtures/rankings.ts; web/src/components/SeatBadge.test.tsx</read_first>
  <behavior>For score_source=global and effective_n=0, desktop, mobile and expanded details explicitly state that this course has no observed historical grade outcomes; any shown numeric estimate is clearly a global prior, and generic limited-history text cannot imply course evidence. Sourced course examples still show actual available evidence.</behavior>
  <action>Write focused student-visible assertions for global and course examples before modifying copy. Use the existing typed score_source/effective_n contract and a shared text helper where it avoids desktop/mobile drift. Preserve numeric score, confidence, denominator, source and date fields supplied by the API; alter only presentation (D-02). For a global fallback, state absence of course evidence next to any numeric estimate in both row/card surfaces and expanded details. Do not invent a grade source, date, denominator or coverage figure (D-06/D-07/D-20). Keep the existing layout and API fields; React text rendering continues to escape input.</action>
  <verify>
    <automated>npm --prefix web test -- src/components/RankingEvidence.test.tsx</automated>
    <fails_when>A global example retains misleading limited-history wording, one layout omits the absence state, sourced course evidence disappears, or the test command exits nonzero.</fails_when>
  </verify>
  <acceptance_criteria>Both search layouts and details distinguish a global prior from observed course history at the point the estimate is shown.</acceptance_criteria>
  <done>Global fallback presentation meets D-20 while score and API semantics remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Distinguish subject fallback and verify responsive evidence presentation</name>
  <files>web/src/components/RankingEvidence.test.tsx, web/src/components/RankingTable.tsx, web/src/components/RankingDetails.tsx, web/src/utils/rankings.ts</files>
  <read_first>web/src/components/RankingEvidence.test.tsx; web/src/components/RankingTable.tsx; web/src/components/RankingDetails.tsx; web/src/utils/rankings.ts</read_first>
  <behavior>Subject fallback is visibly identified as same-subject evidence rather than this course's grade history in desktop, mobile and details; course and instructor-course sources retain their proper labels. A missing or unexpected source state is explicit, not silently rendered as historical course evidence.</behavior>
  <action>Add subject, instructor-course and unexpected-source component cases alongside the global/course tests. Refine shared source labels and visible copy so subject history cannot be mistaken for that course's imported distribution. Verify the actual responsive desktop table and mobile card views using representative course, subject and global API fixtures; record observations or screenshots in the plan summary. Check that expanded details carry source, effective denominator and timestamp where the API provides them. If the browser environment is unavailable, record the manual check as NOT MEASURED rather than assuming the component tests prove layout. D-02 bars score changes; D-20 bars global or subject fallback from counting as course history.</action>
  <verify>
    <automated>npm --prefix web test -- src/components/RankingEvidence.test.tsx</automated>
    <fails_when>Any source type is mislabeled, desktop/mobile output diverges, missing evidence is presented as course history, or the command exits nonzero.</fails_when>
    <automated>npm --prefix web run typecheck</automated>
    <fails_when>The new source-state presentation violates existing TypeScript contracts.</fails_when>
  </verify>
  <acceptance_criteria>All evidence states have distinct visible language in both responsive layouts; manual observations are recorded with measured/not-measured status.</acceptance_criteria>
  <done>Student-facing course, subject and global evidence is represented truthfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| API evidence fields to student view | A numeric estimate can mislead unless source and denominator are interpreted explicitly. |
| API strings to browser | Continue React escaping; no direct HTML insertion for source labels. |

## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-07 | Tampering | evidence copy | high | mitigate | Exact source/effective-N branches and visible assertions for global, subject, course and instructor-course in both layouts; misleading high-severity result blocks PASS. |
| T-08-08 | Information disclosure | source label rendering | low | accept | Existing React text escaping and public aggregate fields remain unchanged; no new private fields are introduced. |
</threat_model>

<verification>Run focused component tests and TypeScript check. Inspect representative desktop and mobile views, recording any unmeasured browser path honestly.</verification>

<success_criteria>Every visible numeric ranking estimate accurately states its evidence scope, especially global-prior and subject-fallback cases.</success_criteria>

## Artifacts this phase produces

- `web/src/components/RankingEvidence.test.tsx` and narrow UI copy changes.
- Desktop/mobile observation in the plan summary, with explicit measurement status.

<output>Create .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-03-SUMMARY.md when done.</output>
