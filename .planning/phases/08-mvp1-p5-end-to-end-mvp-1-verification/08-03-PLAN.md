---
phase: 08-mvp1-p5-end-to-end-mvp-1-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/utils/rankings.ts
  - web/src/components/RankingTable.tsx
  - web/src/components/RankingDetails.tsx
  - web/src/components/RankingEvidence.test.tsx
autonomous: true
requirements: [REQ-GRADES-01]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "For score_source=course with effective_n=0 (the D-21 non-letter-grade exceptions), the desktop row and the mobile card show the note \"No letter-grade history (pass/fail or independent study) — score is a prior\", and expanded details in both layouts show the source \"No letter-grade history (pass/fail or independent study)\" and the sample \"No letter grades\"."
    - "For those same rows no surface shows \"Course-level history\", a \"0 grades\" sample, or \"Based on limited historical data.\"."
    - "For score_source=global (any effective_n) the desktop row and mobile card show \"No historical grades for this course — score is a global prior\", and expanded details show the source \"Global prior — no course evidence\" and the sample \"No course grades\"; no surface shows \"Course-level history\" or a \"0 grades\" sample (D-20)."
    - "For score_source=subject the row and card show \"No course history — score uses SUBJ subject-level history\" (SUBJ = the section's subject), and details show \"Subject-level fallback\" and \"N subject-level grades\", so subject history cannot be read as the course's own distribution."
    - "For course and instructor_course with effective_n > 0 the existing labels \"Course-level history\" / \"Instructor + course history\", the \"N grades\" sample, the low-confidence \"Based on limited historical data.\" explanation and the historical-analytics sentence are unchanged, and no evidence note is added."
    - "An unexpected score_source value renders the no-course-evidence wording and never course-history wording (fail closed)."
    - "The change is presentation-only: easiness score, confidence badge, API types and API client are unchanged, and no backend or scoring file changes (D-02; user decision 2026-09-23)."
  artifacts:
    - web/src/utils/rankings.ts
    - web/src/components/RankingTable.tsx
    - web/src/components/RankingDetails.tsx
    - web/src/components/RankingEvidence.test.tsx
  key_links:
    - "API score_source + effective_n + subject -> describeEvidence() in web/src/utils/rankings.ts -> RankingTable desktop row, RankingTable mobile card and RankingDetails (rendered under both layouts)."
    - "scoreSourceLabel() remains the single label table for course, instructor_course and subject evidence; describeEvidence() overrides it only for effective_n <= 0 and global/unknown sources."
  prohibitions:
    - requirement_id: REQ-GRADES-01
      category: transparency
      statement: "MUST NOT present a course with effective_n = 0, a subject fallback or a global prior as course-level history, and MUST NOT display a \"0 grades\" sample for them (D-20, D-21)."
      status: resolved
      verification: test
    - requirement_id: REQ-GRADES-01
      category: values
      statement: "MUST NOT change the easiness score, the API response fields or the scoring model to resolve evidence wording; the fix is presentation-only (D-02)."
      status: resolved
      verification: test
---

<objective>
Make every student-visible evidence surface state exactly what backs the displayed easiness estimate, including the 300 D-21 non-letter-grade sections that the API returns as score_source=course with effective_n=0.

Purpose: D-20 forbids presenting effective_n = 0 as evidence-backed history, and D-21 keeps listed exceptions on their honest fallback with D-20 labeling. Today web/src/utils/rankings.ts scoreSourceLabel() maps course to "Course-level history" and web/src/components/RankingDetails.tsx renders "Effective sample {n} grades", so an x4900-series section reads "Course-level history · 0 grades", and a global prior carries the generic "Based on limited historical data." text. User decision (2026-09-23): presentation-only fix; no scoring or API change.
Output: a shared describeEvidence() helper, narrow wording changes in RankingTable and RankingDetails, and component tests for course/effective_n=0, global, subject and course/effective_n>0 across desktop rows, mobile cards and expanded details.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-PATTERNS.md
@web/src/utils/rankings.ts
@web/src/components/RankingTable.tsx
@web/src/components/RankingDetails.tsx
@web/src/types/rankings.ts
@web/src/fixtures/rankings.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry the course/effective_n=0 case from the API fields through the helper to row, card and details</name>
  <precondition>web/node_modules is installed from the committed web/package-lock.json; if `npm --prefix web test` reports "vitest: not found", run `npm --prefix web ci --no-audit --no-fund` first (lockfile install only, no new or upgraded packages). Planning-time baseline 2026-09-23: 78 frontend tests passed, lint and typecheck clean.</precondition>
  <files>web/src/utils/rankings.ts, web/src/components/RankingTable.tsx, web/src/components/RankingDetails.tsx, web/src/components/RankingEvidence.test.tsx</files>
  <read_first>web/src/utils/rankings.ts (scoreSourceLabel at line 79); web/src/components/RankingTable.tsx (desktop Easiness cell line 63, Confidence cell line 65, mobile score block line 93, mobile low-confidence paragraph line 96); web/src/components/RankingDetails.tsx (Effective sample line 34, Confidence line 35, Score source line 36, historical sentence line 89); web/src/types/rankings.ts (ScoreSource, SectionRanking); web/src/fixtures/rankings.ts (syntheticRankings; CRN 15502 course 342, CRN 16880 instructor_course 34 low, CRN 17205 global); web/src/components/SeatBadge.test.tsx (fixture override and within() scoping); web/src/App.test.tsx (low-confidence test near line 177 that must keep passing)</read_first>
  <behavior>
    - describeEvidence({score_source: "course", effective_n: 0, subject: "IDS"}) returns scope "no_letter_grade_history", sourceLabel "No letter-grade history (pass/fail or independent study)", sampleLabel "No letter grades", note "No letter-grade history (pass/fail or independent study) — score is a prior".
    - Rendering RankingTable with one course/effective_n=0 ranking expanded shows that note inside the table row and inside the mobile card container, and both expanded detail regions show the source and sample labels above.
    - In that render no element's text equals "Course-level history" or "0 grades", and no button is named "Based on limited historical data.".
    - The numeric easiness score of the fixture is still rendered.
  </behavior>
  <action>Add to web/src/utils/rankings.ts an exported type EvidenceScope ("course_history", "no_letter_grade_history", "subject_fallback", "no_course_evidence"), an exported interface EvidenceDescription (scope, sourceLabel, sampleLabel, note: string or null, detailSummary: string or null) and an exported function describeEvidence(ranking: Pick of SectionRanking with score_source, effective_n, subject). Rules: course or instructor_course with effective_n greater than 0 gives course_history, reusing scoreSourceLabel for the label, a sample of the rounded effective_n followed by " grades", note null and detailSummary null (the existing sentence stays). Course or instructor_course with effective_n less than or equal to 0 gives no_letter_grade_history with the exact strings in this task's behavior list and detailSummary "This course's recorded history has no letter grades, so the score is a prior, not this course's grade outcome.". Keep scoreSourceLabel exported and unchanged as the label table. In RankingTable, compute the description once per ranking; when note is not null, render it under the score in the desktop Easiness cell and as a paragraph under the mobile score block, and render the existing low-confidence "Based on limited historical data." explanation (desktop InfoTip and mobile paragraph) only for the course_history scope (for other scopes the desktop Confidence cell keeps the "Low confidence" text without an InfoTip, because InfoTip also renders its label as tooltip text and the note already sits under the score; this keeps each note string rendered once per layout). In RankingDetails, render sampleLabel in the Effective sample cell, sourceLabel in the Score source cell, the "Based on limited historical data." line only for course_history, and detailSummary in place of the historical-analytics sentence when it is not null. Keep the numeric score, confidence badge, layout classes and API types unchanged; do not touch web/src/types or web/src/api (D-02). React text rendering continues to escape all values. Write the behavior tests first in web/src/components/RankingEvidence.test.tsx by overriding a syntheticRankings entry as SeatBadge.test.tsx does, scoping to screen.getByRole("table"), screen.getByLabelText("Course ranking cards") and screen.getAllByRole("region", { name: /^Details for/ }).</action>
  <verify>
    <automated>npm --prefix web test -- src/components/RankingEvidence.test.tsx</automated>
    <fails_when>non-zero exit, "No test files found" in output, or any course/effective_n=0 assertion reported as failed</fails_when>
  </verify>
  <acceptance_criteria>
    - web/src/utils/rankings.ts exports describeEvidence, EvidenceScope and EvidenceDescription, and still exports scoreSourceLabel with the mapping course -> "Course-level history".
    - web/src/components/RankingEvidence.test.tsx asserts the exact note "No letter-grade history (pass/fail or independent study) — score is a prior" in the table row and in the mobile card container, and asserts two detail regions each containing "No letter grades".
    - The same test asserts that queryAllByText("Course-level history") and queryAllByText(/^0 grades$/) are empty for the course/effective_n=0 render.
    - `npm --prefix web test -- src/components/RankingEvidence.test.tsx` exits 0.
  </acceptance_criteria>
  <done>A course/effective_n=0 section reads as a prior with no letter-grade history in the desktop row, the mobile card and expanded details, while its numeric score is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover global, subject, course-backed and unexpected sources across both layouts and details</name>
  <files>web/src/utils/rankings.ts, web/src/components/RankingTable.tsx, web/src/components/RankingDetails.tsx, web/src/components/RankingEvidence.test.tsx</files>
  <read_first>web/src/utils/rankings.ts; web/src/components/RankingTable.tsx; web/src/components/RankingDetails.tsx; web/src/components/RankingEvidence.test.tsx; web/src/App.test.tsx; web/src/fixtures/rankings.ts</read_first>
  <behavior>
    - global with effective_n 0, and the fixture CRN 17205 global with effective_n 22, both give scope no_course_evidence: row and card note "No historical grades for this course — score is a global prior"; details source "Global prior — no course evidence", sample "No course grades", summary "No historical grade data exists for this course; the score is a global prior, not this course's outcome."; no "Course-level history", no /^0 grades$/ and no /^22 grades$/ text.
    - subject with effective_n 45 and subject "PSY" gives subject_fallback: row and card note "No course history — score uses PSY subject-level history"; details source "Subject-level fallback", sample "45 subject-level grades", summary "This course has no own grade history; the score uses PSY subject-level history, not this course's grade distribution."; subject with effective_n 0 gives no_course_evidence.
    - course with effective_n 342 (fixture CRN 15502) shows details source "Course-level history", sample "342 grades", the existing "Historical analytics cover" sentence, and no evidence note in row or card; instructor_course with effective_n 34 and low confidence (fixture CRN 16880) keeps "Instructor + course history", "34 grades" and the "Based on limited historical data." InfoTip.
    - An unexpected score_source string (cast through unknown) renders the no_course_evidence wording and no "Course-level history".
  </behavior>
  <action>Extend describeEvidence with the remaining rules and the exact strings in this task's behavior list: subject with effective_n greater than 0 gives subject_fallback (label from scoreSourceLabel("subject"), sample of the rounded effective_n followed by " subject-level grades", note and detailSummary interpolating ranking.subject); global, subject with effective_n less than or equal to 0, and any unrecognized score_source give no_course_evidence regardless of effective_n (fail closed). Add the behavior tests as table-driven cases, each rendering RankingTable with the case expanded and asserting the note in the desktop row and mobile card container plus the source, sample and summary in both detail regions, with the negative assertions for evidence-free scopes. Keep web/src/App.test.tsx passing unchanged; if an existing assertion conflicts with the D-20 wording, update only that assertion and name it in the SUMMARY. Inspect the real desktop table and mobile card at representative widths using the synthetic fixture mode or the local API, and record observations for course, course/effective_n=0, subject and global rows in the SUMMARY; if a browser cannot be run in this environment, record the manual check as NOT MEASURED rather than inferring layout from component tests. No scoring, API field or backend change (D-02).</action>
  <verify>
    <automated>npm --prefix web test -- src/components/RankingEvidence.test.tsx</automated>
    <fails_when>non-zero exit, "No test files found", or any global/subject/course/instructor/unexpected-source assertion reported as failed</fails_when>
    <automated>npm --prefix web test</automated>
    <fails_when>non-zero exit or a "failed" count above 0 in the Vitest summary (for example the App.test.tsx low-confidence test regressing)</fails_when>
    <automated>npm --prefix web run typecheck</automated>
    <fails_when>non-zero exit or any "error TS" line in the output</fails_when>
    <automated>npm --prefix web run lint</automated>
    <fails_when>non-zero exit or any ESLint "error" entry reported for the four modified files</fails_when>
  </verify>
  <acceptance_criteria>
    - web/src/components/RankingEvidence.test.tsx contains cases named for course_history, no_letter_grade_history, subject_fallback and no_course_evidence, each asserting desktop row, mobile card container and both expanded detail regions.
    - The test file asserts the literal strings "No historical grades for this course — score is a global prior", "Global prior — no course evidence", "No course grades", "No course history — score uses PSY subject-level history", "45 subject-level grades" and "342 grades".
    - `npm --prefix web test`, `npm --prefix web run typecheck` and `npm --prefix web run lint` each exit 0.
    - `git diff --quiet origin/main -- web/src/types web/src/api web/src/fixtures src` exits 0 (presentation-only).
    - The SUMMARY records desktop and mobile observations for course, course/effective_n=0, subject and global rows, or states NOT MEASURED with the reason.
  </acceptance_criteria>
  <done>Every evidence state has distinct, truthful wording in desktop rows, mobile cards and expanded details, with tests for course/effective_n=0, global, subject and course/effective_n>0, and no scoring or API change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| API evidence fields to student view | A numeric estimate misleads unless score_source and effective_n are interpreted explicitly at the point it is shown. |
| API strings to browser DOM | Values render as React text; no raw HTML insertion. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-07 | Tampering (misleading evidence) | describeEvidence(), RankingTable, RankingDetails | high | mitigate | Exact-string positive and negative assertions per evidence scope across desktop row, mobile card and both detail regions; unknown sources fail closed to no-course-evidence wording. |
| T-08-08 | Information disclosure | source label rendering | low | accept | Existing React text escaping; only already-public aggregate fields are rendered; no new fields introduced. |
| T-08-16 | Tampering (scope creep into scoring) | web/src/types, web/src/api, backend | medium | mitigate | Acceptance criterion requires git diff of web/src/types, web/src/api, web/src/fixtures and src to be empty (D-02). |
</threat_model>

<verification>
Run the focused evidence tests, the full frontend suite, typecheck and lint. Record desktop and mobile observations, or NOT MEASURED, in the SUMMARY; 08-04 carries them into the verification report.
</verification>

<success_criteria>
Every visible easiness estimate states its evidence scope: own-course history, no letter-grade history (prior), subject-level history, or no course evidence (global prior). No course/effective_n=0 or global row shows course-history wording or a "0 grades" sample.
</success_criteria>

## Artifacts this phase produces

- `web/src/utils/rankings.ts` (modified): exported `EvidenceScope`, `EvidenceDescription`, `describeEvidence(ranking)`; `scoreSourceLabel` unchanged.
- `web/src/components/RankingTable.tsx` (modified): evidence note under the desktop score and the mobile score; low-confidence limited-data explanation only for course_history.
- `web/src/components/RankingDetails.tsx` (modified): sample, source and summary lines from `describeEvidence`.
- `web/src/components/RankingEvidence.test.tsx` (new).
- Student-visible strings: "No letter-grade history (pass/fail or independent study) — score is a prior", "No letter-grade history (pass/fail or independent study)", "No letter grades", "No historical grades for this course — score is a global prior", "Global prior — no course evidence", "No course grades", "No course history — score uses {SUBJECT} subject-level history", "{N} subject-level grades", and the two detail summaries above.

<output>Create .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-03-SUMMARY.md when done.</output>
