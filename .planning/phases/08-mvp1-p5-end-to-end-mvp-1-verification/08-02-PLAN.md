---
phase: 08-mvp1-p5-end-to-end-mvp-1-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/inventory_tampa_grades.py
  - tests/refresh/test_inventory_tampa_grades.py
  - scripts/validate_tampa_ingest.py
  - tests/refresh/test_validate_tampa_ingest.py
autonomous: true
requirements: [REQ-GRADES-01]
estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every stored 202701 Tampa section receives exactly one state: evidence_backed, exception_non_letter_grade, exception_no_rows, or a named integrity failure (missing_cache_row, unbacked_course_claim, raw_total_mismatch, history_not_used, global_nonzero_effective_n, unclassified); the state counts sum to the stored section count."
    - "evidence_backed (D-21 sourced own-course history) requires score_source course or instructor_course, effective_n > 0, at least one attributed GradeDistribution row for the exact course key with letter-grade (A-F) weight, and a cached historical_analytics.total_grade_count that reconciles to the persisted total_grades sum for that course key."
    - "D-21 grade coverage is PASS only when there are zero integrity failures and every section that is not evidence_backed is listed per section with reason_category no_rows or non_letter_grade; exception sections are never added to the evidence_backed count (D-20, D-21)."
    - "A no_rows exception carries a fixed reason note that the stored data cannot distinguish InfoCenter's under-five-student omission, a new course, or a course not offered in the window; the inventory never assigns an unproven cause (D-06, D-07)."
    - "Per historical term the inventory reports rows, distinct courses, raw total_grades sum, source values, distinct source_hash count and ingested_at range, plus unattributed rows, bucket-sum mismatches and whether the 202701 cache was refreshed after the latest grade ingest."
    - "Concurrency and interruption: the inventory issues no writes, and --exceptions-md writes through a temporary file plus rename, so an interrupted run leaves either the previous file or none, never a truncated list."
    - { statement: "On PostgreSQL every inventory count is read inside one REPEATABLE READ READ ONLY transaction, so a concurrent refresh cannot produce a mixed snapshot.", verification: backstop }
    - "scripts/validate_tampa_ingest.py honest-coverage accepts score_source=course with effective_n=0 only when stored rows prove letter-grade-free history (A-F sum 0, total_grades sum > 0) for that course key, reports how many such sections it verified, and still fails every other effective_n <= 0 course or instructor_course claim and every subject row with effective_n=0 (D-20, D-21)."
  artifacts:
    - scripts/inventory_tampa_grades.py
    - tests/refresh/test_inventory_tampa_grades.py
    - scripts/validate_tampa_ingest.py
    - tests/refresh/test_validate_tampa_ingest.py
  key_links:
    - "Section (term 202701) -> Course (subject, number) key -> attributed GradeDistribution rows for that key before the term, matching `_course_ids` grouping in src/easy_a/analytics/queries.py -> SectionRankingCache row by section_id."
    - "The inventory's exceptions list and D-21 verdict feed 08-VERIFICATION-REPORT.md and 08-D21-EXCEPTIONS.md in 08-04; the validator's verified non-letter-grade count must equal the inventory's exception_non_letter_grade count there."
  prohibitions:
    - requirement_id: REQ-GRADES-01
      category: transparency
      statement: "MUST NOT count a subject fallback, a global prior, or a course row with effective_n = 0 as own-course grade history in any count, state or verdict (D-20, D-21)."
      status: resolved
      verification: test
    - requirement_id: REQ-GRADES-01
      category: values
      statement: "MUST NOT invent grade data, sources, or an unproven exception cause (under-five enrollment, new course) for a no_rows exception (D-06, D-07)."
      status: resolved
      verification: test
    - requirement_id: REQ-GRADES-01
      category: values
      statement: "MUST NOT fetch, source or import additional grade exports in this phase; coverage work is stopped by D-21 and listed exceptions are the honest end state."
      status: resolved
      verification: judgment
    - requirement_id: REQ-GRADES-01
      category: transparency
      statement: "MUST NOT relax the honest-coverage guard beyond the stored-evidence-proven non-letter-grade case; any other effective_n <= 0 course-backed claim remains a failure."
      status: resolved
      verification: test
    - requirement_id: REQ-GRADES-01
      category: privacy
      statement: "MUST NOT emit or commit raw grade exports or CRN-level grade buckets; outputs carry identifiers, states and derived totals only (D-19)."
      status: resolved
      verification: test
---

<objective>
Measure, for every Spring 2027 Tampa section, whether it has sourced own-course grade history or is a listed D-21 source-limited exception, and make the existing Tampa validator agree with D-21.

Purpose: PROJECT.md D-21 (locked 2026-09-23) defines the MVP-1 grade criterion: each section either has sourced own-course history (score_source course/instructor_course, effective_n > 0) or is reported in an explicit per-section exception list (no rows, under five students, new course, or non-letter-grade course) that keeps its honest subject/global fallback with D-20 labeling. Fallbacks never count as course history and no data is invented. Coverage sourcing is stopped by decision, so this plan measures and lists; it does not import.
Output: a read-only full-term grade inventory CLI with per-section D-21 classification and exception list, and a D-21-consistent honest-coverage check in scripts/validate_tampa_ingest.py.

Live baseline for sanity only (2026-09-23, not a pass condition, re-measure at execution): 3,783 sections; 3,122 evidence-backed; 661 exceptions = 311 subject + 50 global + 300 course with effective_n = 0 (x4900-series courses whose history has no letter grades).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/grade-coverage-import-2026-09-23.md
@.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-RESEARCH.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-PATTERNS.md
@scripts/validate_tampa_ingest.py
@src/easy_a/analytics/grades.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one course key from hosted rows through the cache to a classified JSON record</name>
  <precondition>DATABASE_URL resolves privately to reachable hosted Supabase holding term 202701 sections, section_rankings and grade_distributions; if unreachable the live command must exit 3 (NOT MEASURED) and only the unit tests are proven.</precondition>
  <files>scripts/inventory_tampa_grades.py, tests/refresh/test_inventory_tampa_grades.py</files>
  <read_first>scripts/validate_tampa_ingest.py (session factory, term-scoped joins, build_parser/main); tests/refresh/test_validate_tampa_ingest.py (_add_course, _add_section, _add_ranking); tests/analytics/test_queries.py (_add_grade at line 318, statement-count test at line 553); src/easy_a/models/core.py (GradeDistribution fields, uq_grade_distributions_term_crn_source, uq_courses_identity); src/easy_a/analytics/queries.py (`_course_ids` course-key grouping, course/subject/global selection near line 445); src/easy_a/analytics/grades.py (completed_grade_count = A-F, effective_n = min(grade, withdrawal)); src/easy_a/rankings/cache.py (SectionRankingCache.score_source, effective_n, historical_analytics, refreshed_at; refresh_section_rankings); scripts/benchmark_rankings_search.py (`_environment_label`, SET TRANSACTION READ ONLY)</read_first>
  <behavior>
    - Seed one course key with two historical letter-grade rows, one current section, and build its cache row with the real refresh_section_rankings: the section is classified evidence_backed and its record reports rows=2, the persisted total_grades sum, the historical term codes, source values and the cached total_grade_count equal to that sum.
    - Seed a second course key with no grade rows (cache builds a subject or global fallback): the section is classified exception_no_rows with reason_category no_rows.
    - main() prints one JSON object whose snapshot.section_count equals the seeded section count and exits 0 when both sections are classified without integrity failures.
  </behavior>
  <action>Create scripts/inventory_tampa_grades.py as a read-only CLI with `build_parser()`, `main(argv) -> int`, `collect_inventory(session, term) -> Inventory` and `classify_section(...) -> SectionState`, plus an `EvidenceState` string enum holding every state name listed in this plan's must_haves. Flag `--term` (default 202701, normalized with normalize_banner_term_code). On PostgreSQL, execute SET TRANSACTION ISOLATION LEVEL REPEATABLE READ READ ONLY as the first statement of the one session used for all reads. Read in a fixed number of batched statements, not one query per course: (1) the term's Section rows with Course subject/number and campus, (2) the term's SectionRankingCache rows, (3) attributed GradeDistribution aggregates grouped by Course (subject, number) key and historical Term banner_code for terms earlier than the target term, summing A-F counts, total_grades and row counts and collecting source values, distinct source_hash count and ingested_at range. Group by the course key exactly as `_course_ids` in src/easy_a/analytics/queries.py does, because uq_courses_identity allows several catalog editions per key. Implement the evidence_backed and exception_no_rows paths first, using the definitions in must_haves. Print one JSON object with keys gate ("d21_grade_coverage"), term, observed_at_utc, environment (sanitized like `_environment_label`), snapshot (section_count, represented_course_count, cache_row_count, cache_refreshed_at_min, cache_refreshed_at_max, grade_row_count, grade_ingested_at_max, non_tampa_section_count), sections_by_state and courses_by_state. Emit only identifiers, states and derived totals; never grade bucket values per CRN, connection strings or credentials (D-19). Exit 0 when no integrity failure is present, 1 otherwise, and 3 when the database is unreachable. Write the behavior tests first, building cache rows through the real refresh_section_rankings so the reconciliation invariant is proven against production code rather than assumed, then run the live command once.</action>
  <verify>
    <automated>uv run pytest tests/refresh/test_inventory_tampa_grades.py -q</automated>
    <fails_when>non-zero exit, "no tests ran", or the evidence_backed / exception_no_rows / JSON-snapshot behavior tests report failed</fails_when>
    <automated>uv run python scripts/inventory_tampa_grades.py --term 202701</automated>
    <fails_when>exit code 3, a Python traceback on stderr, or stdout that is not a single JSON object containing snapshot.section_count; exit 1 at this stage is expected until Task 2 classifies the course/effective_n=0 rows and is recorded, not hidden</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/refresh/test_inventory_tampa_grades.py -q` exits 0 with at least 3 passed, including one test that builds cache rows via refresh_section_rankings.
    - The live run prints JSON with "gate": "d21_grade_coverage" and a snapshot.section_count greater than 0; the SUMMARY records the snapshot block verbatim.
    - The JSON contains no key named a_count, b_count, c_count, d_count, f_count, s_count, u_count or w_count.
  </acceptance_criteria>
  <done>One hosted-database path from Section through GradeDistribution and the cache to a classified JSON record is proven live.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify every D-21 state, reconcile provenance and emit the per-section exception list</name>
  <files>scripts/inventory_tampa_grades.py, tests/refresh/test_inventory_tampa_grades.py</files>
  <read_first>scripts/inventory_tampa_grades.py; tests/refresh/test_inventory_tampa_grades.py; .planning/PROJECT.md (D-20, D-21); .planning/grade-coverage-import-2026-09-23.md ("Final result" section); src/easy_a/quality/checks.py (bucket-sum rule in _check_grades near line 214)</read_first>
  <behavior>
    - A course key whose attributed rows have A-F sum 0 and S/U total greater than 0, built through refresh_section_rankings, yields score_source course with effective_n 0 and is classified exception_non_letter_grade with reason_category non_letter_grade.
    - A course/instructor_course row with effective_n greater than 0 but no attributed rows gives unbacked_course_claim; a cached total_grade_count different from the key's persisted sum gives raw_total_mismatch; a subject or global row whose key has attributed rows gives history_not_used; a global row with effective_n greater than 0 gives global_nonzero_effective_n; an unknown score_source or a subject row with effective_n 0 gives unclassified; a section without a cache row gives missing_cache_row.
    - A grade row with null course_id, a row whose ten bucket counts do not sum to total_grades, a cache refreshed before the latest grade ingested_at, and a non-Tampa section each set the integrity verdict to FAIL.
    - With only evidence_backed and listed exceptions, verdicts are integrity PASS and d21_grade_coverage PASS, and evidence_backed_sections excludes every exception section.
    - --exceptions-md writes a table sorted by (subject, course_number, crn) whose exception section count equals the JSON exception count; a simulated write failure leaves the target path absent or unchanged.
    - The inventory changes no row counts in any table (read-only).
  </behavior>
  <action>Complete `classify_section` so every section maps to exactly one must_haves state, applying these rules per D-21. evidence_backed: score_source course or instructor_course, effective_n greater than 0, key has attributed rows with A-F sum greater than 0, and for course the cached historical_analytics.total_grade_count equals the key's persisted total_grades sum (for instructor_course it lies in (0, key sum]). exception_non_letter_grade: score_source course, effective_n equal to 0, key rows exist with A-F sum 0 and total_grades sum greater than 0, cached total equal to the key sum. exception_no_rows: score_source subject (effective_n greater than 0, subject-level) or global (effective_n equal to 0), and the key has no attributed rows before the term. Everything else maps to the named failure states. Add term-level integrity counters unattributed_grade_rows, bucket_sum_mismatch_rows, rows_at_or_after_term (info only), stale_cache and non_tampa_section_count. Add module constants D21_WINDOW_TERMS = ("202501", "202505", "202508", "202601") and D21_CHECKED_EMPTY_TERMS = ("202605",) encoding D-21's allowed source range, and emit a window block listing rows per window term, rows per checked-empty term and rows outside the window (the 202408 pilot rows remain sourced history and are reported as outside_window). Add grade_rows_by_term (term, rows, courses, total_grades_sum, sources, source_hash_count, ingested_at_min, ingested_at_max), evidence_backed_sections, exception_sections_by_reason, an exceptions array (subject, course_number, crn, score_source, effective_n, reason_category, reason_note), and verdicts {integrity, d21_grade_coverage}. For no_rows use one fixed reason_note stating that no stored grade rows exist for the exact course key in the imported range and that stored data cannot distinguish InfoCenter's under-five-student omission, a new course, or a course not offered in the window; never assign a narrower cause (D-06, D-07). Add `--exceptions-md PATH` rendering a deterministic Markdown file (header with term, observed_at_utc, counts by reason_category; one table row per course key listing section count, CRNs, score_source, effective_n and reason_category; then the reason-note legend) through `write_atomic(path, text)` (temporary file in the same directory, then os.replace). Exit 0 only when both verdicts are PASS, 1 otherwise, 3 on connection failure; always print the JSON before exiting. Do not hard-code live counts. Do not fetch or import grades, change scoring (D-02), or write to the database.</action>
  <verify>
    <automated>uv run pytest tests/refresh/test_inventory_tampa_grades.py -q</automated>
    <fails_when>non-zero exit, or any classification, integrity-counter, verdict, exceptions-md, atomic-write or read-only test reports failed</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/refresh/test_inventory_tampa_grades.py asserts each of these state names at least once: evidence_backed, exception_non_letter_grade, exception_no_rows, missing_cache_row, unbacked_course_claim, raw_total_mismatch, history_not_used, global_nonzero_effective_n, unclassified.
    - A test asserts d21_grade_coverage is "FAIL" whenever any failure state is present, and "PASS" when only evidence_backed and listed exceptions exist.
    - A test asserts that evidence_backed_sections plus the sum of exception_sections_by_reason equals snapshot.section_count in the all-classified fixture.
    - A test asserts that the no_rows reason_note text is identical for every no_rows exception.
    - A statement-count test shows the same number of SQL statements for a 2-course and a 6-course fixture.
    - `uv run python scripts/inventory_tampa_grades.py --help` lists `--term` and `--exceptions-md`.
    - `uv run ruff check scripts/inventory_tampa_grades.py tests/refresh/test_inventory_tampa_grades.py` and `uv run mypy scripts/inventory_tampa_grades.py` each exit 0 (scoped: repo-wide ruff/mypy already fail on untouched files).
  </acceptance_criteria>
  <done>The inventory classifies every section under D-21, enumerates every exception per section with its reason category, and reports provenance and integrity without inventing causes or data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Encode D-21's non-letter-grade exception in the validator's honest-coverage check</name>
  <reversibility rating="reversible">Changes one read-only verification function and its tests; reverting the commit restores the prior rule. No scoring, cache, API or schema change.</reversibility>
  <precondition>DATABASE_URL resolves privately to reachable hosted Supabase for the live validator run; if unreachable, record the live run as NOT MEASURED.</precondition>
  <files>scripts/validate_tampa_ingest.py, tests/refresh/test_validate_tampa_ingest.py</files>
  <read_first>scripts/validate_tampa_ingest.py (assert_honest_coverage at line 145 and main); tests/refresh/test_validate_tampa_ingest.py (_seed_honest_dataset, the fabricated course-backed test near line 173); tests/analytics/test_queries.py (_add_grade); .planning/PROJECT.md (D-20, D-21); .planning/phases/06-mvp1-p3-all-tampa-section-ingestion-10-3-782/06-03-PLAN.md (prohibitions on weakening guards)</read_first>
  <behavior>
    - A score_source=course, effective_n=0 section whose course key has attributed rows with A-F sum 0 and S/U total greater than 0 passes, and assert_honest_coverage returns 1.
    - The same row with a course key whose rows include letter grades raises AssertionError naming the CRN.
    - The existing fabricated course-backed row with no grade rows still raises AssertionError naming CRN 30002.
    - A subject row with effective_n 0 raises; an instructor_course row with effective_n 0 raises even with letter-grade-free history.
    - main() prints "PASS honest-coverage (verified non-letter-grade exceptions: N)" with N equal to the verified count.
  </behavior>
  <action>Amend `assert_honest_coverage(session, term)` to return an int (the number of verified non-letter-grade exception sections) while keeping its AssertionError contract. Add `_course_key_grade_totals(session, term, course_keys)` issuing one grouped query that returns, per Course (subject, number) key, the attributed GradeDistribution row count, A-F sum and total_grades sum for historical terms earlier than the target term, grouped the same way as `_course_ids` in src/easy_a/analytics/queries.py. A row with score_source "course" and effective_n exactly 0 is a verified exception only when its key has at least one attributed row, an A-F sum of 0 and a total_grades sum greater than 0 (D-21 non-letter-grade course). Every other course or instructor_course row with effective_n less than or equal to 0 keeps raising the existing D-20 message naming the CRN, and every effective_n 0 row that is neither global nor a verified exception keeps raising. Update the docstring to cite D-20 and D-21 and to say these sections are listed exceptions, never course history. Change main so the honest-coverage PASS line reads "PASS honest-coverage (verified non-letter-grade exceptions: N)"; leave the suffix-exact and reconciliation checks and their output untouched (06-03 prohibition). Add the behavior tests beside the existing honest-coverage tests, seeding grade rows with the `_add_grade` pattern.</action>
  <verify>
    <automated>uv run pytest tests/refresh/test_validate_tampa_ingest.py tests/refresh/test_inventory_tampa_grades.py -q</automated>
    <fails_when>non-zero exit, or the fabricated-row, letter-grade, subject-zero, instructor-zero or verified-exception test reports failed</fails_when>
    <automated>uv run python scripts/validate_tampa_ingest.py --term 202701 --targets config/course_targets.toml</automated>
    <fails_when>any line starting "FAIL " in stdout, or a non-zero exit; a FAIL honest-coverage naming a CRN is a real D-20 finding that must be reported, never answered by loosening the rule further</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/refresh/test_validate_tampa_ingest.py -q` exits 0 and includes test_assert_honest_coverage_raises_on_fabricated_course_backed_row unchanged and passing.
    - New tests cover: verified exception returns 1; course/0 with letter grades raises; subject/0 raises; instructor_course/0 raises.
    - The live validator output contains "PASS suffix-exact", "PASS reconciliation" and a line starting "PASS honest-coverage (verified non-letter-grade exceptions: "; the SUMMARY records N.
    - `git diff --quiet origin/main -- src/easy_a` exits 0 (no production source change).
    - `uv run ruff check scripts/validate_tampa_ingest.py tests/refresh/test_validate_tampa_ingest.py` and `uv run mypy scripts/validate_tampa_ingest.py` each exit 0.
  </acceptance_criteria>
  <done>The Tampa validator agrees with D-21: evidence-proven non-letter-grade courses are counted as listed exceptions, and every other zero-sample course-backed claim still fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hosted grade/cache records to reported claim | A numeric cached score is not proof of own-course history; state is derived from attributed rows, letter-grade weight and reconciled raw totals. |
| Validator rule to D-20 guard | Relaxing a guard is the dangerous direction; the exception must be proven from stored rows. |
| Configured DB connection to stdout and Markdown | Only identifiers, states, derived totals and provenance leave the runtime. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-04 | Tampering | classify_section() | high | mitigate | Every section maps to one state; evidence_backed requires attributed letter-grade rows plus raw-total reconciliation against a cache built by the real refresh path; any failure state blocks D-21 PASS (tests per state). |
| T-08-05 | Information disclosure | JSON stdout and --exceptions-md | high | mitigate | No per-CRN grade buckets, URLs or credentials; acceptance criterion rejects bucket keys in JSON. |
| T-08-06 | Repudiation | provenance reconciliation | medium | mitigate | grade_rows_by_term records source values, source_hash counts, ingested_at range and historical term; the SUMMARY records UTC observation. |
| T-08-14 | Elevation of privilege (guard bypass) | assert_honest_coverage() | high | mitigate | Exception accepted only for score_source course with effective_n exactly 0 and stored A-F sum 0 with total greater than 0; four negative tests keep every other zero-sample claim failing. |
| T-08-15 | Denial of service | collect_inventory() | low | mitigate | Fixed statement count independent of course count (statement-count test); one term per run. |
</threat_model>

<verification>
Run the focused inventory and validator tests, then the live read-only validator. 08-04 runs the inventory against hosted 202701 with --exceptions-md and records both verdicts, the per-term provenance and the exception list.
</verification>

<success_criteria>
Every represented section is evidence-backed or a listed D-21 exception with a reason category, integrity failures are named rather than absorbed, and the Tampa validator no longer disagrees with D-21 while still rejecting every unproven zero-sample course claim.
</success_criteria>

## Artifacts this phase produces

- `scripts/inventory_tampa_grades.py` (new): `build_parser()`, `main(argv) -> int`, `collect_inventory(session, term) -> Inventory`, `classify_section(...) -> SectionState`, `EvidenceState`, `write_atomic(path, text)`, constants `D21_WINDOW_TERMS`, `D21_CHECKED_EMPTY_TERMS`.
- CLI flags: `--term` (default 202701), `--exceptions-md PATH`. Exit codes: 0 both verdicts PASS, 1 otherwise, 3 NOT MEASURED.
- JSON keys: `gate` ("d21_grade_coverage"), `term`, `observed_at_utc`, `environment`, `snapshot`, `grade_rows_by_term`, `window`, `sections_by_state`, `courses_by_state`, `integrity`, `evidence_backed_sections`, `exception_sections_by_reason`, `exceptions`, `verdicts`.
- States: `evidence_backed`, `exception_non_letter_grade`, `exception_no_rows`, `missing_cache_row`, `unbacked_course_claim`, `raw_total_mismatch`, `history_not_used`, `global_nonzero_effective_n`, `unclassified`. Reason categories: `non_letter_grade`, `no_rows`.
- `scripts/validate_tampa_ingest.py` (modified): `assert_honest_coverage(session, term) -> int`, new `_course_key_grade_totals(session, term, course_keys)`, PASS line "PASS honest-coverage (verified non-letter-grade exceptions: N)".
- `tests/refresh/test_inventory_tampa_grades.py` (new); `tests/refresh/test_validate_tampa_ingest.py` (extended).

## Edge resolutions (spec-less probe fallback)

| Requirement | Edge | Resolution |
|---|---|---|
| REQ-GRADES-01 | concurrency | explicit: no writes plus atomic --exceptions-md (read-only and atomic-write tests); backstop: single REPEATABLE READ READ ONLY PostgreSQL snapshot, cross-checked in 08-04 by a before/after snapshot comparison |

## Assumptions to test

- The `score_source` vocabulary is `instructor_course`, `course`, `subject`, `global` (src/easy_a/analytics/confidence.py). Anything else is `unclassified` and blocks D-21 PASS.
- The cached `historical_analytics.total_grade_count` for a `course` row equals the persisted `total_grades` sum for its course key; Task 1 proves this through the real `refresh_section_rankings`. If the live run disagrees, report `raw_total_mismatch` with the affected keys; do not change scoring.

<output>Create .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-02-SUMMARY.md when done.</output>
