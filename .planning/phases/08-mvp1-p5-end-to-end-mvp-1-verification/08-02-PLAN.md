---
phase: 08-mvp1-p5-end-to-end-mvp-1-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/inventory_tampa_grades.py
  - tests/refresh/test_inventory_tampa_grades.py
autonomous: true
requirements: [REQ-GRADES-01]
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Every represented 202701 Tampa course and section belongs to a sourced course-history, subject-fallback, global-fallback, or explicit invalid/unclassified evidence state.
    - A course counts as meeting literal MVP-1 grade coverage only when its sections have course or instructor-course evidence with effective_n greater than zero and matching attributed historical GradeDistribution aggregates.
    - Source, historical term, ingested timestamp and raw grade-count denominator are reported; D-20 global effective_n=0 and subject fallback never count as the course's own history.
    - Missing or inconsistent course history produces a complete remediation inventory without raw workbook rows, credentials, fabricated coverage or a scoring rewrite (D-02/D-06/D-19).
  artifacts:
    - scripts/inventory_tampa_grades.py
    - tests/refresh/test_inventory_tampa_grades.py
  key_links:
    - Represented Course and Section rows join to attributed GradeDistribution.course_id and SectionRankingCache.section_id for the same stored term.
    - The inventory returns both Phase 8 honest-behavior and literal MVP-1 per-course-grade verdict inputs.
---

<objective>
Measure actual per-course grade evidence for every represented Tampa course and enumerate the shortfall from literal MVP-1 coverage.
Purpose: REQ-GRADES-01 must be judged from attributed source aggregates and effective denominators, not from numeric fallback scores or the earlier ten-course pilot alone.
Output: Read-only full-term grade inventory and regression tests.
</objective>

<execution_context>
@/home/aatif101/.codex/gsd-core/workflows/execute-plan.md
@/home/aatif101/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-RESEARCH.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Inventory sourced history and effective denominators for every represented course</name>
  <files>scripts/inventory_tampa_grades.py, tests/refresh/test_inventory_tampa_grades.py</files>
  <read_first>scripts/validate_tampa_ingest.py; tests/refresh/test_validate_tampa_ingest.py; src/easy_a/models/core.py; src/easy_a/rankings/cache.py; src/easy_a/analytics/confidence.py; .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md</read_first>
  <behavior>For each represented 202701 course, report section count, attributed historical row and raw-grade totals, historical terms, source identifiers and timestamps, cache source split and effective-N bounds. Distinguish instructor_course/course, subject, global and invalid/unclassified states. Any course-backed cache row lacking matching attributed source evidence fails its integrity check.</behavior>
  <action>Create a read-only, term-bounded inventory CLI using existing session factory and set PostgreSQL transaction read-only. Batch the represented Course/Section, attributed GradeDistribution and cache reads rather than one query per course. Use term-plus-CRN for section identity and the existing GradeDistribution unique term/CRN/source key (D-04); sum the model's raw grade-count fields exactly as Phase 05 did, not a smoothed score (D-02). Preserve `source`, `source_hash`, `ingested_at` and historical term as provenance. Output derived per-course aggregates and a summary suitable for the final report; never emit workbook rows, credentials or invented sources (D-06/D-19). Treat no-history, subject fallback and global fallback separately, and require global effective_n=0 under D-20. Explicitly classify unexpected `score_source` or null/invalid effective-N as failure rather than silently dropping it.</action>
  <verify>
    <automated>uv run pytest tests/refresh/test_inventory_tampa_grades.py -q</automated>
    <fails_when>A fixture with absent attribution, false course evidence, a global effective N above zero, duplicate source identity, or unclassified source produces a false PASS or the tests exit nonzero.</fails_when>
  </verify>
  <acceptance_criteria>The CLI enumerates every represented course exactly once and produces sourced raw totals, provenance and the complete four-state evidence split.</acceptance_criteria>
  <done>Course-level grade coverage can be verified without relying on score presence or the pilot import record.</done>
</task>

<task type="auto" tdd="true">
  <name>Reconcile source totals and emit separate behavior and milestone verdict inputs</name>
  <files>scripts/inventory_tampa_grades.py, tests/refresh/test_inventory_tampa_grades.py</files>
  <read_first>scripts/inventory_tampa_grades.py; tests/refresh/test_inventory_tampa_grades.py; .planning/PROJECT.md; .planning/REQUIREMENTS.md; .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md</read_first>
  <behavior>The source-backed pilot totals reconcile to the authenticated Phase 05 source aggregate when the live source set is unchanged; any additional historical rows have explicit source/provenance totals. Phase 8 honest evidence handling can PASS while literal MVP-1 per-section grade coverage FAILS; neither verdict overwrites the other.</behavior>
  <action>Implement deterministic JSON summary and per-course gap records listing every course/section lacking its own sourced history, with `missing_source`, `unattributed`, `subject_only`, `global_only` or `invalid_cache` reason as appropriate. Reconcile the existing pilot's expected 179 rows and 7,544 raw grades only when the source identifiers and hashes still match the dated 05-IMPORT-RECORD; if they differ, report a changed-source comparison requiring source review, not an assumed regression. Verify exact source aggregate totals for every attributed course using persisted counts and recorded source identifiers; if external workbook totals are unavailable, label that narrower proof accurately and do not claim source-file parity. Return separate Boolean/tri-state outputs for integrity and full per-section course-grade coverage. The latter can pass only if all represented sections have attributed course/instructor-course history and effective_n>0 (D-20). Do not fetch or synthesize missing grade exports, rewrite scoring (D-02), or add raw files (D-19); the gap feeds an explicit follow-up.</action>
  <verify>
    <automated>uv run pytest tests/refresh/test_inventory_tampa_grades.py tests/refresh/test_validate_tampa_ingest.py -q</automated>
    <fails_when>A source or raw-total mismatch disappears, a missing course is omitted from the gap list, a subject/global fallback counts toward literal coverage, or existing honest-fallback tests fail.</fails_when>
  </verify>
  <acceptance_criteria>The inventory gives a complete remediation list and cannot mark MVP 1 complete while any represented section lacks attributed course history.</acceptance_criteria>
  <done>Final reporting can distinguish verified fallback behavior from unmet literal grade coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Hosted grade/cache records to reported claim | A numeric cache score is not proof of attributed source history; reconcile source and denominator. |
| Configured DB connection to report | Only derived aggregates, source identifiers and timestamps may leave the runtime. |

## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-04 | Tampering | grade evidence classification | high | mitigate | Match each course-backed cache row to attributed source rows and effective N; an unclassified or mismatched row blocks an integrity PASS. |
| T-08-05 | Information disclosure | grade ledger output | high | mitigate | Publish derived course totals/provenance only; exclude workbook rows, grade buckets, DB URLs and secrets. |
| T-08-06 | Repudiation | source reconciliation | medium | mitigate | Record source IDs/hashes, historical term, UTC observation and exact comparison basis in output. |
</threat_model>

<verification>Run focused inventory and existing Tampa validator tests. Phase 08-04 runs the inventory against hosted 202701 and records both verdict inputs with a complete course gap count.</verification>

<success_criteria>Every represented course has a sourced or explicitly missing evidence state and literal MVP-1 grade coverage cannot be inferred from subject/global fallback.</success_criteria>

## Artifacts this phase produces

- `scripts/inventory_tampa_grades.py` and focused tests.
- Derived per-course grade gap and source reconciliation output for the Phase 8 report.

## Assumptions to test

- The current `score_source` vocabulary is `instructor_course`, `course`, `subject` and `global`. Any other value is reported as unclassified and blocks evidence-integrity PASS until its semantics are proven.
- The dated Phase 05 source ledger is a comparison baseline only for unchanged source identifiers/hashes. Changed sources require a new authenticated comparison, with the limitation visible in the report.

<output>Create .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-02-SUMMARY.md when done.</output>
