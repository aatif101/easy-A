---
phase: 08-mvp1-p5-end-to-end-mvp-1-verification
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/inventory_tampa_grades.py
  - tests/refresh/test_inventory_tampa_grades.py
  - scripts/validate_tampa_ingest.py
  - tests/refresh/test_validate_tampa_ingest.py
  - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md
  - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [REQ-GRADES-01, REQ-COVERAGE-03]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every counter the D-21 inventory JSON reports under \"integrity\" (unattributed_grade_rows, bucket_sum_mismatch_rows, rows_at_or_after_term, stale_cache, non_tampa_section_count) gates verdicts.integrity and verdicts.d21_grade_coverage: any one of them nonzero or true yields FAIL/FAIL and exit code 1, so no reported integrity anomaly is silently absorbed (D-06, D-07, D-21)."
    - "A regression test seeds a real GradeDistribution row stamped at the inventoried term, runs the inventory CLI main(), and observes rows_at_or_after_term == 1, stale_cache false, no failure-state section, verdicts FAIL/FAIL and exit code 1; the test fixture helper can express both rows_at_or_after_term == 0 (PASS) and a nonzero value (FAIL), and both are exercised."
    - "stale_cache is derived only from grade rows inside the scoring evidence window (term < inventoried term, the same filter src/easy_a/analytics/queries.py applies, which includes the 202408 pilot rows): an out-of-window ingest newer than the cache refresh leaves stale_cache false, and an in-window ingest newer than the cache refresh sets it true."
    - "The validator's Phase 06 suffix-leak raise condition is unchanged (a catalog suffix course that owns 0 stored sections in the validated term still fails), and its docstring and failure message state that stored data cannot distinguish a leak into the base course from the suffix course not being offered in that term."
    - "A dated, read-only live re-run of the fixed inventory and the validator against hosted Supabase for term 202701 is recorded: verdicts PASS/PASS, sections 3,122 evidence_backed / 361 exception_no_rows / 300 exception_non_letter_grade, all five integrity counters clean, validator PASS suffix-exact, PASS reconciliation and PASS honest-coverage (300); any observed deviation is recorded as FAIL or NOT MEASURED with the observed values, never as PASS."
    - "08-VERIFICATION-REPORT.md's REQ-GRADES-01 D-21 inventory gate row references the 08-05 fixed gate and its dated re-run, and the report records the disposition of every 08-REVIEW.md finding (CR-01 fixed, WR-01 fixed, WR-02 kept and documented, IN-01 deferred, IN-02 deferred)."
    - "Scoring, cache semantics, API contract, models, migrations and frontend are unchanged (D-02 diff against origin/main is empty; web/src unchanged), and no hosted data is written: no cache rebuild, grade import, refresh or exception-list regeneration (D-21)."
  artifacts:
    - path: "scripts/inventory_tampa_grades.py"
      provides: "D-21 inventory whose integrity verdict gates every reported integrity counter, with stale_cache from scoring-window grade rows"
      contains: "evidence_grade_ingested_at_max"
    - path: "tests/refresh/test_inventory_tampa_grades.py"
      provides: "Regression tests: rows_at_or_after_term gates the verdict (unit and CLI), every reported integrity counter gates the verdict, stale_cache window semantics"
      contains: "test_every_reported_integrity_counter_gates_the_verdict"
    - path: "scripts/validate_tampa_ingest.py"
      provides: "Suffix-leak guard with unchanged raise condition and documented fail-closed ambiguity"
      contains: "not offered in term"
    - path: "tests/refresh/test_validate_tampa_ingest.py"
      provides: "Cross-term suffix guard tests (fails closed for the ambiguous term, passes for the offered term)"
      contains: "test_assert_suffix_exact_ingest_fails_closed_when_suffix_offered_only_in_another_term"
    - path: ".planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md"
      provides: "Grade-gate evidence referencing the fixed gate plus the dated 08-05 read-only re-verification"
      contains: "## Gap closure re-verification (08-05)"
    - path: ".planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md"
      provides: "Requirement-to-test map row for the 08-05 regression coverage"
      contains: "08-05"
  key_links:
    - from: "scripts/inventory_tampa_grades.py Inventory.to_dict integrity mapping"
      to: "verdicts.integrity / verdicts.d21_grade_coverage and main() exit code"
      via: "one integrity mapping is both emitted under \"integrity\" and reduced into integrity_ok, so a reported counter cannot bypass the gate"
      pattern: "integrity_ok"
    - from: "scripts/inventory_tampa_grades.py _aggregate_grade_rows"
      to: "stale_cache in collect_inventory"
      via: "evidence-window maximum ingested_at over rows with term_code < before_term, matching Term.banner_code < before_term_code in src/easy_a/analytics/queries.py"
      pattern: "evidence_grade_ingested_at_max"
    - from: "tests/refresh/test_inventory_tampa_grades.py"
      to: "scripts/inventory_tampa_grades.py main()"
      via: "seeded at-or-after-term GradeDistribution row travels through collect_inventory, to_dict and main() to FAIL/FAIL and exit 1"
      pattern: "rows_at_or_after_term"
    - from: "08-VERIFICATION-REPORT.md Gates row REQ-GRADES-01 D-21 inventory"
      to: "the 08-05 live re-run of the fixed inventory"
      via: "reason cell references the fixed gate and the new \"## Gap closure re-verification (08-05)\" section"
      pattern: "08-05"
  prohibitions:
    - requirement_id: REQ-GRADES-01
      category: values
      statement: "MUST NOT write to hosted Supabase in this plan: no cache rebuild, grade import, schedule refresh, cleanup, or regeneration of 08-D21-EXCEPTIONS.md; D-21 exceptions remain the honest end state and grade sourcing stays stopped (D-21)."
      status: resolved
      verification: test
    - requirement_id: REQ-GRADES-01
      category: values
      statement: "MUST NOT change the scoring model, cache semantics, API contract, models, migrations or frontend (D-02); only the read-only gate scripts, their tests and Phase 8 records change."
      status: resolved
      verification: test
    - requirement_id: REQ-COVERAGE-03
      category: values
      statement: "MUST NOT narrow or remove the Phase 06 suffix-leak guard's raise condition; the ambiguous zero-section suffix state keeps failing closed."
      status: resolved
      verification: test
    - requirement_id: REQ-GRADES-01
      category: transparency
      statement: "MUST NOT record the 08-05 re-verification as PASS unless the fixed inventory's verdicts and counts were actually observed in this run; drift, a FAIL verdict or an unreachable database is recorded as FAIL or NOT MEASURED with the observed values (D-06, D-07)."
      status: resolved
      verification: judgment
    - requirement_id: REQ-GRADES-01
      category: privacy
      statement: "MUST NOT commit the raw inventory JSON output, connection strings, credentials or CRN-level grade buckets (D-19)."
      status: resolved
      verification: test
---

<objective>
Close the one blocking Phase 8 verification gap (the verifier's gap truth, from 08-REVIEW.md CR-01): the D-21 grade-coverage inventory reports `rows_at_or_after_term` under `"integrity"` but never lets it gate `verdicts.integrity` / `verdicts.d21_grade_coverage`, so a nonzero value would still print PASS and exit 0. Also fix the two review warnings the user approved (WR-01 `stale_cache` window, WR-02 suffix-guard scoping), then re-verify live against hosted Supabase (read-only) and update the Phase 8 verification record so the MVP-1 verdict rests on a gate that actually enforces what it reports.

Purpose: REQ-GRADES-01's D-21 verdict (PROJECT.md D-21, locked 2026-09-23) is only as trustworthy as the inventory's gate. D-06/D-07 require that every reported integrity anomaly be a named failure, never absorbed. Today's live data has rows_at_or_after_term = 0, so the 08-04 PASS is numerically correct. The code simply does not enforce it, and no test covers a nonzero value.

Output: a fixed `scripts/inventory_tampa_grades.py` with regression tests, a documented (behavior-preserving) validator suffix guard with cross-term tests, a dated read-only live re-run recorded in `08-VERIFICATION-REPORT.md` and `08-VALIDATION.md`, and `08-05-SUMMARY.md`.

Review-finding dispositions this plan implements (user-approved scope, 2026-09-24):
- CR-01 (critical, the gap): FIX, Task 1.
- WR-01 (stale_cache from out-of-window rows): FIX, Task 2.
- WR-02 (suffix existence lookup not term-scoped): KEEP the raise condition and document it (Task 2). The reviewer's first suggestion, scoping the suffix-course lookup to courses with sections in the validated term, would make the zero-section branch unreachable and silently delete the L-variant leak guard. Course rows are catalog-scoped and the schedule response is not persisted, so there is no independent term-scoped signal. The ambiguous state therefore fails closed and is named honestly.
- IN-01 (`_environment_label` duplicated): DEFERRED, not trivial. Scripts run as files and none imports another, so deduping needs a new import convention or a src/easy_a module. The third copy lives in scripts/benchmark_rankings_search.py, whose unchanged state versus origin/main is cited as REQ-PERF-01 evidence.
- IN-02 (UI rounding of fractional effective_n): DEFERRED. It is a student-visible wording change in web/src/utils/rankings.ts that would reopen the 08-03 evidence-wording contract. The behavior is pre-existing and cosmetic, in an edge case current data does not populate.

Expected live values (sanity and the user's acceptance for Task 3): 3,783 sections; 3,122 evidence_backed / 361 exception_no_rows / 300 exception_non_letter_grade; 8,662 grade rows; cache_refreshed_at_max 2026-09-23T21:51:28.116181+00:00; grade_ingested_at_max 2026-09-23T21:36:34.907224+00:00; all five integrity counters 0/false; validator honest-coverage 300.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-REVIEW.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-02-SUMMARY.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-04-SUMMARY.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md
@scripts/inventory_tampa_grades.py
@tests/refresh/test_inventory_tampa_grades.py
@scripts/validate_tampa_ingest.py
@tests/refresh/test_validate_tampa_ingest.py

Interface notes for the executor (verified at planning time, HEAD a1f6d45):
- tests/conftest.py `db_session`: in-memory SQLite with Term id=1 "202701", id=2 "202408", id=3 "202605"; Course id=10 MAC 1105, id=11 ENC 1101.
- `_seed_evidence_backed_and_exception_no_rows` (inventory tests) adds two 202408 (term_id=2) grade rows for course 10, sections 90001 (course 10) and 90002 (course 20, MAC 2311), then runs refresh_section_rankings for 202701.
- `GradeDistribution.ingested_at` and `SectionRankingCache.refreshed_at` are DateTime(timezone=True) with server_default now(); SQLite drops tzinfo on round-trip, so compare timestamps timezone-naively in tests.
- `Inventory` is a frozen dataclass with no field defaults; `_make_inventory` (test helper) and `collect_inventory` are its only constructors.
- `_aggregate_grade_rows` currently returns a 7-tuple (key_evidence, grade_rows_by_term, window, unattributed_grade_rows, bucket_sum_mismatch_rows, overall_ingested_at_max, rows_at_or_after_term); no test calls it directly.
- Scoped baseline: the two test files pass 34 tests (22 inventory + 12 validator); full suite 366 passed / 3 skipped; mypy on the four touched Python files reports exactly one error: tests/refresh/test_inventory_tampa_grades.py:212 (`inv.os` not explicitly exported).
</context>

## Artifacts this phase produces

| Artifact | Change | Produced by |
|---|---|---|
| `scripts/inventory_tampa_grades.py` | modified: integrity gate covers every reported counter (CR-01); `stale_cache` from scoring-window rows plus new `snapshot.evidence_grade_ingested_at_max` (WR-01); docstrings | Tasks 1, 2 |
| `tests/refresh/test_inventory_tampa_grades.py` | modified: `_make_inventory` overrides, `_add_grade` optional `ingested_at`, 6 new tests, touched-file mypy fix | Tasks 1, 2 |
| `scripts/validate_tampa_ingest.py` | modified: docstring, comment and failure message only; raise condition unchanged (WR-02) | Task 2 |
| `tests/refresh/test_validate_tampa_ingest.py` | modified: `_add_section` `term_id` kwarg, 2 new cross-term tests | Task 2 |
| `.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md` | modified: grade-gate rows reference the fixed gate; new "## Gap closure re-verification (08-05)"; verdict note | Task 3 |
| `.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md` | modified: one 08-05 row in the requirement map | Task 3 |
| `.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-05-SUMMARY.md` | new: execution summary with RED/GREEN evidence, live re-run values, dispositions | output step |

Not produced or changed: `08-D21-EXCEPTIONS.md` (not regenerated), `scripts/verify_rankings_pages.py`, `scripts/benchmark_rankings_search.py`, anything under `src/`, `migrations/`, `web/`, `.planning/WINDOWS.md`, and hosted Supabase data.

## Source coverage audit

| Source | Item | Coverage |
|---|---|---|
| GOAL | Phase 8: "MVP 1 is demonstrably met end to end". The D-21 verdict must rest on a gate that enforces every integrity counter it reports | Tasks 1-3 |
| REQ | REQ-GRADES-01: the gap's requirement; the inventory gate plus live re-run | Tasks 1, 2, 3 |
| REQ | REQ-COVERAGE-03: WR-02 touches its validator suffix-exact gate; live validator re-run re-confirms suffix-exact and reconciliation | Tasks 2, 3 |
| REQ | REQ-PERF-01: not affected. No search/serve/benchmark code changes, so the 08-04 p95 evidence stands; Task 3 proves the benchmark script is unchanged | n/a (Task 3 invariance check) |
| VERIFICATION gap `missing` 1 | rows_at_or_after_term must equal 0 inside integrity_ok | Task 1 |
| VERIFICATION gap `missing` 2 | Regression test seeding a row with term_code >= before_term, asserting FAIL/FAIL | Task 1 |
| VERIFICATION gap `missing` 3 | Live re-run after the fix, recorded in a follow-up SUMMARY | Task 3 + 08-05-SUMMARY.md |
| User scope | Fix the fixture helper that hardcodes rows_at_or_after_term = 0 | Task 1 |
| REVIEW | CR-01 / WR-01 / WR-02 / IN-01 / IN-02 | T1 / T2 / T2 (keep + document) / deferred with reason / deferred with reason |
| CONTEXT | No 08-CONTEXT.md exists. Locked PROJECT.md decisions honored: D-02, D-06, D-07, D-10, D-19, D-20, D-21 | cited in task actions |
| RESEARCH | 08-RESEARCH.md items were planned and completed by 08-01..08-04. This gap plan introduces no new research item | n/a |

<tasks>

<task type="tracer">
  <name>Task 1: Tracer - one seeded at-or-after-term grade row travels DB to collect_inventory to to_dict to main() and yields FAIL/FAIL, exit 1 (CR-01)</name>
  <reversibility rating="reversible">One derived boolean in one read-only CLI and its tests; reverting restores the prior gate, and no stored data or published contract depends on it.</reversibility>
  <files>scripts/inventory_tampa_grades.py, tests/refresh/test_inventory_tampa_grades.py</files>
  <read_first>scripts/inventory_tampa_grades.py (module docstring lines 1-18; Inventory and to_dict lines 147-253; _aggregate_grade_rows lines 455-599; collect_inventory lines 602-680; main lines 818-843); tests/refresh/test_inventory_tampa_grades.py (_add_grade lines 51-92, _seed_evidence_backed_and_exception_no_rows lines 95-110, test_main_prints_snapshot_json_and_exits_nonzero_when_unclassified_present lines 145-177, _make_inventory lines 394-413, test_d21_grade_coverage_pass_when_only_evidence_backed_and_exceptions lines 416-445); tests/conftest.py (db_session terms and courses); .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-REVIEW.md (CR-01)</read_first>
  <behavior>
    - A clean inventory (only evidence_backed and listed-exception sections, every integrity counter 0/false) is PASS/PASS; the same inventory with rows_at_or_after_term = 3 is FAIL/FAIL.
    - For every key the JSON reports under "integrity", setting that single Inventory field dirty (1 for int, True for bool) turns both verdicts FAIL.
    - CLI end to end: a real GradeDistribution row on term 202701 (equal to the inventoried term), with stale_cache pinned false, makes main(["--term", "202701"]) print integrity.rows_at_or_after_term == 1, verdicts FAIL/FAIL and return 1, with no failure-state section present.
  </behavior>
  <action>Close CR-01 end to end per D-06, D-07 and D-21: a reported integrity anomaly must never be absorbed into a PASS.

Tests first (RED), in tests/refresh/test_inventory_tampa_grades.py:
(a) Fix the fixture helper that hardcodes rows_at_or_after_term = 0. Change `_make_inventory(sections)` to accept keyword overrides: build exactly the clean Inventory it builds today, then return `dataclasses.replace(inventory, **overrides)`, so a caller can set rows_at_or_after_term (or any other Inventory field). Existing callers stay unchanged.
(b) Add an optional keyword `ingested_at: datetime | None = None` to `_add_grade`, passed to the GradeDistribution constructor only when given, so the server default still applies otherwise.
(c) Add a small helper `_pin_timestamps(session, *, refreshed_at, ingested_at)`. It bulk-updates every SectionRankingCache.refreshed_at and every existing GradeDistribution.ingested_at with SQLAlchemy `update(...)` statements, then commits. Define module constant `T0 = datetime(2026, 9, 23, 21, 51, tzinfo=UTC)`.
(d) Add `test_rows_at_or_after_term_gates_integrity_verdict`, parametrized over exactly two cases, `(0, "PASS")` and `(3, "FAIL")`. Build the three clean sections that `test_d21_grade_coverage_pass_when_only_evidence_backed_and_exceptions` builds (evidence_backed, exception_no_rows, exception_non_letter_grade) and call `_make_inventory(..., rows_at_or_after_term=value)`. Assert that `output["integrity"]["rows_at_or_after_term"]` equals the value and that `output["verdicts"]` equals `{"integrity": expected, "d21_grade_coverage": expected}`.
(e) Add `test_every_reported_integrity_counter_gates_the_verdict`. Build a clean inventory and assert it is PASS/PASS. Then, for every key of `to_dict()["integrity"]`:
   - assert the key is an Inventory field name (`dataclasses.fields(inv.Inventory)`);
   - build `dataclasses.replace(clean, **{key: dirty})`, where dirty is True if the clean value is a bool and 1 otherwise;
   - assert both verdicts are FAIL, including the key in the assertion message.
   This test fails automatically if a future counter is reported without being gated.
(f) Add `test_main_exits_nonzero_when_grade_row_at_or_after_term_exists`:
   - Seed with `_seed_evidence_backed_and_exception_no_rows`.
   - Call `_pin_timestamps` with refreshed_at=T0 and ingested_at=T0 minus one hour, so stale_cache cannot explain a FAIL either before or after Task 2.
   - Add one attributed GradeDistribution row for course 10 on term_id=1 (banner 202701), with crn "80003", source "synthetic-at-term" and ingested_at=datetime(2020, 1, 1, tzinfo=UTC). Commit.
   - Reuse the monkeypatch pattern from `test_main_prints_snapshot_json_and_exits_nonzero_when_unclassified_present`: get_engine returns the session's bind, and engine.dispose becomes a no-op. Call `inv.main(["--term", TERM])` and json-parse captured stdout.
   - Assert:
     - exit code 1;
     - `integrity.rows_at_or_after_term == 1`;
     - `integrity.stale_cache is False`;
     - unattributed_grade_rows, bucket_sum_mismatch_rows and non_tampa_section_count are all 0;
     - `sections_by_state` keys are exactly {"evidence_backed", "exception_no_rows"}, so no failure state explains the FAIL;
     - verdicts are FAIL/FAIL.
Run the three new tests against the unfixed script. They must fail: the (3, "FAIL") case and the main() test read PASS / exit 0, and the counter loop fails on rows_at_or_after_term. Save that RED output for the SUMMARY (workflow.tdd_mode is false, so no automated RED gate runs; pytest's per-test output is the evidence).

Fix (GREEN), in scripts/inventory_tampa_grades.py `Inventory.to_dict`:
- Build the integrity mapping once as a local, keeping its five keys in their current order: unattributed_grade_rows, bucket_sum_mismatch_rows, rows_at_or_after_term, stale_cache, non_tampa_section_count.
- Emit that same local under "integrity".
- Derive integrity_ok as `failure_section_count == 0` and no value in that mapping truthy. This enforces the verifier's missing item (rows_at_or_after_term must be 0) and makes it structurally impossible to report a counter that does not gate.
- Do not change main()'s exit-code logic, classify_section, the JSON key names or order, or _not_measured.
- Update the module docstring and a short comment at the integrity mapping. State that every integrity counter gates both verdicts, and that rows_at_or_after_term > 0 (grade rows stamped at or after the inventoried term) is a named integrity failure. Also state that the gate is intended for a term with no grade history yet, the upcoming term: run against a past term that has later grade rows, it reports integrity FAIL by design.</action>
  <verify>
    <automated>uv run pytest "tests/refresh/test_inventory_tampa_grades.py::test_rows_at_or_after_term_gates_integrity_verdict" "tests/refresh/test_inventory_tampa_grades.py::test_every_reported_integrity_counter_gates_the_verdict" "tests/refresh/test_inventory_tampa_grades.py::test_main_exits_nonzero_when_grade_row_at_or_after_term_exists" -q</automated>
    <fails_when>non-zero exit (includes "ERROR: not found" when a named test does not exist), or a summary line other than "4 passed"</fails_when>
    <automated>uv run pytest tests/refresh/test_inventory_tampa_grades.py -q</automated>
    <fails_when>non-zero exit, any "failed" or "error" in the summary line, or fewer than 26 passed (22 existing + 4 new)</fails_when>
    <automated>uv run ruff check scripts/inventory_tampa_grades.py tests/refresh/test_inventory_tampa_grades.py && uv run mypy scripts/inventory_tampa_grades.py</automated>
    <fails_when>non-zero exit; ruff prints any rule violation or mypy's last line is not "Success: no issues found in 1 source file"</fails_when>
  </verify>
  <acceptance_criteria>
    - The three named tests exist and pass; the parametrized test covers exactly the cases (0, "PASS") and (3, "FAIL").
    - `_make_inventory` accepts keyword overrides; no caller or helper in the test file fixes rows_at_or_after_term at 0 without allowing an override.
    - The CLI test proves the FAIL comes from rows_at_or_after_term alone: stale_cache false, the other three counters 0, and no failure-state key in sections_by_state.
    - In `Inventory.to_dict`, the mapping emitted under "integrity" is the same object whose values integrity_ok checks (one definition, not two lists).
    - The saved RED output shows the new tests failing before the fix; the SUMMARY records it.
    - All 22 pre-existing tests in the file still pass unchanged.
  </acceptance_criteria>
  <done>A nonzero rows_at_or_after_term, or any other reported integrity counter, now makes the D-21 inventory print FAIL/FAIL and exit 1, proven end to end through main() on a seeded database and by an invariant test over every reported counter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stale_cache from scoring-window rows only (WR-01); suffix guard kept and documented as fail-closed (WR-02); touched-file mypy fix</name>
  <reversibility rating="reversible">Adds one additive snapshot field and changes the input of one derived boolean, plus docstring and message text in the validator; the raise condition and every query are untouched, and reverting is a local edit.</reversibility>
  <files>scripts/inventory_tampa_grades.py, tests/refresh/test_inventory_tampa_grades.py, scripts/validate_tampa_ingest.py, tests/refresh/test_validate_tampa_ingest.py</files>
  <read_first>scripts/inventory_tampa_grades.py (_aggregate_grade_rows lines 455-599 as left by Task 1; collect_inventory stale_cache lines 643-651); src/easy_a/analytics/queries.py (lines 321-332 and 520-530: the Term.banner_code < before_term_code scoring filter); scripts/validate_tampa_ingest.py (assert_suffix_exact_ingest lines 49-100); tests/refresh/test_validate_tampa_ingest.py (_add_section lines 34-49, _seed_honest_dataset lines 93-105, suffix tests lines 128-142); src/easy_a/models/core.py (Course has no term_id); src/easy_a/refresh/coverage.py (refresh_targets: Course rows come from the catalog, not per term); .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-REVIEW.md (WR-01, WR-02)</read_first>
  <behavior>
    - Out-of-window ingest: cache refreshed at T0, in-window (202408) rows ingested at T0-1h, one 202701 row ingested at T0+1h. Expect stale_cache false; rows_at_or_after_term 1; verdict FAIL (from rows_at_or_after_term); snapshot.grade_ingested_at_max is T0+1h; snapshot.evidence_grade_ingested_at_max is T0-1h.
    - In-window newer ingest: same setup, with one 202408 row re-stamped to T0+1h. Expect stale_cache true; rows_at_or_after_term 0; verdicts FAIL/FAIL.
    - Validator cross-term: CHM 2045 has a section in 202701 and CHM 2045L has a section only in 202605. Validating 202701 raises AssertionError naming 2045L, 202701, "leaked into" and "not offered in term"; validating 202605 does not raise.
  </behavior>
  <action>WR-01: stale_cache must reflect only the evidence the cache's scores were built from (D-21, D-07).
In scripts/inventory_tampa_grades.py `_aggregate_grade_rows`:
- Keep `overall_ingested_at_max` updated for every fetched row exactly as now. It feeds snapshot.grade_ingested_at_max, which 08-04 used as a snapshot-drift field, so its meaning must not change.
- Add a second running maximum, `evidence_ingested_at_max`, updated only after the `term_code >= before_term` guard has let the row through. That is, only for rows with term_code < before_term, the same filter src/easy_a/analytics/queries.py applies (`Term.banner_code < before_term_code`).
- This deliberately includes the 202408 pilot rows and any other historical term outside the D-21 reporting window, because those rows do feed the cached scores. Do not use D21_WINDOW_TERMS here.
- Return the new maximum as an additional element and update the docstring's Returns line.
Then:
- Add Inventory field `evidence_grade_ingested_at_max: str | None` directly after grade_ingested_at_max.
- Populate it in collect_inventory as an ISO string or None.
- Emit it in the "snapshot" block as "evidence_grade_ingested_at_max", directly after "grade_ingested_at_max".
- Compute stale_cache as cache_refreshed_at_max strictly earlier than the evidence maximum. When either side is None, stale_cache is False, as today.
- Update `_make_inventory` in the test file to pass `evidence_grade_ingested_at_max=None`.
Tests, written first (the first must fail before the change):
- `test_stale_cache_ignores_out_of_window_grade_ingest`:
  - Seed with `_seed_evidence_backed_and_exception_no_rows`, then run `_pin_timestamps` (refreshed_at=T0, ingested_at=T0 minus one hour).
  - Add a term_id=1 (202701) grade row for course 10 with ingested_at T0 plus one hour and commit. Call `inv.collect_inventory`.
  - Assert stale_cache is False, rows_at_or_after_term == 1 and verdicts FAIL/FAIL.
  - Assert the snapshot's grade_ingested_at_max equals T0 plus one hour and evidence_grade_ingested_at_max equals T0 minus one hour. Compare with `datetime.fromisoformat(value).replace(tzinfo=None)` against the naive expected value, because SQLite drops tzinfo.
- `test_stale_cache_trips_on_newer_in_window_grade_ingest`:
  - Same seed and pinning, then set the ingested_at of the 202408 (term_id=2) row with crn "80001" to T0 plus one hour, simulating a re-import after the cache build. Commit and collect.
  - Assert stale_cache is True, rows_at_or_after_term == 0 and verdicts FAIL/FAIL.
  - Give the test a docstring noting that 202408 is outside the D-21 reporting window but inside the scoring evidence window, so it must count.

WR-02: keep the Phase 06 suffix-leak raise condition. Change only its documentation and message (D-06, D-07).
In scripts/validate_tampa_ingest.py `assert_suffix_exact_ingest`, do not change any query, condition, loop, `continue` or the order of checks. Change only:
(1) The docstring. Explain that the suffix Course lookup is catalog-scoped (Course has no term) while the section count is term-scoped. The schedule response is not persisted, so stored data has no other term-scoped signal. Scoping the lookup to courses that own sections in the term would make the zero-section branch unreachable and remove the guard. A catalog suffix course that owns 0 sections in the validated term is therefore ambiguous between a leak into the base course and not being offered that term, and it fails closed.
(2) A one-line comment above the suffix_course_id lookup saying the lookup is intentionally catalog-wide.
(3) The AssertionError message. Keep the leading "{subject} {suffix_number}:" and the term. State both possibilities: its sections "leaked into" {subject} {base_number}, or the suffix course is "not offered in term" {normalized_term}. Tell the operator to confirm against the USF schedule for that term.
Do not add new skip conditions, such as skipping when the base course has no sections. Every added skip narrows the guard.
In tests/refresh/test_validate_tampa_ingest.py:
- Extend `_add_section` with `term_id: int = 1`.
- Add `test_assert_suffix_exact_ingest_fails_closed_when_suffix_offered_only_in_another_term`: CHM 2045 (id 1001) has one section in term_id 1; CHM 2045L (id 1002) has one section only in term_id 3 (202605). Calling assert_suffix_exact_ingest for "202701" with PAIR raises AssertionError. Capture it with `pytest.raises(...) as excinfo` and assert the text contains "2045L", "202701", "leaked into" and "not offered in term".
- Add `test_assert_suffix_exact_ingest_passes_for_the_term_the_suffix_is_offered_in`: same seed; validating "202605" does not raise, because the count stays term-scoped.
- `test_assert_suffix_exact_ingest_raises_on_leaked_suffix_section` and `test_assert_suffix_exact_ingest_passes_on_honest_dataset` must keep passing unmodified. They prove the leak detection is unchanged.

Touched-file mypy (the inventory-test part of .planning/WINDOWS.md entry 9): `test_write_atomic_leaves_target_absent_on_simulated_failure` patches `inv.os`, which mypy rejects as a non-exported attribute. Import os at the top of the test module and monkeypatch `os.replace` directly. `inv.os` is the same module object, so behavior is identical. Do not edit tests/api/test_verify_rankings_pages.py or .planning/WINDOWS.md; entry 9 stays open for that other file.</action>
  <verify>
    <automated>uv run pytest "tests/refresh/test_inventory_tampa_grades.py::test_stale_cache_ignores_out_of_window_grade_ingest" "tests/refresh/test_inventory_tampa_grades.py::test_stale_cache_trips_on_newer_in_window_grade_ingest" "tests/refresh/test_validate_tampa_ingest.py::test_assert_suffix_exact_ingest_fails_closed_when_suffix_offered_only_in_another_term" "tests/refresh/test_validate_tampa_ingest.py::test_assert_suffix_exact_ingest_passes_for_the_term_the_suffix_is_offered_in" "tests/refresh/test_validate_tampa_ingest.py::test_assert_suffix_exact_ingest_raises_on_leaked_suffix_section" -q</automated>
    <fails_when>non-zero exit (includes "ERROR: not found" for a missing test name), or a summary line other than "5 passed"</fails_when>
    <automated>uv run pytest tests/refresh/test_inventory_tampa_grades.py tests/refresh/test_validate_tampa_ingest.py -q</automated>
    <fails_when>non-zero exit, any "failed" or "error" in the summary line, or fewer than 42 passed (34 baseline + 8 new across Tasks 1-2)</fails_when>
    <automated>uv run ruff check scripts/inventory_tampa_grades.py scripts/validate_tampa_ingest.py tests/refresh/test_inventory_tampa_grades.py tests/refresh/test_validate_tampa_ingest.py && uv run mypy scripts/inventory_tampa_grades.py scripts/validate_tampa_ingest.py tests/refresh/test_inventory_tampa_grades.py tests/refresh/test_validate_tampa_ingest.py</automated>
    <fails_when>non-zero exit; ruff prints any violation, or mypy's last line is not "Success: no issues found in 4 source files"</fails_when>
  </verify>
  <acceptance_criteria>
    - `stale_cache` in collect_inventory compares against the evidence-window maximum. `snapshot.grade_ingested_at_max` still reflects every grade row, and `snapshot.evidence_grade_ingested_at_max` is emitted directly after it.
    - The evidence-window maximum is updated only for rows with term_code < before_term; D21_WINDOW_TERMS plays no part in it.
    - In `assert_suffix_exact_ingest`, every select statement, if-condition, loop and `continue` is identical to HEAD a1f6d45. Only the docstring, one comment and the AssertionError message text differ.
    - The validator failure message contains the substrings "leaked into" and "not offered in term" and still starts with "{subject} {suffix_number}:".
    - mypy on the four touched Python files reports no issues; tests/api/test_verify_rankings_pages.py and .planning/WINDOWS.md are unmodified.
    - The out-of-window stale_cache test and the validator message test were observed failing before their fixes; the SUMMARY records it.
  </acceptance_criteria>
  <done>An out-of-window grade ingest can no longer set stale_cache, and an in-window one still does. The suffix guard's leak detection is byte-for-byte unchanged in logic and now names its ambiguous case honestly. Both are covered by tests, and the touched test files are mypy-clean.</done>
</task>

<task type="auto">
  <name>Task 3: Read-only live re-verification of the fixed gates against hosted Supabase and record it in the Phase 8 verification report</name>
  <precondition>DATABASE_URL (environment or the project's .env; never printed) points at hosted Supabase and accepts a read-only connection; if it does not, stop and report NOT MEASURED instead of recording any live PASS.</precondition>
  <reversibility rating="reversible">Read-only commands plus scoped edits to two Phase 8 markdown records; no hosted data or contract changes.</reversibility>
  <files>.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md, .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md</files>
  <read_first>.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md (Gates table, "## D-21 grade coverage", "## Regression and invariance", "## Verdicts"); .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md (requirement map rows at lines 40-46); .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-REVIEW.md (finding IDs for the disposition list); scripts/inventory_tampa_grades.py (the Task 1-2 JSON shape); .planning/PROJECT.md (D-02, D-10, D-19, D-21)</read_first>
  <action>Run a read-only live re-verification (D-21: coverage work stays stopped; this plan writes nothing to hosted data). Do not pass --exceptions-md, and do not run any refresh, import, cache-rebuild or cleanup script. No API server is needed.
1. Record the UTC start time, the output of git rev-parse HEAD, and the branch.
2. Run the fixed inventory once with its stdout redirected to a JSON file outside the git worktree (under $TMPDIR, or your session scratchpad). Its exception list has hundreds of entries, so do not print it into context, and never commit it (D-19). Extract environment, observed_at_utc, the snapshot block (including evidence_grade_ingested_at_max), the five integrity counters, sections_by_state, exception_sections_by_reason, evidence_backed_sections and verdicts.
   Expected:
   - exit 0, environment Supabase;
   - integrity PASS and d21_grade_coverage PASS;
   - all five counters 0/false;
   - sections_by_state exactly evidence_backed 3,122, exception_no_rows 361, exception_non_letter_grade 300;
   - section_count 3,783 and grade_row_count 8,662;
   - cache_refreshed_at_max and grade_ingested_at_max equal the 08-04 values in this plan's objective;
   - evidence_grade_ingested_at_max equal to grade_ingested_at_max, because every stored grade row precedes 202701.
   If any value differs, record the observed value and the delta. Mark the gate FAIL (or NOT MEASURED on exit 3), do not substitute the expected value, do not repair hosted data, and make it the headline of the SUMMARY (D-06, D-07).
3. Run the validator. Expect exactly three lines: PASS suffix-exact, PASS reconciliation, and PASS honest-coverage (verified non-letter-grade exceptions: 300). The 300 must equal the inventory's exception_non_letter_grade count.
4. Run the full Python suite, then ruff and mypy on the four touched Python files. Record the counts.
5. Confirm invariance:
   - git fetch origin main (D-10);
   - the D-02 diff against origin/main is empty;
   - 08-D21-EXCEPTIONS.md, scripts/verify_rankings_pages.py, scripts/benchmark_rankings_search.py and web/src are unchanged since the plan-start commit a1f6d45 (this also proves IN-01 and IN-02 were not applied);
   - no spreadsheet or CSV export is tracked (D-19).
6. Edit 08-VERIFICATION-REPORT.md with scoped Edit calls only (never rewrite the whole file):
   a. Gates table. In the "REQ-GRADES-01 D-21 inventory" row, keep the 08-04 values. Append to its Reason cell: re-confirmed at the observed UTC time against the 08-05 fixed gate (every reported integrity counter gates both verdicts; stale_cache from scoring-window rows), the observed verdict, the 3,122 / 361 / 300 counts, and a pointer to "## Gap closure re-verification (08-05)". Add a short "re-confirmed (08-05)" note with the UTC time to the "REQ-COVERAGE-03 validator" and "REQ-GRADES-01 validator honest-coverage" rows.
   b. Insert a new section "## Gap closure re-verification (08-05)" immediately before "## Verdicts". It must contain:
      - the UTC window and HEAD SHA;
      - what changed: CR-01, which closes the Phase 8 verification gap; WR-01; and the WR-02 disposition (raise condition kept, and why);
      - the exact commands run;
      - a table of observed live values: snapshot fields including evidence_grade_ingested_at_max, the five integrity counters, sections_by_state, verdicts, and the three validator lines;
      - a delta column against the 08-04 values;
      - scoped and full test counts, ruff and mypy results, and the invariance results;
      - the disposition of all five 08-REVIEW.md findings, with IN-01 and IN-02 recorded as deferred together with the reasons given in this plan's objective;
      - an explicit statement that no hosted data was written and 08-D21-EXCEPTIONS.md was not regenerated.
   c. In "## Verdicts", add one sentence after the MVP-1 requirement table: the REQ-GRADES-01 verdict now rests on the fixed gate, re-confirmed live in 08-05. If step 2 or 3 deviated, instead change the affected verdict to FAIL or NOT MEASURED with the observed values. Never keep PASS against contrary evidence.
7. Edit 08-VALIDATION.md. Add one requirement-map row for 08-05: REQ-GRADES-01 (every reported integrity counter gates the verdict; stale_cache from scoring-window rows; REQ-COVERAGE-03 suffix guard fails closed on an ambiguous zero-section suffix course). Use the scoped pytest command over the two test files, with status "Yes - closed 08-05 (N tests); live PASS re-confirmed 08-05", where N is the observed count.
Write derived aggregates only. Neither file may contain connection strings, credentials, raw JSON or grade buckets (D-19).</action>
  <verify>
    <automated>OUT="${TMPDIR:-/tmp}/easy-a-08-05-inventory.json" && uv run python scripts/inventory_tampa_grades.py --term 202701 > "$OUT" && uv run python -c 'import json,sys; d=json.load(open(sys.argv[1])); print(json.dumps({k: d[k] for k in ("environment","snapshot","integrity","sections_by_state","exception_sections_by_reason","verdicts")}, indent=1)); ok = d["verdicts"]=={"integrity":"PASS","d21_grade_coverage":"PASS"} and d["sections_by_state"]=={"evidence_backed":3122,"exception_no_rows":361,"exception_non_letter_grade":300} and not any(d["integrity"].values()) and d["snapshot"]["evidence_grade_ingested_at_max"]==d["snapshot"]["grade_ingested_at_max"]; sys.exit(0 if ok else 1)' "$OUT"</automated>
    <fails_when>non-zero exit: inventory exit 1 (a FAIL verdict) or 3 (NOT MEASURED) stops the chain, or the checker exits 1 because a verdict is not PASS, a D-21 count differs from 3,122 / 361 / 300, an integrity counter is nonzero/true, or the evidence and overall ingested_at maxima differ</fails_when>
    <automated>uv run python scripts/validate_tampa_ingest.py --term 202701 --targets config/course_targets.toml</automated>
    <fails_when>non-zero exit, any stdout line starting "FAIL ", or no line reading exactly "PASS honest-coverage (verified non-letter-grade exceptions: 300)"</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, "failed" or "error" in the summary line, or fewer than 374 passed (366 baseline + 8 new)</fails_when>
    <automated>git fetch origin main && git diff --quiet origin/main -- src/easy_a/analytics src/easy_a/rankings src/easy_a/api src/easy_a/models migrations web/src/types web/src/api && git diff --quiet a1f6d45 -- .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-D21-EXCEPTIONS.md scripts/verify_rankings_pages.py scripts/benchmark_rankings_search.py web/src && test -z "$(git ls-files -- '*.xlsx' '*.xls' '*.csv')"</automated>
    <fails_when>non-zero exit: a D-02 path differs from origin/main, the exception list, API-identity scanner, benchmark or web/src changed since a1f6d45, or a spreadsheet/CSV export is tracked</fails_when>
    <automated>R=.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md; grep -q "^## Gap closure re-verification (08-05)" "$R" && grep -F "| REQ-GRADES-01 D-21 inventory |" "$R" | grep -q "08-05" && ! grep -q -E "postgresql://|pooler\.supabase\.com" "$R" && grep -q "08-05" .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md</automated>
    <fails_when>non-zero exit: the new section heading is missing, the D-21 inventory gate row does not mention 08-05, the report contains a connection-string or pooler-host substring, or 08-VALIDATION.md has no 08-05 row</fails_when>
  </verify>
  <acceptance_criteria>
    - 08-VERIFICATION-REPORT.md contains the heading "## Gap closure re-verification (08-05)" before "## Verdicts". The section has the UTC window, HEAD SHA, commands, observed values with deltas against 08-04, test/lint/type results, invariance results and all five review-finding dispositions.
    - The "REQ-GRADES-01 D-21 inventory" Gates row keeps its 08-04 evidence and adds the 08-05 fixed-gate re-confirmation with UTC time and observed verdict.
    - The observed live values are recorded as observed. If they matched: PASS/PASS, 3,122 / 361 / 300, counters clean, validator 300. If they differed: FAIL or NOT MEASURED with the values.
    - 08-D21-EXCEPTIONS.md is byte-identical to a1f6d45, and the inventory JSON file is not inside the repository.
    - Neither edited file contains "postgresql://", "pooler.supabase.com", a credential, or any a_count..w_count column.
    - 08-VALIDATION.md has an 08-05 row with the observed test count.
  </acceptance_criteria>
  <done>The fixed D-21 gate and the validator were re-run read-only against hosted Supabase, and the results were recorded with dates and deltas. The Phase 8 report's grade-gate evidence now references the fixed gate, so the MVP-1 verdict rests on a gate that enforces every integrity counter it reports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate script to verdict consumer | The inventory's PASS/FAIL JSON is trusted by the Phase 8 report and the MVP-1 decision; a false PASS certifies unverified data as honest |
| executor to hosted Supabase | Live re-verification crosses into the production database; only reads are permitted |
| CLI stdout to repository | Inventory JSON carries per-section identifiers; it must stay outside Git (D-19) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-01 | Tampering (false-PASS integrity verdict) | scripts/inventory_tampa_grades.py Inventory.to_dict | high | mitigate | integrity_ok is derived from the same mapping emitted under "integrity" (Task 1); test_every_reported_integrity_counter_gates_the_verdict fails if any reported counter stops gating; CLI regression test proves exit 1 |
| T-08-05-02 | Tampering (production data) | Task 3 live re-verification | high | mitigate | Only read-only commands: the inventory runs in one REPEATABLE READ READ ONLY transaction and the validator issues only SELECTs. No --exceptions-md, refresh, import, cleanup or cache-rebuild script is run. The Task 3 verify proves 08-D21-EXCEPTIONS.md is unchanged since a1f6d45 |
| T-08-05-03 | Information disclosure | inventory JSON stdout, report edits | medium | mitigate | JSON redirected to a file outside the worktree and never committed; report records derived aggregates only; the environment label is the sanitized Supabase/SQLite/Postgres (other host) string; Task 3 verify rejects connection-string and pooler-host substrings (D-19) |
| T-08-05-04 | Tampering (validation weakened) | scripts/validate_tampa_ingest.py assert_suffix_exact_ingest | medium | mitigate | Raise condition, queries and loop unchanged (Task 2 acceptance); existing leak test kept unmodified; new cross-term test pins fail-closed behavior for the ambiguous state |
| T-08-05-05 | Tampering (stale cache hidden or falsely flagged) | stale_cache in collect_inventory | medium | mitigate | Evidence-window maximum matches the scoring filter in src/easy_a/analytics/queries.py; two tests pin both directions (out-of-window ignored, in-window 202408 counted) |
| T-08-05-06 | Information disclosure | git fetch origin main (D-10) | low | accept | Updates remote-tracking refs only; no credential is printed or recorded |
</threat_model>

<verification>
- Scoped: `uv run pytest tests/refresh/test_inventory_tampa_grades.py tests/refresh/test_validate_tampa_ingest.py -q` gives at least 42 passed, 0 failed.
- Full: `uv run pytest -q` gives at least 374 passed, 3 skipped, 0 failed (the PostgreSQL integration tests skip without EASY_A_TEST_POSTGRES_URL and are reported as skipped, not passed).
- `uv run ruff check` and `uv run mypy` on the four touched Python files are clean.
- Live read-only: the inventory reports PASS/PASS with 3,122 / 361 / 300 and clean counters, and the validator prints three PASS lines with honest-coverage 300. Otherwise the report records FAIL or NOT MEASURED with the observed values.
- D-02 diff empty against origin/main. 08-D21-EXCEPTIONS.md, scripts/verify_rankings_pages.py, scripts/benchmark_rankings_search.py and web/src are unchanged since a1f6d45. D-19 check passes.
</verification>

<success_criteria>
- The Phase 8 verification gap is closed: every integrity counter the D-21 inventory reports gates both verdicts and the exit code, with unit, invariant and CLI end-to-end regression tests.
- WR-01 is fixed with tests in both directions. WR-02 is dispositioned (raise condition kept, ambiguity documented, cross-term tests). IN-01 and IN-02 are deferred with recorded reasons.
- The hosted Supabase re-run is dated and recorded, and 08-VERIFICATION-REPORT.md's grade-gate evidence references the fixed gate.
- No hosted data written; no scoring, cache, API, model, migration or frontend change.
</success_criteria>

<output>
Create `.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-05-SUMMARY.md` when done. Include:
- RED and GREEN evidence for Tasks 1 and 2;
- the live re-run values with UTC times and deltas against 08-04;
- the five review-finding dispositions (CR-01 fixed, WR-01 fixed, WR-02 kept and documented with rationale, IN-01 and IN-02 deferred with reasons);
- a note that .planning/WINDOWS.md entry 9 is now partially addressed (the inventory-test error is fixed) and stays open for tests/api/test_verify_rankings_pages.py.
</output>
