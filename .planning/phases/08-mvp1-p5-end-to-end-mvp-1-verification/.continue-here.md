---
context: phase
phase: 08-mvp1-p5-end-to-end-mvp-1-verification
task: planning-gate
total_tasks: 0
status: paused
last_updated: 2026-09-23T18:15:36.984Z
---

# Phase 8 checkpoint — wait for sourced grade coverage

## Blocking constraint discovered by the plan checker

- [ ] **Do not execute or close Phase 8 from the current draft plans.** The Phase 8 goal is to demonstrate MVP 1 end to end, while the latest dated snapshot has sourced own-course grade history for only 132 of 3,783 sections (10 of 1,401 represented courses). A subject fallback is not that course's history; a global prior has `effective_n=0`. Recheck these figures against the live hosted database before judging completion.

This is an observed planning failure, not a hypothetical warning: the independent checker returned a `requirement_coverage` blocker. The checker also flagged unresolved research headings; that second issue was addressed in `08-RESEARCH.md` under “Question dispositions and external dependencies.”

<current_state>

Phase 7 is complete and merged in the verified `origin/main` at `4e4b944`. Phase 8 has four draft plans (`08-01` through `08-04`), plus research, pattern map and validation strategy. Planning was paused before the requirements coverage gate, STATE.md planned-phase transition, roadmap annotation or final plan commit. No Phase 8 plan has been executed.

The user is obtaining approved aggregate historical grade reports through an authenticated Codex browser session. Their intended loop is at most 20 **courses** per batch, saving each successful course import into the live Supabase `grade_distributions` table, then rebuilding the Spring 2027 `section_rankings` cache once per batch. This activity is external to this session; no completion evidence or local async-job manifest exists yet. Do not claim a batch has completed until the live database and provenance ledger prove it.

</current_state>

<completed_work>

- Phase 8 research, validation strategy, pattern map and four executable draft plans were written in this directory.
- Deterministic command-path and failure-signal probes found zero issues across 19 planned commands.
- The independent plan checker found two blockers. The research question disposition was fixed. The goal/grade-coverage blocker remains.
- Current working branch `codex/phase7-plan` was fast-forwarded to verified `origin/main` without changing branches. Existing unrelated untracked files were preserved.

</completed_work>

<remaining_work>

1. Continue bounded, authenticated grade sourcing and per-course imports. Confirm the actual historical Banner term for each export. Keep raw XLSX files and row-level derivatives out of Git. Record explicit unavailable, failed and completed courses with source provenance and exact raw totals.
2. After each batch, rebuild the `202701` ranking cache, validate imported courses with `score_source=course` or `instructor_course` and `effective_n>0`, and run the term quality check. Keep progress resumable.
3. At the end, independently query the live hosted database: every current Tampa section must have own-course sourced history, positive effective N, and traceable grade aggregate provenance. Require zero `subject` and `global` fallbacks for the literal MVP-1 grade goal. If any source is unavailable, keep MVP 1 open and report the exact gap.
4. Re-run `/gsd-plan-phase 8` to revise/check the drafts against fresh live evidence. Only after the checker passes and the plans are finalized should `/gsd-execute-phase 8` run.

</remaining_work>

<decisions_made>

- Grade sourcing precedes Phase 8 execution because the controlling milestone in `PROJECT.md` requires imported history behind every section's easiness score.
- The unit of work is a course report, not a current section: one historical course aggregate can support multiple current sections.
- Each successful course import is saved to Supabase before moving on; cache rebuild and full-term validation occur after a bounded batch.
- No scoring rewrite, fabricated grade coverage, raw export commit, broad crawl or unauthorized source bypass is allowed.

</decisions_made>

<blockers>

- Approved historical exports for remaining courses have not been demonstrated; the user is sourcing them through their authenticated browser.
- Phase 8 checker cannot pass its goal-backward gate on the dated grade coverage snapshot. The four plans are drafts, not approved execution plans.

</blockers>

## Required reading

1. `AGENTS.md` and `.planning/STATE.md` — durable constraints and fresh position.
2. `.planning/PROJECT.md` and `.planning/ROADMAP.md` — literal MVP-1 and Phase 8 goals.
3. `docs/runbooks/grade-import.md` — actual import and separate live-cache rebuild commands.
4. This directory's `08-RESEARCH.md`, `08-VALIDATION.md`, `08-PATTERNS.md` and `08-01` through `08-04` plans — draft verification approach.
5. `.planning/HANDOFF.json` — machine-readable resume record.

## Infrastructure state

- Hosted Supabase is the intended live database. This session did not query or mutate it. Treat STATE.md counts as dated until remeasured.
- No API server, watcher or local async job was started by this session. The other Codex grade-import work may be active, but its state is not represented by a local job manifest.
- Existing unrelated untracked files were present before this checkpoint; preserve them.

## Pre-execution critique

- Design artifacts: four `08-*-PLAN.md` drafts in this directory.
- Critique focus: whether live grade coverage now satisfies every current section; whether the independent checker can prove Phase 8's stated goal without treating subject/global fallback as own-course evidence; and whether every live gate uses the same current term snapshot.
- Gate: do not execute Phase 8 until the checker passes and planning is finalized.

<next_action>

When the grade-import loop reports completion, first refresh the live 202701 course/section/source inventory and quality report. If every current section has sourced own-course evidence with positive effective N, rerun `/gsd-plan-phase 8` to finalize and verify the four plans. Then run `/gsd-execute-phase 8`.

</next_action>
