---
phase: 08-mvp1-p5-end-to-end-mvp-1-verification
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md
  - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md
autonomous: true
requirements: [REQ-COVERAGE-03, REQ-GRADES-01, REQ-PERF-01]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - One dated hosted-Supabase snapshot has exact target/stored/cache/API identity counts, grade evidence split, quality severities and fresh loopback HTTP search p95.
    - Phase 8 behavior and literal PROJECT.md MVP-1 completion receive separate PASS, FAIL or NOT MEASURED verdicts; no missing or unmeasured grade coverage becomes a milestone PASS.
    - Quality reports zero errors to pass, with warning/info counts and reasons visible; performance passes only on a fresh full-scale hosted-DB loopback HTTP p95 below 1.5 seconds.
    - Scoring and API contract remain unchanged per D-02, and all global effective_n=0 / subject-only sections are explicitly excluded from course-history coverage per D-20.
    - If source-grade coverage is incomplete, the report gives exact affected courses/sections and a bounded sourced-import follow-up without inventing exports, committing raw files or changing scope (D-06/D-19).
  artifacts:
    - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md
    - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md
  key_links:
    - Phase 08-01 page identities, 08-02 grade inventory and 08-03 UI results feed distinct report gates.
    - Existing Tampa validator, quality CLI and HTTP benchmark run against the same hosted database/term close to the report's UTC observation time.
---

<objective>
Gate amendment (PROJECT.md D-21, 2026-09-23): "literal MVP-1 grade coverage" below means D-21 — evidence-backed OR a listed source-limited exception; fallbacks still never count as course history.

Produce the dated end-to-end Phase 8 verification record and an honest decision on whether literal MVP 1 is complete.
Purpose: REQ-COVERAGE-03, REQ-GRADES-01 and REQ-PERF-01 need current hosted evidence; Phase 8 success criteria do not erase the PROJECT.md requirement for sourced course history on every section.
Output: 08-VERIFICATION-REPORT.md, updated 08-VALIDATION.md and a concrete follow-up if grade coverage remains incomplete.
</objective>

<execution_context>
@/home/aatif101/.codex/gsd-core/workflows/execute-plan.md
@/home/aatif101/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-RESEARCH.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-01-SUMMARY.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-02-SUMMARY.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Run one fresh hosted coverage, grade, quality and HTTP performance evidence pass</name>
  <precondition>DATABASE_URL resolves privately to reachable hosted Supabase for term 202701, and a loopback uvicorn process can serve easy_a.api.app:app using that same URL; if unavailable, record NOT MEASURED and stop before a live PASS claim.</precondition>
  <files>.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md</files>
  <read_first>scripts/verify_rankings_pages.py; scripts/inventory_tampa_grades.py; scripts/validate_tampa_ingest.py; scripts/check_data_quality.py; scripts/benchmark_rankings_search.py; .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md</read_first>
  <action>Record UTC start/end, commit, term, sanitized hosted database class and a stable snapshot basis. Run full-term Tampa validator, complete identity scanner, grade inventory, quality JSON and the existing 50-measured-call loopback HTTP benchmark with five warmups against the same hosted Supabase configuration. Start/stop the local API safely; do not measure a different local database. Verify target/store/cache/API identity and zero non-Tampa sections; compare exact stored/API term-plus-CRN sets, not just totals. Capture per-course source identifiers, historical terms, raw totals, effective-N/source split and all missing-grade courses. Capture quality error/warning/info counts and check IDs. Record HTTP p50/p95/max, query mix, response validation, denominator and environment, explicitly limiting the result to loopback API over hosted DB rather than deployed/browser latency. If a refresh changes section/cache/grade counts mid-run, repeat or label affected gates NOT MEASURED. No raw grade files or credentials enter the report (D-06/D-19). D-02 and D-20 remain invariant.</action>
  <verify>
    <automated>uv run python scripts/validate_tampa_ingest.py --term 202701 --targets config/course_targets.toml</automated>
    <fails_when>Configured targets, stored sections, cache, Tampa scope or honest global fallback checks fail.</fails_when>
    <automated>uv run python scripts/verify_rankings_pages.py --term 202701 --http-base-url http://127.0.0.1:8000</automated>
    <fails_when>Any stored term-plus-CRN is missing, duplicated or extra across HTTP pages; API metadata or ordering is unstable.</fails_when>
    <automated>uv run python scripts/inventory_tampa_grades.py --term 202701</automated>
    <fails_when>Integrity or provenance reconciliation fails; an incomplete literal grade-coverage verdict must still be captured as FAIL rather than suppressing the output.</fails_when>
    <automated>uv run python scripts/check_data_quality.py --term 202701 --json</automated>
    <fails_when>The command fails or its JSON shows any error-severity finding; warnings and info still require reporting.</fails_when>
    <automated>uv run python scripts/benchmark_rankings_search.py --live --http-base-url http://127.0.0.1:8000 --term 202701 --iterations 50</automated>
    <fails_when>Any HTTP response is invalid, dataset scale is below the full stored Tampa term, or fresh p95 is 1.5 seconds or higher.</fails_when>
  </verify>
  <acceptance_criteria>The report contains exact command outputs summarized with dates and denominators, plus a PASS/FAIL/NOT MEASURED result for each live gate; an unmet grade gate does not suppress other observations.</acceptance_criteria>
  <done>Fresh hosted evidence exists for coverage, grades, quality, honest semantics and HTTP p95.</done>
</task>

<task type="auto">
  <name>Close regression gates and issue independent Phase 8 and MVP-1 verdicts</name>
  <files>.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md, .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md</files>
  <read_first>.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md; .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md; .planning/PROJECT.md; .planning/REQUIREMENTS.md; .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-01-SUMMARY.md; .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-02-SUMMARY.md; .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-03-SUMMARY.md</read_first>
  <action>Run the full Python and frontend suites and existing search/scoring parity checks. State PostgreSQL integration passed only if EASY_A_TEST_POSTGRES_URL is actually configured; otherwise record skips. Report API response shape and score parity regression evidence, UI course/subject/global wording evidence, quality 0-error gate and full-term hosted p95. Issue one Phase 8 behavior verdict against ROADMAP.md success criteria and a separate literal MVP-1 verdict against PROJECT.md and D-15: every represented section requires sourced own-course historical distributions with effective_n>0. For each gate use PASS, FAIL or NOT MEASURED, evidence command/time, denominator and precise reason. If grades remain missing, report MVP-1 FAIL with full missing-course/section counts and a concrete bounded per-course SGDIS source/import follow-up with owner/source dependency; do not silently defer the requirement or claim success from honest fallback alone. If sources are unavailable, mark source collection as external dependency; do not synthesize grades, modify scoring (D-02), or commit raw exports (D-19). Update 08-VALIDATION.md only with actual measured status; preserve honest pending status for unavailable browser or PostgreSQL checks. Report whether source totals were verified against authenticated exports or only persisted aggregates. No RMP, alerts or deployment claim enters this phase.</action>
  <verify>
    <automated>uv run pytest -q</automated>
    <fails_when>Any Python regression fails; PostgreSQL tests skipped without a configured test URL cannot be reported as passed.</fails_when>
    <automated>uv run pytest tests/api/test_benchmark_rankings_search.py tests/api/test_rankings_search_sql.py -q</automated>
    <fails_when>Existing benchmark or API search contract tests regress.</fails_when>
    <automated>npm --prefix web test</automated>
    <fails_when>Any frontend evidence or existing component test fails.</fails_when>
    <automated>npm --prefix web run typecheck</automated>
    <fails_when>The frontend TypeScript build has errors.</fails_when>
    <automated>npm --prefix web run lint</automated>
    <fails_when>The frontend lint gate reports a new or existing failure; record the exact result rather than claiming full frontend validation.</fails_when>
    <automated>npm --prefix web run build</automated>
    <fails_when>The production frontend build fails, so the UI changes cannot be signed off as buildable.</fails_when>
  </verify>
  <acceptance_criteria>08-VERIFICATION-REPORT.md and 08-VALIDATION.md show complete measured gates, explicit limitations, exact missing-grade gap and two independent verdicts; MVP 1 passes only when every literal criterion is proven.</acceptance_criteria>
  <done>Phase 8 is closed with an evidence-backed verdict and an actionable coverage follow-up if the milestone remains incomplete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Hosted DB and API to verification report | Live mutable state requires dated, comparable denominators; stale evidence must not count. |
| Runtime secrets/raw exports to committed artifact | Report contains derived aggregates only, never URLs, credentials or raw workbooks. |
| Performance harness to milestone claim | Loopback HTTP over hosted DB proves the specified gate only, not deployed/browser latency. |

## STRIDE Threat Register (ASVS L1)
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-09 | Tampering | milestone verdict | high | mitigate | Separate roadmap behavior and literal PROJECT.md verdicts; any missing own-course history blocks MVP-1 PASS. |
| T-08-10 | Information disclosure | report | high | mitigate | Review generated report for connection strings, secrets, workbook rows and CRN-level grade buckets before committing. |
| T-08-11 | Repudiation | live evidence | medium | mitigate | Record UTC window, commit, exact commands, dataset size, source IDs and measured/not-measured basis. |
| T-08-12 | Denial of service | benchmark | low | accept | Existing bounded 50-request loopback harness and single term scope are retained. |
</threat_model>

<verification>Require fresh hosted term gates, full regression suites, source/denominator reconciliation and independent verdicts. All high-severity threat findings block a PASS.</verification>

<success_criteria>Phase 8 has a dated, reproducible evidence report and exact status for REQ-COVERAGE-03, REQ-GRADES-01 and REQ-PERF-01. MVP 1 is declared complete only if every section has sourced own-course grade history as PROJECT.md requires.</success_criteria>

## Artifacts this phase produces

- `08-VERIFICATION-REPORT.md` with per-gate evidence, two verdicts and explicit coverage follow-up if needed.
- Updated `08-VALIDATION.md` with measured test and live-gate status.

## Assumptions and measurement limits

- The existing performance gate is a single-client, 50-call loopback HTTP run against hosted Supabase, as in Phase 07. Concurrent-user p95 is not specified by REQ-PERF-01 and remains NOT MEASURED; no concurrency claim follows from this gate.
- A stable hosted snapshot is an observation assumption. If the live term changes during evidence gathering, repeat the affected measurements or mark them NOT MEASURED.
- A passing Phase 8 behavior check can coexist with a failing literal MVP-1 verdict when sourced course history is missing. Report both.

## Planning source audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | ROADMAP Phase 8 end-to-end demonstrability | 08-01 through 08-04 | COVERED |
| REQ | REQ-COVERAGE-03 full searchability | 08-01, 08-04 | COVERED |
| REQ | REQ-GRADES-01 sourced analytics and honest gaps | 08-02, 08-03, 08-04 | COVERED |
| REQ | REQ-PERF-01 full-term hosted HTTP p95 | 08-04 | COVERED |
| RESEARCH | Exact API page identities, grade/source ledger, fallback UI, quality, fresh p95, separate verdicts | 08-01 through 08-04 | COVERED |
| CONTEXT | No Phase 8 CONTEXT.md exists; PROJECT.md D-02/D-06/D-07/D-20 govern | 08-01 through 08-04 | COVERED |

<output>Create .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-04-SUMMARY.md when done.</output>
