---
phase: 08-mvp1-p5-end-to-end-mvp-1-verification
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md
  - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-D21-EXCEPTIONS.md
  - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md
autonomous: true
requirements: [REQ-COVERAGE-03, REQ-GRADES-01, REQ-PERF-01]
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One dated hosted-Supabase snapshot (UTC window, commit SHA, term 202701, sanitized environment) records section, represented-course and cache counts, the cache refreshed_at range, grade rows by historical term and the latest grade ingested_at; a second inventory run after the live gates shows the same snapshot fields, or the affected gates are repeated or marked NOT MEASURED."
    - "REQ-COVERAGE-03 is PASS only if the Tampa validator prints PASS suffix-exact and PASS reconciliation, and scripts/verify_rankings_pages.py returns verdict PASS with stored_count greater than 0, stored_count == cache_count == api_total, zero missing/extra/duplicate identities and zero non-Tampa sections."
    - "REQ-GRADES-01 is PASS under D-21 only if the inventory verdicts are integrity PASS and d21_grade_coverage PASS, 08-D21-EXCEPTIONS.md lists every exception section with reason category no_rows or non_letter_grade, the validator's verified non-letter-grade count equals the inventory's exception_non_letter_grade count, the API score-source split equals the cache split, and the 08-03 evidence-wording tests pass."
    - "The report states the evidence-backed section count and the exception count separately and never adds exceptions, subject or global fallbacks, or course rows with effective_n = 0 to course-history coverage (D-20, D-21)."
    - "Quality for 202701 reports zero error-severity findings to pass, with warning and info counts and check IDs recorded."
    - "REQ-PERF-01 is PASS only on a fresh, complete 50-call loopback HTTP run (5 warmups) over hosted Supabase at the full stored term with p95 below 1,500 ms."
    - "Concurrency and interruption for REQ-PERF-01: the gate is single-client and serial; an interrupted or partial benchmark run is never reported as PASS (rerun all 50 calls), and concurrent-user, deployed-host and browser latency are recorded NOT MEASURED."
    - "Scoring model, cache semantics and API contract are unchanged: git diff against origin/main is empty for src/easy_a/analytics, src/easy_a/rankings, src/easy_a/api, src/easy_a/models, migrations, web/src/types and web/src/api, and cache-parity and API tests pass (D-02)."
    - "No raw grade export, CSV, credential, connection string or CRN-level grade bucket is committed (D-19)."
    - "The report issues a Phase 8 verdict (ROADMAP success criteria) and an MVP-1 verdict (PROJECT.md goal read through D-21), each PASS, FAIL or NOT MEASURED; listed D-21 exceptions are the honest end state, while any unlisted section or integrity failure keeps MVP 1 at FAIL with the exact list."
  artifacts:
    - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md
    - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-D21-EXCEPTIONS.md
    - .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md
  key_links:
    - "08-01 scanner JSON (gate api_identity), 08-02 inventory JSON (gate d21_grade_coverage) plus 08-D21-EXCEPTIONS.md, the amended validator output and 08-03 test results each feed one named gate row in 08-VERIFICATION-REPORT.md."
    - "Validator, scanner, inventory, quality CLI and HTTP benchmark all run against the same DATABASE_URL and term inside one recorded UTC window."
  prohibitions:
    - requirement_id: REQ-GRADES-01
      category: transparency
      statement: "MUST NOT count listed D-21 exceptions, subject or global fallbacks, or course rows with effective_n = 0 toward evidence-backed course-history coverage in any report figure or verdict (D-20, D-21)."
      status: resolved
      verification: test
    - requirement_id: REQ-GRADES-01
      category: values
      statement: "MUST NOT propose or perform new grade sourcing or import, or mutate hosted data, as part of this phase; D-21 exceptions are the honest end state and coverage work is stopped by decision."
      status: resolved
      verification: judgment
    - requirement_id: REQ-PERF-01
      category: transparency
      statement: "MUST NOT claim deployed-host, browser or concurrent-user latency from the single-client loopback benchmark."
      status: resolved
      verification: judgment
    - requirement_id: REQ-GRADES-01
      category: privacy
      statement: "MUST NOT commit credentials, connection strings, raw grade exports or CRN-level grade buckets in the report or exception list (D-19)."
      status: resolved
      verification: test
    - requirement_id: REQ-COVERAGE-03
      category: values
      statement: "MUST NOT add RMP, seat-alert, deployment or scoring-methodology scope to MVP-1 verification (D-02, D-16, D-17)."
      status: resolved
      verification: judgment
---

<objective>
Produce the dated end-to-end Phase 8 verification record and an honest MVP-1 decision under PROJECT.md D-21.

Purpose: REQ-COVERAGE-03, REQ-GRADES-01 and REQ-PERF-01 need current hosted evidence from one snapshot. D-21 (locked 2026-09-23) is the MVP-1 grade criterion: every Spring 2027 Tampa section either has sourced own-course history (score_source course/instructor_course with effective_n > 0) or is listed as a source-limited exception (no rows, under five students, new course, or non-letter-grade course) that keeps its honest subject/global fallback with D-20 labeling. Fallbacks never count as course history, no data is invented, and grade sourcing is stopped by decision.
Output: 08-VERIFICATION-REPORT.md, the committed per-section exception list 08-D21-EXCEPTIONS.md, and an updated 08-VALIDATION.md.

Expected baseline for sanity checks only (2026-09-23 live read, not a pass condition): 3,783 sections / 1,401 courses; 3,122 evidence-backed; 661 exceptions = 311 subject + 50 global + 300 course with effective_n = 0; quality 0 errors, 1,058 low-confidence warnings, 50 no-history info; grade rows 202408 179, 202501 2,096, 202505 465, 202508 2,887, 202601 3,035 (8,662).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/grade-coverage-import-2026-09-23.md
@.planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md
@.planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-01-SUMMARY.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-02-SUMMARY.md
@.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-03-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Freeze the snapshot and carry the D-21 grade gate end to end into the report</name>
  <precondition>DATABASE_URL resolves privately to reachable hosted Supabase for term 202701, and 08-01-SUMMARY.md, 08-02-SUMMARY.md and 08-03-SUMMARY.md exist; if the database is unreachable, record every live gate NOT MEASURED and make no live PASS claim.</precondition>
  <files>.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md, .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-D21-EXCEPTIONS.md</files>
  <read_first>scripts/inventory_tampa_grades.py; scripts/validate_tampa_ingest.py; .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-02-SUMMARY.md; .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-03-SUMMARY.md; .planning/grade-coverage-import-2026-09-23.md ("Final result"); .planning/phases/05-mvp1-p2-grade-data-sourcing-import-to-supabase/05-IMPORT-RECORD.md; .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md (report layout); .planning/PROJECT.md (D-20, D-21)</read_first>
  <action>Record the UTC start time, `git rev-parse HEAD`, the branch name, term 202701 and the inventory's sanitized environment label. Run the inventory with `--exceptions-md` pointing at 08-D21-EXCEPTIONS.md so the committed exception list is machine-generated, never hand-transcribed. From its JSON, record the snapshot block, grade_rows_by_term, the window block, sections_by_state, courses_by_state, integrity counters, evidence_backed_sections, exception_sections_by_reason and both verdicts. Provenance: compare the per-term row counts with the ledger's Final result (202408 179, 202501 2,096, 202505 465, 202508 2,887, 202601 3,035; 8,662 total) and the 202408 rows and raw total with 05-IMPORT-RECORD.md (179 rows, 7,544 raw grades). Equal values give provenance PASS; any difference gives provenance FAIL with the per-term delta unless a dated import record newer than 2026-09-23 explains it. State that totals are verified against persisted aggregates and the ledger, not re-read from raw workbooks, because workbooks are outside Git (D-19) and no source is re-queried in this phase. D-21 source basis: cite the ledger for the source-side check (USF InfoCenter, Spring 2025 through Spring 2026, Summer 2026 returned no rows) and the inventory for the stored-data side (window rows, zero rows for 202605). Compare live counts with the expected baseline in this plan's objective as a sanity check only: report every delta with an explanation, and do not use the baseline as a pass condition. Run the validator; it must print PASS suffix-exact, PASS reconciliation and PASS honest-coverage, and its verified non-letter-grade count N must equal the inventory's exception_non_letter_grade section count; a mismatch is REQ-GRADES-01 FAIL. Create 08-VERIFICATION-REPORT.md with the headings "## Snapshot", "## Gates", "## D-21 grade coverage", "## Provenance by term", "## Regression and invariance" and "## Verdicts". The Gates table has one row per gate (REQ-COVERAGE-03 validator, REQ-COVERAGE-03 API identity, REQ-GRADES-01 D-21 inventory, REQ-GRADES-01 validator honest-coverage, REQ-GRADES-01 evidence wording, grade provenance, quality, REQ-PERF-01 p95, D-02 invariance, D-19 hygiene) with columns status (PASS, FAIL, NOT MEASURED or "pending Task 2/3"), evidence command, UTC time, denominator and reason. In the D-21 section, report evidence-backed sections and courses, exceptions by reason category and by score_source, a link to 08-D21-EXCEPTIONS.md, the fixed no_rows note, and the sentence "Fallbacks and course rows with effective_n = 0 are not counted as course history." If the inventory reports failure states, list each failing section and state from the JSON and do not suppress other observations. Write derived aggregates only: no connection strings, credentials or grade bucket counts.</action>
  <verify>
    <automated>uv run python scripts/inventory_tampa_grades.py --term 202701 --exceptions-md .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-D21-EXCEPTIONS.md</automated>
    <fails_when>exit code 3 (NOT MEASURED) or exit code 1; on exit 1 the verdicts block names the failing states, and the report must record REQ-GRADES-01 FAIL with them rather than omit the run</fails_when>
    <automated>uv run python scripts/validate_tampa_ingest.py --term 202701 --targets config/course_targets.toml</automated>
    <fails_when>any stdout line starting "FAIL ", a non-zero exit, or no line starting "PASS honest-coverage (verified non-letter-grade exceptions: "</fails_when>
    <automated>test -s .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-D21-EXCEPTIONS.md && grep -q "no_rows" .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-D21-EXCEPTIONS.md && grep -q "## D-21 grade coverage" .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md</automated>
    <fails_when>non-zero exit: the exception list is missing or empty, lacks the no_rows category, or the report lacks its D-21 section</fails_when>
  </verify>
  <acceptance_criteria>
    - 08-VERIFICATION-REPORT.md contains the headings "## Snapshot", "## Gates", "## D-21 grade coverage", "## Provenance by term", "## Regression and invariance" and "## Verdicts".
    - The report contains the exact sentence "Fallbacks and course rows with effective_n = 0 are not counted as course history."
    - 08-D21-EXCEPTIONS.md exists, was written by the inventory CLI, and its exception section total equals the inventory JSON's exception count recorded in the report.
    - The report records the validator's N and the inventory's exception_non_letter_grade count side by side.
    - The report and exception list contain no substring "postgresql://" or "pooler.supabase.com", and no column headed a_count, b_count, c_count, d_count, f_count, s_count, u_count or w_count.
  </acceptance_criteria>
  <done>The report has a dated snapshot, a D-21 grade verdict backed by a machine-generated per-section exception list, reconciled provenance, and a validator result consistent with the inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Run the identity, quality and p95 gates against the same snapshot and confirm it held</name>
  <precondition>A loopback server can be started with `uv run uvicorn easy_a.api.app:app --host 127.0.0.1 --port 8000 --no-access-log` using the same DATABASE_URL as Task 1; otherwise record the identity and p95 gates NOT MEASURED.</precondition>
  <files>.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md</files>
  <read_first>.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md; scripts/verify_rankings_pages.py; scripts/check_data_quality.py; src/easy_a/quality/checks.py (_check_grades is term-scoped; _check_analytics check IDs); scripts/benchmark_rankings_search.py (_report output lines); .planning/phases/07-mvp1-p4-full-scale-cache-build-search-performance-p95-1-5s/07-PERF-REPORT.md</read_first>
  <action>Start the loopback server in the background with the same DATABASE_URL and wait until a GET of /api/v1/rankings/search?term=202701&limit=1 returns HTTP 200 (at most 60 seconds, otherwise NOT MEASURED). Run the identity scanner and record its JSON verdict and counts. Cross-check its api_score_source_split against the inventory's per-score_source section counts and effective_n = 0 counts from Task 1; any difference is REQ-GRADES-01 FAIL (the API would not be exposing the cached evidence state). Run the quality CLI with --json; record section_count, error_count, warning_count, info_count and counts per check_id without pasting per-CRN findings. Note in the report that the 202701 quality run does not inspect historical grade rows (_check_grades is term-scoped), which is why the inventory's unattributed_grade_rows and bucket_sum_mismatch_rows counters cover them. Run the existing 50-call benchmark with 5 warmups and record its dataset size, environment, iteration count, p50, p95 and max lines; limit the claim to single-client loopback HTTP over hosted Supabase and mark concurrent-user, deployed-host and browser latency NOT MEASURED. If the benchmark is interrupted, rerun all 50 calls; a partial run is never PASS. Stop the server. Rerun the inventory without --exceptions-md and compare section_count, cache_row_count, cache_refreshed_at_max, grade_row_count and grade_ingested_at_max with Task 1; if any changed, repeat the affected gates once or mark them NOT MEASURED with the observed change. Do not mutate hosted data, rebuild the cache or import grades in this phase. Update the matching Gates rows.</action>
  <verify>
    <automated>uv run python scripts/verify_rankings_pages.py --term 202701 --http-base-url http://127.0.0.1:8000</automated>
    <fails_when>exit code 1 with "verdict": "FAIL" (missing, extra or duplicate identity, count disagreement, non-Tampa row, unstable order or snapshot), or exit code 3 with "verdict": "NOT MEASURED"</fails_when>
    <automated>uv run python scripts/check_data_quality.py --term 202701 --json</automated>
    <fails_when>exit code 1, or the JSON "error_count" is greater than 0</fails_when>
    <automated>uv run python scripts/benchmark_rankings_search.py --live --http-base-url http://127.0.0.1:8000 --term 202701 --iterations 50</automated>
    <fails_when>non-zero exit, an "Iterations:" line other than 50, a "Dataset size:" below the inventory's stored section count, or a "p95:" value of 1500.00ms or more</fails_when>
    <automated>uv run python scripts/inventory_tampa_grades.py --term 202701</automated>
    <fails_when>exit code 3, or snapshot fields that differ from Task 1 without the affected gates being repeated or marked NOT MEASURED in the report</fails_when>
  </verify>
  <acceptance_criteria>
    - The status of each Gates table row for REQ-COVERAGE-03 API identity, quality and REQ-PERF-01 p95 is exactly one of PASS, FAIL or NOT MEASURED.
    - The report records the scanner's stored_count, api_total, missing_count, extra_count and duplicate_count and the api_score_source_split next to the inventory's split.
    - The report records quality error_count, warning_count and info_count and the counts for check IDs low_confidence_ranking and no_historical_analytics.
    - The report records p50, p95 and max in ms, the dataset size and "50" iterations, plus the sentence "Concurrent-user, deployed-host and browser latency: NOT MEASURED."
    - The report contains a before/after snapshot comparison table for the five snapshot fields.
  </acceptance_criteria>
  <done>Identity, quality and performance evidence is recorded against the same verified snapshot, with measurement limits stated.</done>
</task>

<task type="auto">
  <name>Task 3: Close regression and invariance gates and issue the Phase 8 and MVP-1 verdicts</name>
  <files>.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md, .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md</files>
  <read_first>.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md; .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md; .planning/PROJECT.md (milestone goal, D-02, D-15, D-19, D-20, D-21); .planning/ROADMAP.md (Phase 8 success criteria); .planning/REQUIREMENTS.md; .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-01-SUMMARY.md; .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-02-SUMMARY.md; .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-03-SUMMARY.md; docs/runbooks/grade-import.md (section 2, live cache rebuild)</read_first>
  <action>Fetch origin (git fetch origin main) per D-10 and run the D-02 invariance diff, the D-19 hygiene check, the full Python suite, the search/parity tests, scoped ruff and mypy on the files this phase added or changed, and the frontend test, typecheck, lint and build commands. Record the planning-time baselines for comparison: 324 passed / 3 skipped Python, 78 passed frontend, and repo-wide ruff (2) and mypy (30 in 9 files) errors that pre-date Phase 8 and sit in files it does not touch. Report PostgreSQL integration as passed only if EASY_A_TEST_POSTGRES_URL was set; otherwise record the skip count. Fill "## Regression and invariance". Then issue the verdicts in "## Verdicts". Phase 8 verdict against ROADMAP success criteria: all Tampa sections searchable (REQ-COVERAGE-03 rows), easiness grade-derived wherever data exists (evidence-backed sections carry own-course history and every other section is a listed D-21 exception with D-20 labeling), quality 0 errors, p95 below 1.5 s on Supabase, honest data semantics (validator, inventory, API split and UI wording), API contract and scoring unchanged. MVP-1 verdict against the PROJECT.md goal read through D-21: PASS only if every gate row is PASS and 08-D21-EXCEPTIONS.md is complete; it is FAIL when any section is unlisted or in a failure state, and NOT MEASURED when a required live gate could not run. Listed D-21 exceptions do not make MVP 1 FAIL; they are the honest end state and are named as such. For every FAIL, give the exact failing sections or checks and a repair action tied to the observed state: stale_cache or history_not_used names the operator's live cache rebuild in docs/runbooks/grade-import.md section 2 followed by a Phase 8 re-verification; unbacked_course_claim, raw_total_mismatch or unclassified names a /gsd-debug investigation of attribution or cache integrity. Never propose new grade sourcing or import, a scoring change or data fabrication (D-02, D-06, D-21). Record the browser layout observation from 08-03-SUMMARY.md as measured or NOT MEASURED. Update 08-VALIDATION.md with the actual measured status of every row; set nyquist_compliant to true only if every automated test and live gate passed, and keep unavailable checks honestly pending. No RMP, alert or deployment claim enters the report.</action>
  <verify>
    <automated>git fetch origin main && git diff --quiet origin/main -- src/easy_a/analytics src/easy_a/rankings src/easy_a/api src/easy_a/models migrations web/src/types web/src/api</automated>
    <fails_when>non-zero exit: a scoring, cache, API, model, migration or frontend API-contract file differs from origin/main (D-02)</fails_when>
    <automated>test -z "$(git ls-files -- '*.xlsx' '*.xls' '*.csv')"</automated>
    <fails_when>non-zero exit: a spreadsheet or CSV export is tracked (D-19)</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit or a "failed" count in the summary line; skipped PostgreSQL tests must be reported as skipped, not passed</fails_when>
    <automated>uv run pytest tests/api/test_benchmark_rankings_search.py tests/api/test_rankings_search_sql.py tests/rankings/test_cache_parity.py -q</automated>
    <fails_when>non-zero exit or any failed benchmark, search-contract or cache-parity test</fails_when>
    <automated>uv run ruff check scripts/verify_rankings_pages.py scripts/inventory_tampa_grades.py scripts/validate_tampa_ingest.py tests/api/test_verify_rankings_pages.py tests/refresh/test_inventory_tampa_grades.py tests/refresh/test_validate_tampa_ingest.py</automated>
    <fails_when>non-zero exit or any reported violation in the listed files</fails_when>
    <automated>uv run mypy scripts/verify_rankings_pages.py scripts/inventory_tampa_grades.py scripts/validate_tampa_ingest.py</automated>
    <fails_when>non-zero exit or any "error:" line</fails_when>
    <automated>npm --prefix web test</automated>
    <fails_when>non-zero exit or a failed count above 0 in the Vitest summary</fails_when>
    <automated>npm --prefix web run typecheck</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
    <automated>npm --prefix web run lint</automated>
    <fails_when>non-zero exit or any ESLint error</fails_when>
    <automated>npm --prefix web run build</automated>
    <fails_when>non-zero exit or no "built in" line in the Vite output</fails_when>
  </verify>
  <acceptance_criteria>
    - "## Verdicts" contains a line starting "Phase 8 verdict:" and a line starting "MVP-1 verdict (D-21):", each followed by PASS, FAIL or NOT MEASURED.
    - The status of every Gates table row is exactly one of PASS, FAIL or NOT MEASURED, with a command, UTC time, denominator and reason.
    - The report's evidence-backed figure equals the inventory's evidence_backed_sections and is stated separately from the exception count.
    - Every FAIL has an exact list or count and a repair action; the report contains no instruction to fetch, source or import grade exports.
    - 08-VALIDATION.md has no "Wave 0 gap" entry left for the tests created in 08-01, 08-02 and 08-03, and states the PostgreSQL skip status.
  </acceptance_criteria>
  <done>Phase 8 closes with a dated, reproducible report, a committed D-21 exception list, and independent Phase 8 and MVP-1 verdicts that never count fallbacks as course history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hosted DB and API to report | Live mutable state needs one dated, comparable snapshot; stale or mixed evidence must not count. |
| Runtime secrets and raw exports to committed artifacts | Report and exception list carry derived aggregates and public identifiers only. |
| Loopback benchmark to milestone claim | Proves the specified single-client gate only, not deployed, browser or concurrent latency. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-09 | Tampering | MVP-1 verdict | high | mitigate | Verdict derived from gate rows; evidence-backed and exception counts reported separately; unlisted or failure-state sections force FAIL; exceptions list is machine-generated. |
| T-08-10 | Information disclosure | 08-VERIFICATION-REPORT.md, 08-D21-EXCEPTIONS.md | high | mitigate | Acceptance criteria reject connection-string substrings and grade-bucket columns; D-19 check rejects tracked spreadsheets and CSVs. |
| T-08-11 | Repudiation | live evidence | medium | mitigate | UTC window, commit SHA, exact commands, denominators and before/after snapshot comparison recorded. |
| T-08-12 | Denial of service | benchmark | low | accept | Existing bounded 50-request loopback harness and single-term scope. |
| T-08-17 | Tampering | hosted data | medium | mitigate | Phase is read-only: no cache rebuild, import or write; repairs are named for an operator follow-up, not performed. |
</threat_model>

<verification>
Fresh hosted gates from one snapshot, the full regression suites, D-02 invariance and D-19 hygiene checks, source and denominator reconciliation, and independent Phase 8 and MVP-1 verdicts. Any unmitigated high-severity threat finding blocks a PASS.
</verification>

<success_criteria>
Phase 8 has a dated, reproducible evidence report with exact status for REQ-COVERAGE-03, REQ-GRADES-01 and REQ-PERF-01, a committed per-section D-21 exception list, and an MVP-1 verdict that is PASS only when every section is evidence-backed or a listed exception and every other gate passes.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VERIFICATION-REPORT.md` (new): headings Snapshot, Gates, D-21 grade coverage, Provenance by term, Regression and invariance, Verdicts; lines "Phase 8 verdict:" and "MVP-1 verdict (D-21):".
- `.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-D21-EXCEPTIONS.md` (new, generated by `scripts/inventory_tampa_grades.py --exceptions-md`): per-section exception list with reason categories `no_rows` and `non_letter_grade`.
- `.planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-VALIDATION.md` (updated): measured status, `nyquist_compliant` set from actual results.

## Edge resolutions (spec-less probe fallback)

| Requirement | Edge | Resolution |
|---|---|---|
| REQ-PERF-01 | concurrency | explicit: single-client serial gate; interrupted or partial run never PASS; concurrent, deployed and browser latency recorded NOT MEASURED |
| REQ-GRADES-01 | concurrency | explicit: before/after snapshot comparison (Task 2) backs the 08-02 single-transaction backstop |

## Assumptions and measurement limits

- The performance gate is the Phase 07 single-client, 50-call loopback HTTP run against hosted Supabase. REQ-PERF-01 does not specify concurrent load.
- Source-side absence of rows for D-21 no_rows exceptions rests on the dated import ledger; this phase verifies stored-side absence and does not re-query InfoCenter.
- The browser layout check may be NOT MEASURED if no browser can run; the evidence wording itself is proven by the 08-03 component tests in both render paths.

## Planning source audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | ROADMAP Phase 8: MVP 1 demonstrably met end to end | 08-01 through 08-04 | COVERED |
| REQ | REQ-COVERAGE-03 full searchability and count reconciliation | 08-01, 08-04 | COVERED |
| REQ | REQ-GRADES-01 sourced analytics, reconciled totals, lacking courses recorded | 08-02, 08-03, 08-04 | COVERED |
| REQ | REQ-PERF-01 full-term hosted HTTP p95 | 08-04 | COVERED |
| RESEARCH | Page identities, grade/source ledger, fallback UI, quality, fresh p95, separate verdicts | 08-01 through 08-04 | COVERED (grade-coverage figures and sourcing follow-up in research superseded by D-21) |
| CONTEXT | No CONTEXT.md; PROJECT.md D-02, D-06, D-07, D-19, D-20 and D-21 govern | 08-01 through 08-04 | COVERED |
| USER | 2026-09-23: revise drafts, presentation-only wording for effective_n = 0, proceed without CONTEXT.md | 08-03 (wording), all plans | COVERED |

<output>Create .planning/phases/08-mvp1-p5-end-to-end-mvp-1-verification/08-04-SUMMARY.md when done.</output>
