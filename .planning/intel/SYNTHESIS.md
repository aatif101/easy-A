# Ingest Synthesis

Generated by `gsd-doc-synthesizer`. Mode: `new`. This is the entry point for
`gsd-roadmapper`. Read the per-type intel files for detail; read INGEST-CONFLICTS.md
before routing.

Precedence applied: ADR > SPEC > PRD > DOC, using the explicit per-doc ranks in
docs/gsd-mvp-manifest.yaml (0/1/2/3).

---

## Documents consumed

4 of 4 classifications consumed. All `high` confidence, all manifest-declared type
overrides. No UNKNOWN classifications.

- ADR (rank 0, locked): docs/gsd-core-mvp-prompt.md - "Ready-to-paste GSD Core prompt"
- SPEC (rank 1): docs/final-mvp-ui-spec.md - "Easy-A final MVP UI contract"
- PRD (rank 2): docs/final-mvp-plan.md - "Easy-A final MVP: evidence, policies, and seat alerts"
- DOC (rank 3): docs/live-source-drift-2026-09-01.md - "Narrow Live Source Drift Check, 2026-09-01"

Referenced by all three of the above but not itself a classified doc:
docs/gsd-mvp-manifest.yaml (the ingestion manifest).

## Cycle detection

Run on the cross_refs graph. Two 2-cycles found (plan <-> ui-spec, plan <-> prompt).
Max traversal depth 2, well under the 50 cap. Both were classified as non-derivational
companion references resolved by the manifest's strict precedence total order, recorded
as INFO, and synthesis proceeded on the full set. This was a judgment call - see
INGEST-CONFLICTS.md INFO 1 for how to force the strict blocking behavior instead.

## Decisions locked

18 locked decisions, all from the single locked ADR docs/gsd-core-mvp-prompt.md.
No LOCKED-vs-LOCKED contradiction is possible with one locked source. Mode is `new`, so
there was no existing CONTEXT.md decisions block to check against.

ADR-01 architecture preservation, ADR-02 grade-only ease, ADR-03 prior-not-substitute,
ADR-04 source identity and attribution, ADR-05 score isolation, ADR-06 seat honesty,
ADR-07 durable email monitoring, ADR-08 verified ownership not accounts, ADR-09
no-fabrication boundary, ADR-10 RMP link only, ADR-11 explicit insufficient states,
ADR-12 method defaults, ADR-13 baseline and branch safety, ADR-14 GSD Core .planning
structure, ADR-15 delivery outcome sequence, ADR-16 validation obligations, ADR-17
never invent coverage or completion, ADR-18 definition of done.

File: `.planning/intel/decisions.md`

## Requirements extracted

17 requirements. 13 carry the source document's own IDs; 4 are derived from prose
contracts in the same PRD and are marked as derived in the file.

Source-native: REQ-GRADE-01, REQ-GRADE-02, REQ-GRADE-03, REQ-GRADE-04, REQ-EVID-01,
REQ-EVID-02, REQ-RMP-01, REQ-POLICY-01, REQ-SEAT-01, REQ-SEAT-02, REQ-ALERT-01,
REQ-UI-01, REQ-OPS-01.

Derived: REQ-ALERT-02 (transactional outbox and delivery durability), REQ-ALERT-03
(tokens, privacy and abuse controls), REQ-LAUNCH-01 (declared launch coverage
manifest), REQ-DATA-01 (grade-to-course identity resolution and deduplication).

No competing acceptance variants were found. Only one PRD is present in this ingest
set, and its internal acceptance statements are consistent, so nothing was routed to a
competing-variants bucket and nothing was merged.

File: `.planning/intel/requirements.md`

## Constraints

14 constraints.

- api-contract: 8 (CON-UI-01, CON-UI-02, CON-UI-03, CON-UI-04, CON-UI-05, CON-UI-06, CON-API-01, CON-OPS-01)
- protocol: 2 (CON-UI-07 email watch screen states, CON-UI-08 required data and failure states)
- schema: 2 (CON-CALC-01 observed metric calculations, CON-CALC-02 reference cohort fallback chain)
- nfr: 2 (CON-UI-09 accessibility and browser acceptance, CON-NFR-01 acceptance budgets)

Nine come from the SPEC (docs/final-mvp-ui-spec.md, rank 1). Five come from SPEC-like
calculation, API and budget content embedded in the PRD; the classifier flagged that
PRD as containing SPEC-like material. PRD-sourced constraints are attributed and carry
PRD precedence - they lose to the SPEC and to the ADR on any contradiction.

File: `.planning/intel/constraints.md`

## Context topics

3 topics.

- Live source drift check 2026-09-01 (ingested DOC content, observational only - no
  seat or instructor value from it was promoted into requirements or constraints)
- Baseline commit state (verification note from repository inspection)
- Codebase map corroboration (verification note from `.planning/codebase/`)

File: `.planning/intel/context.md`

## Conflicts

0 blockers, 3 warnings, 10 info.

Warnings requiring user resolution before routing:

1. Stale local main with untracked work is still live. The ingest task described this
   as already resolved and suggested INFO. Verification contradicts that premise -
   `refs/heads/main` is still `d880d3c`, the user's primary checkout is on it, and
   `web/` is untracked there. This warning was deliberately not downgraded.
2. Live evidence shows every observed section in the sampled Spring 2027 Tampa scope
   has instructor `Staff`, putting REQ-RMP-01 and professor-specific grade evidence
   coverage at risk. This is a Phase 1 scope decision.
3. No current-term syllabus was found for either sampled course, making the historical
   source path primary rather than a fallback for REQ-POLICY-01.

None of these are contradictions between documents. All three are tensions between what
the documents require and what verified reality currently shows, which is why they are
WARNINGs rather than auto-resolved.

Full detail: `.planning/INGEST-CONFLICTS.md`

## Not produced here

PROJECT.md, REQUIREMENTS.md, ROADMAP.md and STATE.md are downstream outputs of
`gsd-roadmapper`. This synthesizer wrote only the four intel files, this summary, and
the conflicts report.
